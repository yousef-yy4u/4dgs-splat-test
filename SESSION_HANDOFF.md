# 4DGS Humanoid — Session Handoff (2026-07-04)

> Read this FIRST in a new session, alongside **PROJECT.md** (SSOT, Decision Log D53–D61) and
> **LHM-plusplus/BLACKWELL_BUILD.md** (env recipe). This file = the concrete current state + next steps.

> **UPDATE 2026-07-18 (D64) — dataset build STARTED; the RTX-5090 box is LOST → renting a GPU.** §5/§6's `/home/sov2/...` box and its `lhmpp`/`trellis2`/`anny` clones + `/tmp` avatar PLYs are **gone** — treat every "on disk" item below as needing re-provision on a fresh **rented** box (RunPod/Vast/Lambda; SSH-only, headless). **Platform decided:** the dataset pipeline is **pure-Python headless** (pyrender + trimesh, EGL offscreen) — Anny is a PyTorch model, so posing + multi-cam rendering is a *script*, not a game engine / GUI (answers "Unreal? Blender?" — neither, for v1). **Anny-One VERIFIED** (the §8 open question): **Apache-2.0, ungated**, 800k / 1280² / ≤40 cams, **static** (AMASS poses, HumGen3D characters, Anny params = GT label) → so **our build = the 4D animated multi-view set ONLY**; raw-Anny renders are geometry/motion supervision, not photoreal (appearance stays with Anny-One). Two license flags to verify (AMASS-posed, HumGen3D-clothed). Scripts live in **`dataset/`** (`00_bootstrap.sh`, `00_bootstrap_windows.ps1`, `01_probe_anny.py`, `02_render_slice.py`, `README.md`). **✅ EXECUTED LOCALLY (2026-07-18):** the render harness works end-to-end **on the user's Windows laptop** (conda env `4dgs-data` / Python 3.11 / torch-CPU) — Anny loads and renders a clean neutral body from 8 cameras into `dataset/slice_out/contact_sheet.png`; **pyrender works on the AMD iGPU via the default hidden-window GL context → no GPU rental needed for dev.** **Anny real API (ground truth from the probe):** `anny.Anny()` → `RiggedModelWithPhenotypeParameters`; `model()` returns a dict with `vertices [1,13718,3]` + `bone_poses [1,163,4,4]` (+ `rest_vertices/rest_bone_heads/rest_bone_tails`); **13,718 verts, 163 NAMED bones** (`root`,`pelvis.L`,`upperleg01.L`,`foot.L`,…, tree in `bone_parents`); faces are **QUADS** → use `model.get_triangular_faces()` (27,420 tris); **pose model = `'local-bone'`** (per-bone local rotations; `forward(pose_parameters=…, pose_parameterization='local-bone')`); **6 phenotype/shape axes** = `gender, age, muscle, weight, height, proportions` (via `phenotype_kwargs`); Z-up, height ≈1.626 m; per-vertex skinning at `vertex_bone_weights`/`vertex_bone_indices` (for future splat binding). **Next:** add MOTION (the 4D part) — nail the `pose_parameters` format and drive the 163 local-bone rotations over time → render M frames × N cams; vary subjects via the 6 phenotypes; then scale the full set on the rented GPU box. **✅ MOTION HARNESS DONE (`03_render_motion.py`, 2026-07-18):** Anny's pose = a `(1,163,4,4)` per-bone LOCAL-transform tensor (rest = identity; round-trip verified to 1e-6). Drive a limb by rotating its bone about the WORLD-lateral axis expressed in the bone's rest frame — `local_axis = rest_bone_poses[b][:3,:3].T @ [1,0,0]` (the poc_splat_smooth trick). A 24-frame procedural WALK (legs 2/22, knees 4/24, arms 50/76, elbows 52/78) renders a clean multi-view MOVING body locally → `motion_out/walk_strip.png` (cycle) + `multiview.png` (one instant, 8 synced cams = the training-data shape) + `verts_seq.npy`. That sequence now **PLAYS LIVE IN BLENDER** via a `frame_change_pre` handler (`Anny_Walk` object; Blender 5.1 MCP bridge working). Also added `probe_pose.py` (pose-format discovery) + `export_anny.py` (mesh→Blender). **Full local 4D loop proven: pose → animate → multi-cam render → inspect/play in Blender — all on the AMD laptop, no GPU rental.** **Next:** swap renderer to photoreal (Cycles on the rented box, per D66) + real clean motion (GEM-X/Kimodo clips, not procedural) + per-subject gaussian optimization (Milestone 0/1). See PROJECT.md **D64/D66**. **✅ SOMALayer BRIDGE PROVEN ON CPU (2026-07-18, D67):** `SOMALayer(identity_model_type="anny")` renders a posed human on the laptop (`soma_forward_test.py` → `soma_rest.png` = SOMA T-pose). API: `layer(poses[1,77,3] axis-angle, identity_coeffs[1,11], transl=[1,3])` → dict{`vertices`,`joints`}. **TWO shims** (in `soma_forward_test.py`): (1) monkeypatch anny's `get_phenotype_blendshape_coefficients` to coerce `local_changes=None`→`{}` (soma-0.1.0 ↔ anny-0.5 drift — its try/except catches only `KeyError`, not the `TypeError` from subscripting None); (2) pass `transl` (None crashes batched_skinning). **⚠️ TOPOLOGY:** SOMALayer(anny) outputs **18,056-v SOMA-resampled mesh, NOT Anny's default 13,718** → for UV/texture/splat assets keyed to the default mesh, use the FALLBACK (`anny.Anny(rig="mixamo")` + D67 retarget kernel). **REMAINING for natural motion = a real SOMA clip** (Kimodo/GEM-X): ~1GB checkpoint + diffusion, GPU-oriented → the rented-GPU step (CPU gen impractical). New: `probe_soma.py`, `soma_forward_test.py`. See PROJECT.md **D67**. **⚠️ MOTION-CLIP DOWNLOAD REALITY (2026-07-18):** a clean natural mocap clip is NOT a quick free download — **BONES-SEED** (`bones-studio/seed` on HF, CC-BY-4.0, SOMA-native, 142k real motions with text labels = the ideal clean library + the open set Kimodo trained on) is **GATED (accept-license) + only 45GB monolithic tarballs** (`soma_uniform.tar.gz` etc. — no single-clip grab, only a `metadata/seed_metadata_v004.csv`); SOMA-X's `ROM5.npy` demo motion is HF-auto-fetched (no plain URL); Kimodo generation needs a GPU. → **real natural motion = (a) a briefly-rented-GPU Kimodo session, or (b) accept BONES-SEED's license + download a SOMA tarball, then the proven SOMALayer bridge renders the tiny clip locally.** **STOPGAP SHIPPED:** `03b_walk_bio.py` = a much-improved PROCEDURAL walk applying D67 biomechanics (forward root translation + COM bounce, **posterior knee — inversion fixed**, pelvis yaw + lateral sway, spine counter-rotation, contralateral arm swing) → `motion_out/walk.gif` + plays live in Blender (`Anny_Walk`). Not mocap, but no longer doll-like/inverted. **✅ REAL CLEAN MOTION ACHIEVED (2026-07-18, D69): CMU MoCap → Anny retarget WORKS on the laptop (no GPU, no license trap).** `cmu_to_anny.py` downloads a CMU walk BVH (`una-dinosauria/cmu-mocap` GitHub, subject `data/007/07_01.bvh`, 31-joint cgspeed skeleton, Y-up), parses it, FKs per-joint WORLD rotations, and retargets onto Anny's 163-bone rig via the D67 delta-rotation kernel `P[b]=Rb.T·(C·Rw_cmu·C.T)·Rb` (C=Rx(90°) Y-up→Z-up; root translation scaled ×0.0705 to meters; CMU rest = identity-rotations so ΔR = FK world rot). Rendered `motion_out/cmu_walk.gif` + plays live in Blender (`Anny_Walk`). **Result: a clean natural walk from all angles** (legs/knees/weight-shift/foot-roll + arm swing; verified via `diag_render.py` front/side/top). **TWO FIXES over the first pass** (user reported "arms and legs inverted sideways"): (1) **reference-frame anchor** — retarget the DELTA from CMU's own frame-0 pose (`dR = Rw[j]·Rw0[j].T`), NOT its zero-rotation T-bind → fixes the T-vs-A limb PLACEMENT + facing that read as "sideways"; (2) **simplified arm map to upperarm-only swing** (`LeftArm→50, RightArm→76`; dropped clavicle/forearm/wrist which over-rotated into a bent-forward pose). Root travels +Y (forward) 3.3, lateral ~0.08 = walks straight. **FURTHER REFINEMENT (optional):** full elbow/forearm articulation needs proper per-bone rest-orientation alignment (T-pose matching) — deferred; the upperarm-swing version looks clean.
> **⚠️ DECISION (2026-07-18): STOP polishing the hand-rolled CMU→Anny retarget.** User (watching it animate) reports it's still janky — arms crossed behind the back, feet not right. Root cause: hand-rolling a BVH cross-skeleton retarget (CMU 31-joint → Anny 163-bone, T-pose-vs-A-pose arms) is genuinely hard and my delta-kernel only half-works (legs OK, arms mangled). **This is CMU-SPECIFIC and a DEAD END** — the REAL corpus sources (GEM-X / Kimodo-RP, D69) output the **SOMA skeleton, which Anny reads NATIVELY via `SOMALayer(identity_model_type="anny")` = NVIDIA's OFFICIAL SOMA→Anny retarget (no hand-rolled kernel)** → the arms-behind-back / foot-jank won't exist on that path. CMU was a valuable GPU-FREE proof that the laptop can render real mocap, but production motion = **rent a GPU → Kimodo-RP/GEM-X → SOMALayer → Anny** (clean license + clean mechanics). `cmu_to_anny.py`'s BVH parser/FK stay reusable; its retarget kernel does NOT transfer to the SOMA path (SOMALayer handles that). Blocker to clean proper motion = a rented NVIDIA GPU (~$0.30/hr); nothing clean+proper renderable GPU-free. Next: turnkey GPU scripts (bootstrap + Kimodo-RP gen + SOMALayer render) OR pick a GPU host. **▶ CHOSEN WORKFLOW (2026-07-18): run Claude Code ON the rented GPU box via Cursor Remote-SSH** (user confirmed this works). Host pick = **Vast.ai** (direct SSH out-of-the-box for Remote-SSH + cheapest RTX 3090/4090 ~$0.12–0.35/hr; RunPod needs "SSH over exposed TCP", not just its web proxy). Self-contained handoff for the box-Claude written: **[dataset/GPU_BOX_BRIEF.md](dataset/GPU_BOX_BRIEF.md)** — carries the goal, the ⛔ licensing guardrails (Kimodo-**RP** only, never -SEED/-SMPLX; Anny default/soma topology, never smplx; NVIDIA-Open-Model gate is safe to accept, BONES-SEED is not), the verified **SOMALayer shims** (None→{} phenotype monkeypatch + required `transl`), the Kimodo-RP→SOMALayer wiring, and success criteria. User gives that brief to the box-Claude → it installs, generates a Kimodo-RP walk/idle/wave, renders via SOMALayer→Anny (18,056-v topology; official native retarget = no hand-rolled jank), outputs `walk.gif`. `scp` the `dataset/` folder up too for the proven render patterns. Joint map (CMU→Anny) in the script. **This retarget is REUSABLE for GEM-X / Kimodo-RP** (feed their world rotations through the same kernel). Fetch helpers: `fetch_cmu2.py` (GitHub API nav). Next: arm T-pose-match fix → then the clean corpus (GEM-X own-video + Kimodo-RP) per D69.
>
> **UPDATE 2026-07-13 (D61) — NVIDIA open-source sweep changes the plan of action.** A license-verified scan found a deliberately-clean **NVIDIA "SOMA ecosystem"** that supplies most of §7.3's Stage 1–2 pre-built: **GEM-X** (video→SOMA motion) + **Kimodo** (text→SOMA motion) + **SOMA-X** (topology hub, **Anny is a native backend**) — Apache code + NVIDIA-Open-Model weights, trained NVIDIA-owned / **non-AMASS**. Also: **⚠️ `nvdiffrast` (used in `generation/server.py`) is NON-COMMERCIAL → replace** (Kaolin DIB-R / gsplat); Stage-3 render harness options = Omniverse Replicator (now free-for-production) or lightweight Kaolin/Warp; Stage-4 clean reference = **Lyra 1.0** (feed-forward image→standalone `.ply` gaussians); Stage-5 = **Slang** + **vk_gaussian_splatting** (sort-free rendering for the per-frame re-sort cost). **Full findings + sequenced Plan of Action (Phases A–E) in [research/nvidia-open-source-scan.md](research/nvidia-open-source-scan.md); see PROJECT.md D61.** The §7 next-steps below still hold, but Stage-2 motion + retarget is now largely a wiring job (clone GEM-X/Kimodo/SOMA-X → retarget to Anny) rather than a from-scratch build.

## 1. THE GOAL (do not lose this again)
Train **our own clean-license 4DGS model** that generates **animated Gaussian-splat avatars**, where:
- the **phone renders the splat ON-DEVICE** (no video streaming; only tiny **motion/bone data** streamed — the §2/D50 architecture),
- the splat is **photorealistic STANDALONE** (the raw gaussians look good with a normal splat rasterizer — **NO neural renderer at runtime**),
- **humanoids first** (Anny + Anny-One clean stack), animation via skinning gaussians to a body rig + per-frame LBS deform in the shader (D33/SPIKE already prototyped this).

GS is chosen specifically for photorealism. Mesh-first is the *other* product lane (D34/D46); this track is the splat lane.

## 2. WHERE WE ACTUALLY ARE (honest)
- The last long stretch built **LHM++ inference** (a pretrained model) to de-risk phone playback (Phase 0). It **works** and produced a 160k-gaussian avatar — but that was a DETOUR.
- **The core deliverable — our own training dataset + model — is NOT started.**
- **Dataset-creation setup = DOES NOT EXIST yet.** We have the Anny parametric model cloned (a building block), but Anny-One (the dataset) is NOT downloaded and we've written ZERO dataset-gen scripts.

## 3. KEY FINDING that shapes the model choice (from the LHM++ detour)
- **LHM++ is feed-forward + a NEURAL RENDERER.** Its good looks come from a per-frame neural net that decodes gaussian *features* → image. The exportable **raw splat is muted/soft** (verified: RGB per-gaussian std ~0.09 even on a colorful input).
- A neural renderer at runtime = GPU-per-viewer = server-side streaming = expensive + doesn't scale + not on-device. **Rejected by the user.**
- **Implication:** do NOT retrain the LHM++ architecture as-is (it inherits the muted-standalone problem). Our model must output **good STANDALONE gaussians** — i.e. either (a) a feed-forward arch designed for standalone quality (D55 levers: per-pixel/adaptive budget, high-res supervision, per-primitive textures à la LGTM), or (b) per-scene optimization (minutes/avatar, photoreal standalone; GaussianAvatars/3DGS-Avatar/SC-GS family, rebuilt on gsplat+Anny). **The multi-view animated-human DATASET is needed either way.**

## 4. THE DATASET (what to build next — the real spine)
Two parts (D57):
1. **Anny-One** — ~800k synthetic multi-view humans (camera params, 2D/3D joints, seg masks). Clean/commercial. Covers appearance/shape/pose/multi-view. **STATIC poses.** → just download it (verify its own download license).
2. **Our 4D/motion sequences (WE BUILD THIS)** — drive **Anny** with **CLEAN motion** (self-authored/procedural/Mixamo-audited — **NOT AMASS = NC**) and render **multi-view animated sequences** (M frames × N cameras + camera params). This temporal supervision is what makes it 4D.
→ "Dataset creation setup" = a pipeline that **poses + animates Anny bodies and renders them from N cameras over time.** Anny gives the poseable/skinnable body (`src/anny/`: anthropometry, shape_distribution, skinning, keypoints); we build the motion + multi-cam render harness (Anny is differentiable PyTorch → can render via gsplat/nvdiffrast, or rasterize meshes for supervision).

## 5. WHAT'S BUILT / ON DISK
- **conda env `lhmpp`** (Blackwell: torch 2.11+cu128, xformers 0.0.35, sm_120). Cloned from `trellis2`. Full recipe: `LHM-plusplus/BLACKWELL_BUILD.md`.
- **`/home/sov2/projects/LHM-plusplus`** — LHM++ repo (architecture reference for retrain). Weights `checkpoints/LHMPP-700M` (4.3GB, HF `3DAIGC/LHMPP-700M`, **CC-BY-NC — eval only**). Priors `pretrained_models/huggingface/...LHMPP-Prior` (7.7GB, incl. SMPL-X/FLAME/voxel_grid). Patched: `core/utils/model_download_utils.py` (disabled the hf_hub self-downgrade). Shim: `_compat/sitecustomize.py` (numpy-alias + xformers→SDPA Blackwell fix).
- **`/home/sov2/projects/anny`** — Anny parametric body (Apache-2.0). Use **default or `soma` topology ONLY** (the `smplx` topology is NC — D56 trap). Native `mixamo`/`game_engine` rigs.
- **Avatar assets** (feed-forward, muted — expected): `/tmp/lhmpp_test.ply`, `/tmp/lhmpp_avatar2_upright.ply` (160k gaussians, standard 3DGS PLY: xyz/opacity/scale/rot/SH-deg0).
- **`~/4dgs-splat-test`** — GitHub Pages phone-test repo (mkkellogg WebGL viewer, auto-orbit + FPS HUD). Avatar staged as `assets/lhmpp_avatar_160k.ply`, viewer defaults to it. **User must `git push` (no creds in headless env).** Live: https://yousef-yy4u.github.io/4dgs-splat-test/
- **`research/salvage/`** — SV4D Blackwell attention patch (reusable).
- Prior clean-stack tooling still present: `generation/bind_splat.py`, `split_by_bone.py`, `poc_splat_smooth.html` (skinned-splat rig+LBS-deform-in-shader PoC — the on-device animation prototype), `TRELLIS.2`, `UniRig`, `gsplat`.

## 6. ENVIRONMENT GOTCHAS
- **Shared box.** shnri's `qwen`/ollama (~20–26GB VRAM) + a birthd-asr service run on the single 5090. **Do NOT evict.** GPU windows open when qwen idle-unloads (watch `nvidia-smi`; LHM++ inference needs ~20GB peak). ollama is a persistent server (not a finite batch), so "free" = idle gaps.
- **Blackwell (sm_120):** compile ALL CUDA exts with `TORCH_CUDA_ARCH_LIST="9.0+PTX"` (NOT 12.0 — CUDA-12.5 nvcc can't target compute_120), `CUDA_HOME=/usr/local/cuda-12.5`. spconv must be **`spconv-cu126`** (cu120's cumm SIGFPEs on sm_120).
- **DISK: 99% full (7.9GB free).** MUST clean before downloading Anny-One (tens of GB). Fat: LHM++ priors/weights (~12GB), env caches. Other users'/projects' envs (gof 15G, spike_lam 10G) and MM2 (8.4G, user's clothing work — KEEP), LAM (11G, KEEP) — do NOT delete without asking. `conda clean -a`, `pip cache purge`, `/tmp/pip-*` are safe reclaims.
- **HF token** in `generation/.env` (`HF_TOKEN`, user yyousef4u).
- **No GitHub push creds** in this headless env — user pushes `~/4dgs-splat-test` manually.

## 7. NEXT STEPS (the re-focus)
1. **Free disk** (safe reclaims + ask about big envs).
2. **Download Anny-One** (verify its download license).
3. **Build the Anny → pose → clean-motion → multi-camera render pipeline** (our 4D dataset generator). Clean motion source only (not AMASS).
4. **Decide the target architecture** for standalone-quality gaussians (D55 feed-forward levers vs per-scene-optimized rigged splat). This determines what supervision the dataset must emit (multi-view render loss; deformable/rigged target).
5. **Wire training** (rent multi-GPU per D53; the 5090 is prototyping only).

## 8. OPEN QUESTIONS / DON'T FORGET
- Anny-One's exact download license (verify before commercial reliance).
- LHM++/DGS-LRM own weight licenses (LHM++ weights = CC-BY-NC confirmed; treat as eval-only).
- The diff_gaussian_rasterization in the lhmpp env is Inria **NC** (eval only) — clean build renders via **gsplat**.
- MM2 (clothing/drape) + LAM (headless human + FLAME lipsync) are existing user projects that feed the D57 compositional layers (clothing / face) — revisit when building attribute control.
- Runtime playback: D50 proved 600k static splats @ 45–60fps on iPhone; the **animated-deformation** (Tier-2) on-device test is still the real gate (poc_splat_smooth.html is the tool).
