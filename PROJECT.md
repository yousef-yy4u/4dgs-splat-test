# PROJECT.md — Single Source of Truth

> **Status:** PRODUCT PIVOT (D35, 2026-06-25). New lead product = **Marker-Anchored AR Asset Platform** (§0). Prior stealth-glasses / 4D-splat-generation vision (§1–§9) is NOT discarded — it is **repurposed** (generation pipeline → authoring tools; mesh-first render reality → rendering guidance; glasses → deferred endgame surface) per the continuity map in §0a. Generation/library code exists and works (see §9c).
> **Last updated:** 2026-07-18 — D69: **⛔ MOTION LICENSING VERIFIED (3-agent, license text read VERBATIM; corrects D67). REJECT BONES-SEED (`bones-studio/seed`): it's a custom proprietary contract (AIDVISORY Sp. z o.o., Poland), NOT CC-BY-4.0. Fatal for a scaling product: (1) AUTO-TERMINATES 30 days after you cross $1M revenue OR a Change-of-Control (investment/M&A/IPO/>50% transfer), and on termination you must WITHDRAW the baked-in motion from shipped products + delete the data; (2) Section 6 bans using it to build a generative model / "commercial substitute" that outputs motion/animation = our EXACT use case (disqualifying even while <$1M); (3) no-redistribution, Poland jurisdiction, $100 liability cap, unilateral term changes. Same bucket as AMASS/SMPL/Tencent → rejected. CLEAN PATH (NVIDIA Open Model License = verified genuinely UNCAPPED commercial: perpetual/royalty-free/derivatives+redistribution, NO revenue/MAU cap; but REVOCABLE + AS-IS + YOU indemnify NVIDIA for third-party claims on OUTPUTS). Taint is BY VARIANT: ✅ GEM-X (video→motion, trained on NVIDIA-OWNED data, NOT Bones-SEED — cleanest, needs own video); ✅ Kimodo-**RP** (SOMA/G1, NVIDIA-owned RigPlay data); ⛔ Kimodo-**SEED** (trained on gated Bones data → outputs arguably derivative — NEVER); ⛔ Kimodo-SMPLX (non-commercial). My earlier "NVIDIA-Open = clean regardless of training data" was TOO LOOSE — RP clean, SEED not. 🟡 CMU MoCap = free, no revenue cap, but "no resell even converted" → BAKE-into-avatars OK, standalone-library NO. PRODUCT DISTINCTION: bake-motion-INTO-avatars (looser: CMU/Kimodo-RP) vs sell-standalone-motion-library (stricter: GEM-X-own-video / own-capture). Corpus = GEM-X(own video)+Kimodo-RP core + CMU bake-only bulk + own-capture hedge → retarget to Anny. For SEEING motion now clean+no-GPU: CMU walk → retarget → render local. See Decision Log D69.** Earlier: D68: **RUNTIME AI-CONTROL ARCHITECTURE verified (survey of NVIDIA ACE / Inworld / Convai + primary sources) — the user's puppet/data-packet + separate-specialists + DeepSeek-MTP intuitions are all CORRECT and match the mainstream pattern. THE "TWO-CLOCK" SPLIT: (1) DIRECTOR = the LLM, runs PER TURN, emits text + emotion label + sparse gesture/action TAGS — NEVER joint angles/blendshapes, never per-frame; (2) ANIMATOR = on-device @30-60fps, FACE driven by AUDIO (audio→ARKit blendshapes), BODY driven by the tags (clip library + state machine). FACE = solved+clean+on-device: LAM_Audio2Expression (Apache, ONNX, CPU real-time, audio→52 ARKit) primary; Audio2Face-3D (NVIDIA Open Model, 30fps, 11 emotions) higher-fidelity; AVOID NeuroSync (CC-BY-NC). BODY = curated clean mocap library + state machine triggered by LLM tags (ACE knobs: posture/gesture/facial_gesture/position_state); NO shipping system LLM-generates joint angles; generative co-speech gesture (EMAGE/DiffSHEG/MotionGPT) is AUTHORING-TIME/desktop research + mostly NON-COMMERCIAL data → only Kimodo-SOMA-SEED is clean (NVIDIA Open Model, 288h open Bones-SEED; offline ~2-5s/clip). Non-robotic look = INERTIALIZATION on transitions + MOTION MATCHING for locomotion + always-on additive breathing/gaze (NOT a heavier model). DEEPSEEK MTP/SPECULATIVE DECODING: user right — it's LOSSLESS (full quality, ~1.8x), but it speeds SUSTAINED STREAMING (keeps TTS fed), NOT the initial response latency (that's ENDPOINTING/smart-turn VAD + warm connection + start-TTS-on-first-sentence), and it's LANGUAGE-LAYER ONLY (motion/face nets are frame-clocked, nothing to speculate). SEPARATE SPECIALISTS validated (each on its own clock; face stays audio-synced even while LLM is slow). Control packet ≈ 52 blendshapes + 163 joint angles/frame ≈ 25-30 KB/s (3 orders below video = the whole justification for on-device standalone gaussians). LLM placement: cloud director + on-device SLM fallback (Phi-3.5-mini MIT / Qwen2.5-1.5B Apache; AVOID Qwen2.5-3B custom, Llama-Community, Gemma-Terms = non-OSI; Nemotron-4-4B = purpose-built on-device digital-human SLM). See Decision Log D68.** Earlier: D67: **MOTION SOURCE DECIDED (5-source license-verified research). STOP hand-authoring motion (the sine-wave walk looked doll-like — no root translation/foot-lock, frozen pelvis, wrong-phase arms, single-peak + inverted knee). Use a real clean-license motion LIBRARY/GENERATOR: NVIDIA Kimodo (TEXT→motion, promptable, primary) + GEM-X (VIDEO→motion) → SOMA-X → Anny; all Apache code + NVIDIA-Open-Model weights, non-AMASS. DROP-IN: SOMA-X `SOMALayer(identity_model_type="anny")` outputs posed Anny VERTICES directly (03_render_motion.py already consumes out["vertices"]); `py-soma-x` is ALREADY INSTALLED. Fallback for the exact default 13,718-v mesh: `rig="mixamo"` + delta-rotation retarget kernel. ⛔ MIXAMO = RESTRICTED (FAQ bars using content to TRAIN ML models = our pipeline) → prototyping only; ⛔ AMASS/SMPL/SMPL-X = non-commercial (drive Anny via soma/anny topology, NEVER smplx); ✅ CMU MoCap = free real mocap (needs retarget). Correction: Anny `rig="mixamo"` confirmed, `game_engine` rig UNCONFIRMED. Natural walk needs (real mocap supplies all): root-translation-locked-to-foot-contacts, posterior knee bend (heel up-and-back; fix the sign per-leg — L/R mirrored), double-peak knee timing, pelvis yaw/obliquity/tilt, COM lateral sway + 2×/stride bounce, heel-toe foot roll, CONTRALATERAL arm swing, spine counter-rotation; foot-IK after retarget to kill skate. See Decision Log D67.** Earlier: D66: **CLARIFICATION/CORRECTION of D65 realism framing. The ONE rule: `output realism = training-image realism`. Photoreal images can come from real capture OR high-quality SYNTHETIC renders (Omniverse/Cycles) — realism ceiling = asset quality; the user's "render photoreal synthetic humans moving, multi-view → reproduce as animatable 4DGS" vision is VALID and adopted. D65's "render motion GRAY" was a cost shortcut for the real-capture branch ONLY; on the synthetic-photoreal path we render photoreal-AND-moving together (appearance+motion NOT separate). The ONLY true "separate" is the customization LAYERS (D63): swap needs each garment/hairstyle as its own gaussian group → optimize base body + each part SEPARATELY, compose+recolor at runtime — which also makes it AFFORDABLE (render a few bodies + a wardrobe, not body×outfit×hair combos). "The model" = PER-SUBJECT OPTIMIZATION (once per avatar/part → library), NOT a feed-forward net (feed-forward = muted gaussians needing a runtime neural renderer, D60). Audio2Face/ElevenLabs = face/voice DRIVERS, not appearance sources. See Decision Log D66.** Earlier: D65: **ECONOMICS + REALISM VERIFIED (5-agent research, primary sources). Cost fear was the WRONG regime: the D62 per-subject-avatar-library path needs ONE cheap consumer GPU (RTX 4090 24GB ~$0.25–0.35/hr spot; 3090 ~$0.12–0.20) — NOT A100/H100 — at ~$1–3 GPU / ~$5–15 all-in PER AVATAR (fit is minutes-to-1hr: GART 30s, 3DGS-Avatar 30min, GaussianAvatars ~40–60min; VRAM 11–16GB so 24GB ample). A 100-avatar library ≈ $100–1,500. The DROPPED feed-forward path = 8–24× A100/H100 for days = $1.5–8k/run (correctly out of scope). NO full dataset needed to see results — one subject in → one avatar out: Milestone 0 (~$1–3) = fit a static 3DGS to ONE Anny-One scene (proves gsplat); Milestone 1 (~$5–15) = render 1 subject × ~8 cams × 300–600 poses → bind to the 163-bone rig (LBS+blendshapes) → optimize → drive with held-out motion = first ANIMATABLE avatar. OMNIVERSE = hard NO locally (needs NVIDIA RTX ≥3070; AMD iGPU can't) AND wrong tool even on the rented box → use headless Blender Cycles+OptiX (Blender-native, matches Anny-One, same annotation passes); local viewer = Blender / browser gsplat viewer. REALISM CRUX: photorealism = f(input pixels the gaussians are optimized against), NOT the method or prettier renders — real capture in → photoreal, CG in → CG. MAIN texture path = PER-SUBJECT DENSE CAPTURE of real people (real skin baked into per-gaussian SH on the Anny rig = both the photoreal source AND the on-device deliverable); Anny-One Cycles renders = geometry/rig/coverage supervision ONLY (never the skin source); PBR-diffusion(D43)+UV libs = secondary lanes (capture-free fictional members + fill unseen regions). RENDER OUR MOTION DATASET GRAY (pyrender/nvdiffrast) — its job is geometry/motion/coverage; Cycles is ~100–1000× costlier to bake appearance we discard (reserve for a small validation set / the funded fully-synthetic premium path). Trainer MUST route appearance from capture/canonical + deformation from rig, NOT RGB-loss on gray frames. STRATEGIC FORK surfaced: photoreal library ⇒ real per-subject capture; synthetic-only Anny characters look CG. See Decision Log D65.** Earlier: D64: **DATASET-BUILD KICK-OFF + platform decided. Lost access to the RTX-5090 box → RENT a GPU (headless/SSH). The 4D dataset pipeline is PURE-PYTHON headless (pyrender+trimesh EGL), NOT a game engine — Anny is a PyTorch model, so posing + multi-cam rendering is a script, not GUI work. Anny-One VERIFIED clean (Apache-2.0, ungated, 800k imgs / 1280² / ≤40 cams) but STATIC (poses from AMASS, chars HumGen3D, Anny params = GT label) → our contribution is the 4D/animated multi-view set ONLY; raw-Anny renders = geometry/motion supervision, NOT photoreal RGB (appearance stays with Anny-One). Two license flags to verify (AMASS-posed, HumGen3D-clothed) though NAVER released the set Apache-2.0. Starter scripts written: dataset/00_bootstrap.sh + 01_probe_anny.py (API-discovery + headless render smoke test). Next: probe → render one slice → scale. See Decision Log D64.** Earlier: D63: **CUSTOMIZATION = disentangled LAYERS (refines D57) + one-page avatar spec written (`docs/avatar-architecture.md`). Keep body-shape + face identity FIXED per avatar (the "base"); hair/clothing/eyes/accessories are separate attachable, recolorable gaussian LAYERS on top. TWO kinds of customization: RECOLOR (change per-gaussian color = instant, on-device, unlimited — hair/eye/skin/clothing color) vs SWAP (attach a different pre-made part from a parts-library/"wardrobe" — hairstyle, garment). Fixing the base = hair/clothing fit ONCE (no re-fitting; varying body shape would force hard auto-refitting). Extends the data-packet/puppet model: packets also carry which-parts + what-colors. Bonus: cuts the dataset burden (train bodies+faces, NOT every avatar×outfit×hair). Hard bits: clothing drape-under-motion (MM2), hair-as-gaussians, seams. See Decision Log D63.** Earlier: D62: **FACE/EXPRESSION LAYER decided. The lip-sync/expression weakness is a SUPERVISION problem — the FLAME expression basis is the ceiling (LAM's own paper says so: no tongue, mushy visemes), not the renderer. Fix: drive the face from AUDIO + ARKit blendshapes (NVIDIA Audio2Face-3D = NVIDIA Open Model / commercial-clean; or LAM_Audio2Expression = Apache / on-device), NOT audio→FLAME-jaw; ARKit's 52 blendshapes = the expression 'vocabulary' (phone-native, speech-ready) over FLAME. Faces train the SAME synthetic-multi-expression way the body trains on multi-view-motion — the only new choice is the expression vocabulary (the 'skeleton for the face'). Composite avatar = Anny body (GEM-X/Kimodo motion) + LAM head (audio/ARKit-driven) + MM2 clothing, all on-device; the server/LLM streams only tiny control signals (joint angles + ARKit coefficients + audio) = the 'data packets'. For library heroes, per-subject dense-capture face optimization sidesteps the synthetic-FLAME ceiling. See Decision Log D62.** Earlier: D61: **NVIDIA open-source sweep (license-verified). Corrects the license read: the NVIDIA Open Model License = permissive COMMERCIAL (you may sell/redistribute the model; NVIDIA claims no ownership of outputs → outputs usable as training data) — distinct from the NON-COMMERCIAL NVIDIA Source Code License. 🎯 The 'SOMA ecosystem' = a deliberately-clean, near-complete Stage 1-2: GEM-X (video→SOMA motion) + Kimodo (text→SOMA motion) + SOMA-X (topology hub, Anny is a NATIVE backend) — Apache code + NVIDIA-Open-Model weights, trained NVIDIA-owned/non-AMASS. ⚠️ nvdiffrast (already used in generation/server.py) = NON-COMMERCIAL → must replace (Kaolin DIB-R / gsplat). Clean option now exists per stage: Replicator (free-for-production) or lightweight Kaolin/Warp for the Stage-3 multi-cam render harness; Lyra 1.0 = feed-forward image→standalone .ply gaussians (clean Stage-4 reference) + Cosmos/GEN3C data-gen; Slang + vk_gaussian_splatting sort-free rendering (Stage-5). Full scan + sequenced plan of action in research/nvidia-open-source-scan.md. See Decision Log D61.** Earlier: D60: **COURSE-CORRECTION. The LHM++ avatar splat is muted because LHM++ is feed-forward + a NEURAL RENDERER (raw standalone splat is soft; the quality lives in a per-frame neural net = GPU-per-viewer streaming = NOT on-device → rejected). Goal re-anchored: on-device standalone-photoreal animated GS, motion-data-only streaming (§2/D50). Don't retrain LHM++'s arch as-is (inherits muted-standalone); target a standalone-quality arch (D55 levers) or per-scene-optimized rigged splat. BIGGER: we detoured into LHM++ inference for hours and NEVER built the actual core — our own training DATASET. Dataset-creation setup DOES NOT EXIST (Anny cloned as a building block; Anny-One NOT downloaded; no dataset-gen scripts). Re-focus: free disk → download Anny-One → build the Anny→pose→clean-motion→multi-cam render pipeline → pick arch → train. Full handoff in `SESSION_HANDOFF.md`. See Decision Log D60.** Earlier: D59: **LHM++ RUNS END-TO-END on the Blackwell 5090 → first clean-path humanoid avatar: a 160k-gaussian standard 3DGS PLY, generated in SMPLX-FREE mode (the shippable-clean variant). Cleared a long Blackwell build; the 5 reusable keys (recipe in `LHM-plusplus/BLACKWELL_BUILD.md`): arch `9.0+PTX` not 12.0; `spconv-cu126` not cu120 (cumm SIGFPE was THE final blocker); xformers→SDPA (D52 reuse); chumpy numpy shim; hf_hub 0.34.4 + patched the self-downgrade hack. Phase-0 asset ready (`/tmp/lhmpp_test.ply`); Phase-2 clean-retrain base validated. Next: deploy to the D50 phone-test repo for on-device check. See Decision Log D59.** Earlier: D58: **Phase 0+1 execution started. Cloned LHM++ (`aigc3d/LHM-plusplus`, use over LHM) + Anny. LHM++ = 700M/1024-res/16-view+video/160k-gaussians/~8GB VRAM/exports .ply. LICENSE two-layer trap: code Apache, weights CC-BY-NC (retrain mandatory), + `LICENSE_NVIDIA` EG3D-NC CODE module (must audit+replace, not just retrain). GIFT: LHM++ has an SMPLX-FREE variant → start clean retrain there. Resolution: retrain = ~their quality clean, not higher (arch-capped). Phase 0 = I generate the avatar test asset myself (no user fetch); PARKED on shared GPU (~8GB need, 5GB free) + disk 95% full (need cleanup OK). See Decision Log D58.** Earlier: D57: **HUMANOID/AVATAR track chosen as the first 4DGS-gen spike. Anny-One (~800k synthetic multi-view humans, commercial-clean) = the training set largely EXISTS (skip building it; only 4D motion sequences remain, via clean-motion-driven Anny, NOT AMASS). LHM (aigc3d/LHM) = Apache-2.0 CODE but weights are SMPL-X-tainted → fork arch, retrain on Anny-One. Privacy resolved at the foundation (Anny synthetic + WHO averages sever the biometric link); residual = app-layer only (don't hoard uploads, train synthetic, no surveillance). Compositional layered design adopted (body=Anny sliders, + separate skin/hair/clothing modules for user-set attributes). Roadmap: Phase 0 phone-playback gate + Phase 1 stand-up repos (parallel) → Phase 2 clean retrain → Phase 3 4D motion → Phase 4 attribute layers. Still deferred-premium vs the mesh-first MVP. See Decision Log D57.** Earlier: D56: **Anny web-verified clean (scan-free: MakeHuman/MPFB2 CC0 + WHO calibration → escapes Max-Planck NC scan chain) — BUT a D41-class hidden-NC TOPOLOGY TRAP: its `smplx` interoperability topology is download-for-NON-COMMERCIAL-only. Ship rule: use Anny DEFAULT or `soma` (Apache-2.0) topology ONLY, never toggle the `smplx` flag in any shippable code path. Payoff: Anny(default/SOMA) + clean-retrained LHM = first fully commercially-clean end-to-end single-photo animatable-avatar path. See Decision Log D56.** Earlier: D55: **Deep-research VERIFIED (23 primary sources, 24/25 claims 3-0): L4GM = LGM U-Net + temporal attn, FIXED 65,536 gaussians/frame, 256px in/512px supervision, weights CC-BY-NC-SA (commercially DEAD, code Apache → retrain-clean); its training set was 200 GPU-DAYS just to render (44K obj/110K anim/48 views/300M frames). Levers to beat its blur: per-pixel/adaptive budget (GS-LRM/GRM/F4Splat), per-primitive textures for 4K w/o gaussian blowup (LGTM, LPIPS −23–75%), and near-optimization-at-feed-forward-speed DEFORMABLE recon (DGS-LRM, +3dB over L4GM) → target a DEFORMABLE multi-view-input high-res-supervised architecture on gsplat, NOT per-frame-stitch. Mixed-modality = decoupled front-ends → one reconstructor. HUMANOIDS upgraded from "avoid" to "deferred-but-viable": clean path = Anny (Apache-2.0 parametric body) + LHM architecture. Start narrow+deep, behind the D49 phone-playback gate. See Decision Log D55.** Earlier: D54: **Multi-modal "full-context" dataset (dense views + mesh GT + motions + physics + text) evaluated + L4GM-quality diagnosed. Of the 5 modalities only TWO are fidelity levers — dense angle images + 3D mesh GT; motions=coverage, physics/text=capability-not-sharpness. L4GM is blurry BY ARCHITECTURE (feed-forward amortized, low fixed gaussian budget, single-view), NOT a pixel-cap-for-smoothness (smoothness = temporal interpolation). Key reframe: a rich dataset is necessary but NOT sufficient — quality is capped by representation/budget, supervision resolution, input-view count, and feed-forward-vs-optimization (hybrid init+refine = biggest jump). "Rent GPUs, no problem" underestimates: training hours are ~⅓; the multi-TB dataset pipeline + architecture R&D iteration + eval harness are the real cost. See Decision Log D54.** Earlier: D53: **Synthetic-data (Unreal/Omniverse Replicator/Unity Perception → multi-view animated Objaverse → 4DGS) plan to train a clean-license feed-forward text/image/video→4DGS model evaluated — FEASIBLE (it's the L4GM recipe; synthetic ground truth dissolves D49's cross-view-consistency problem for free), but it improves only the GENERATION axis: cleanliness bottleneck moves OFF the engines onto MOTIONS (SMPL/AMASS=NC, Mixamo gray), CC0/CC-BY Objaverse filtering, gsplat-not-Inria fitting, and open patent FTO; "per-frame-fit-then-stitch" is the wrong supervision (prefer deformable rep / rendering-loss); sim-to-real gap → object-centric; foundation training unrealistic on the shared 5090. Runtime gates (D47/D48) untouched → still deferred-premium; D49 playback test still comes first. See Decision Log D53.** Earlier: D52: **SV4D 2.0 Stage-A RUNS end-to-end on the 5090** — `camel.gif` → 4 novel-view videos (`generation/sv4d2_out/`, ~7 min, clean). Reusable win = the Blackwell (sm_120) attention fix (monkeypatch `xformers.memory_efficient_attention`→torch SDPA, rank-aware for head_dim-512; `generative-models/run_sv4d2_patched.py`). **Stage A done; Stage B (multi-view video → dynamic splat, gsplat-rasterized) is the real remaining work.** Still gated by shared GPU (opportunistic windows; don't evict shnri's qwen). See Decision Log D52. Earlier: D51: **SV4D 2.0 self-host build COMPLETE on the 5090** (env from `trellis2` clone, 12GB weights, deps reconciled, `sgm` imports clean, validated to model-load). Key findings: (a) SV4D2's open release outputs **multi-view VIDEOS not splats** → a Stage-B 4D-gaussian recon (gsplat-rasterized, clean) is the real remaining work; (b) **license flag** — README says "research purposes," verify commercial terms; (c) box is a **shared multi-user server**, the 5090 is held by another user's live qwen3.6 service → SV4D run **parked, ready, resumes when GPU frees** (watcher armed; won't evict others' work). See Decision Log D51. Earlier: D50: **Tier-1 phone render-floor test PASSES empirically** — **~600k gaussians (3×200k objects) hold 45–60fps sustained over 2 min on an iPhone 14 @ DPR 2.0 with auto-orbit, no thermal decay** (via WebGL/mkkellogg, auto-orbit forcing per-frame re-sort) — exceeds §2's per-object budget with headroom on a 2022 phone. Refines D48's "rendering wall overstated" from argued → measured: the runtime floor is reachable. Caveats: floor only (4D adds deformation), one 200k object (multi-object TBD), one device (LOD tiers still mandatory), streaming untested. Deploy lesson: GS3D needs `worker-src blob:` + `wasm-unsafe-eval` CSP (Railway blocked both) or just GitHub Pages. Next: Tier-2 animated-deformation test, Tier-3 streaming sizes. See Decision Log D50. Earlier: D49: evaluated two image/video→4D papers. **SV4D (Stability, arXiv 2407.17470) = usable today for a pre-revenue startup** (Community License, commercial under ~$1M ARR; verify HF terms, check SV4D 2.0); **CAT4D (Google DeepMind, 2411.18613) = NOT usable** (no weights released). User's `image→multiview→per-view-videos→4DGS` pipeline is the correct SOTA shape, **but the crux is cross-view temporal consistency** — animating views independently → garbage; SV4D/CAT4D's contribution is enforcing it jointly, so use SV4D's core or retrain clean-room. Generation = per-scene optimization, **minutes/asset** (premium async, not instant), and quality doesn't touch the D48 runtime gates. **Action: de-risk in REVERSE — prove ONE SV4D output plays back on a real phone (WebGPU player, fps/thermal/size/iOS-AR) BEFORE building the generator.** See Decision Log D49. Earlier: D48: refined D47 after user due-diligence. **"Apple SHARP" = apple/ml-sharp = STATIC single-image 3DGS (non-commercial), NOT 4D**; the "4DGS-Video-Generator" repo fakes 4D by stacking per-frame SHARP PLYs (~63MB/frame, NC, no playback). **Correction owed: D47 overstated the mobile-rendering wall** — WebGPU GPU-sort shipped on iOS Safari (iOS 26) + Android Chrome, and the streaming-cost nightmare assumed continuous volumetric streaming, not our download-once-loop (~20–80MB on a free-egress CDN). **Clean-room "copy the model not the code"** = legal on copyright (gsplat precedent) but a months-long retrain gated on PATENT freedom-to-operate, not a loophole. **Still gates 4DGS to deferred-premium (not pivot):** iOS renders-in-canvas but can't world-anchor splats in AR (viewer-not-AR), ~5–30× heavier asset + mesh fallback needed, research-grade tooling, NC generation. **Updated verdict: 4DGS = a credible premium un-anchored WebGPU-device hero feature worth a spike AFTER the mesh MVP — not the foundation.** See Decision Log D48. Earlier: D47: a **pure-4DGS (4D Gaussian Splatting) animation pivot** (L4GM / "Apple SHARP" / 4D-GS → "pure 4DGS web viewer") was evaluated and **DECLINED**. Research verdict: **generation is feasible on the 5090, but RUNTIME PLAYBACK on consumer phones is a brick wall** — the most advanced commercial 4DGS streamer (Gracia) is WebGPU-desktop-only at ~80 Mbit/s, smartphones unsupported; iOS has no path at all; and the stack is non-commercial at every layer (L4GM NC, Inria rasterizer NC). It optimized the wrong latency (§3). "Apple SHARP" could not be confirmed to exist. **Mesh-first D46 stands; animated splats stay deferred R&D; today's premium "wow" = STATIC TripoSplat, not animated.** See Decision Log D47. Earlier: D46: generation architecture LOCKED. **Self-host = the user-facing ENGINE; Tripo API = back-office spare tire (overflow only, never in the user path — routing end-users' generation through a vendor IS the anti-resale violation, D45). Meshy DROPPED (UniRig rigs any category vs Meshy's humanoid-only).** Stack: mesh "Standard" lane = TRELLIS.2; splat "Photoreal" lane = **TripoSplat (MIT) self-host** (static hero tier, mesh-first still governs); rig = UniRig. **MVP animation = presets + custom-upload** (auto-rig→motion-library + transform anims + user-uploaded rigged GLB/FBX; keyframe studio + AI-assist deferred). **2D = billboard + 2D→3D** (Live2D-style 2D rigging deferred). See Decision Log D46. Earlier: D45: deep-research settled the Tripo-vs-Meshy "buy" comparison (the D43/D44 open item). Two findings reshape the call: (a) **neither sells Gaussian splatting via paid API** — Tripo's TripoSplat is MIT open-source you self-host, so "offload splatting" is impossible; (b) **our "customer uploads → we generate → they own it" reseller flow bumps both vendors' anti-resale/anti-compete clauses** (need a written white-label/enterprise agreement either way; Meshy §2.6(xi) also bars training a competing model on their output). Pricing is ~$0.30/textured mesh on both; Meshy is 4× cheaper on rig+animation (8cr vs 35cr). **Decision: KEEP self-hosted TRELLIS.2 as default ($0/asset, clean license, no anti-resale exposure); buy only for overflow — Tripo if one mesh vendor (transparent $0.01/cr + MIT TripoSplat ecosystem), Meshy only as a cheap rig/anim lever. Splatting stays self-hosted (TripoSplat MIT) regardless.** See Decision Log D45. Earlier: 2026-06-28 — D44: benchmarked TRELLIS.2-4B locally on the 5090 — EXCELLENT game-ready PBR (decisively beats our gsplat-bake), self-hosted, clean-license (with RMBG→u2net swap), $0/asset, ~80–145s/asset; **recommend adopting TRELLIS.2 as the geometry+texture backend** (Tripo "buy" comparison still pending credits). D43: our gsplat→bake is the WRONG texture method (commercial tools use multi-view PBR diffusion) — see [research/generation-texture-rigging.md](research/generation-texture-rigging.md). Earlier: 2026-06-27 — D42 deepens the no-code AUTHORING/GENERATION suite as the lead workstream (video+image→3D object/animation, animation editor + drag-drop motion + AI-assist, templates, AR scene/page editor + themes, splatting-made-easy); the four Phase-1 plumbing gaps (Stripe, R2/CDN, analytics, 2D-canvas) are recorded + deferred. See §0.5 + Decision Log D42. Earlier: 2026-06-25 (D36: a SEPARATE wearer-facing product — **AR Productivity Glasses** — was spun out into its own SSOT [GLASSES.md](GLASSES.md). Both projects are kept: this file remains the SSOT for the D35 platform; the glasses live in GLASSES.md. D35: PIVOT to a no-code, marker-anchored AR asset platform for businesses — two publish surfaces, un-anchored web "view in 3D" FIRST, anchored marker-pointer + owner-seeded per-location map SECOND. Carries D34 mesh-first + D32 library + D24–D29 generation as building blocks.)
> **Companion SSOT:** [GLASSES.md](GLASSES.md) — the personal AR productivity/dev cockpit glasses (gaze + confirm-ladder input, phone-offload). Distinct product from the platform below; shares phone-offload (§2) + cross-device discipline (§2a).
> **Maintenance rule:** This file is the canonical project log. Update it whenever a decision, plan, feature spec, or idea changes — in conversation OR in files. Append to the **Decision Log** when an idea evolves; edit the relevant section when a spec changes. Keep older decisions visible (strike-through or "superseded by") rather than deleting them, so the reasoning trail survives.

---

## 0. CURRENT PRODUCT DIRECTION — Marker-Anchored AR Asset Platform (D35, the lead product)

*A no-code platform where any business creates a 3D/AR asset and publishes it to two surfaces: **anchored** to a real place (via a physical marker that points to a cloud-hosted, owner-seeded map), or **un-anchored** as a "view in 3D" widget on a website or print.* One asset, two surfaces, no code.

### 0.1 The product in one paragraph
A restaurant owner designs an animated 3D sign (from a drawing, a product photo, or a template) and chooses how to publish. **Anchored:** scan the storefront once, place the sign in 3D, and the platform mints a **physical marker** (QR or branded image) to hang by the door — a passerby with AR glasses or a phone reads it and the sign appears welded to the storefront, staying put as they move. **Un-anchored:** the same asset becomes a "view in 3D / view in your space" widget for a website or print piece, viewable instantly in any browser — no app, no marker.

### 0.2 Why this shape (the core insight)
Precise, persistent, world-anchored AR normally needs a pre-built 3D map of the location, and building a global map is a planetary data problem. We avoid it entirely:
1. **The marker carries a POINTER, not a map.** A QR/image marker can't hold a multi-MB visual map, but it can hold a URL/ID pointing to the store's map+asset in the cloud. This turns "which of millions of world maps am I looking at?" into "load *this* map." Localization goes global→local.
2. **The map is owner-seeded and per-location.** Each business scans and publishes only its own storefront. The "global" map assembles itself one shop at a time, exactly where someone will pay to place a sign — no global scanning, no cold-start, no hard dependency on Google/Niantic.
→ A persistent AR platform that launches **today**, shop by shop, with no map infrastructure to build first.

### 0.3 Two anchoring concepts (kept separate)
- **Identity** — *which* asset belongs here. Solved entirely by the marker (read link → fetch asset). Needs no map.
- **Pose** — *where exactly in 3D* it sits and how it stays locked as the viewer moves. Solved by marker (momentary local pose) + on-device SLAM (short-range persistence) + stored map (durable, shared, multi-user persistence). The marker is excellent at identity and good at the *initial* pose lock; durability past the marker is the map's job.

### 0.4 Flows
**Owner (publish):** create asset → choose surface (anchored / un-anchored / both) → (anchored) guided phone scan of storefront (LiDAR stronger, plain camera works but lighting-sensitive) → cloud processes scan into a re-localizable map in a stable coordinate frame (bundle-adjusted, tied to coarse GPS for indexing) → place asset in 3D + preview → publish → platform stores a lightweight anchor record + **mints a marker** to print/hang.
> Stored per anchor (small): `{ anchor pose in map frame, asset ref, map ID, coarse GPS, owner, permission scope, spatial claim }`. The heavy map is stored ONCE per location, shared by every asset+viewer there.

**Viewer (consume):** device reads marker → gets map ID + asset ID → fetches that specific map+asset → localizes camera against the map (cm-accurate; owner+viewer share one coordinate frame) → renders at stored pose, welded to storefront → as marker leaves view, on-device SLAM carries the anchor; re-acquire storefront → map re-locks.
> **Coarse GPS** isn't required for the marker path (marker already IDs the exact map) but is stored for discovery/indexing ("anchors near you", pre-fetch the right map).

**Un-anchored path:** no scan, no marker, no map. Asset shown on a turntable or placed on a surface via standard WebAR/surface-tracking. Works in any modern browser on billions of phones today; embeds anywhere a link/iframe goes.

### 0.5 Authoring tools (no-code suite) — EXPANDED (D42, the lead workstream)
> **D42 (2026-06-27)** promoted this from "build one beachhead pipeline, add on demand" to the **core product surface**: a full no-code creation studio spanning generation → animation → AR scene composition. Original beachhead-first bullets are kept (marked) for the trail; the active build now leads with deepening authoring (rationale + the deferred Phase-1 plumbing in D42).

**Asset generation (input → 3D):**
- **Product-image upload** — photo → 3D object (static) **or animated 3D** (rigged). *(LIVE: `/generate_static` textured GLB; **`/generate` rigged path now productized (D42, 2026-06-27)** — a Static/Animated toggle + motion-preset picker in the dashboard produces a **high-fidelity 1024² UV-textured**, rigged GLB with a baked motion that auto-plays in `<model-viewer>` and survives the one-track USDZ bake. **NOW BACKED BY TRELLIS.2 (D44, 2026-06-28):** the muddy gsplat-bake is REPLACED — generation runs through [generation/server_t2.py](generation/server_t2.py) (TRELLIS.2-4B native-PBR, conda `trellis2`, :8077). Static = textured PBR GLB; animated = TRELLIS.2 mesh → UniRig → rig-transferred onto the textured mesh. Game-ready PBR, verified in the product viewer. Commercial-clean (u2net bg-cut instead of gated RMBG-2.0; "Built with DINOv3" attribution). )*
- **Video upload → 3D object or 3D animation** *(NEW, D42)* — reconstruct an animated asset, or extract motion from the video to drive a rig (see AI-assist). Research-grade for arbitrary subjects; humanoid/simple-rig first (see [[animax-reimplementation-findings]], [[generation-pipeline-reality-check]]).
- **2D canvas / drawing** — flat sign/graphic → 3D-presented asset. *(beachhead bullet, kept)*

**Animation (no-code):**
- **Animation editor** — keyframe/timeline + interaction triggers, no JS; built on the studio's already-working in-browser GLB skinning. *(D46: keyframe STUDIO deferred to post-MVP; MVP ships **presets + custom-upload** instead — auto-rig→motion-library + transform anims, plus user-uploaded rigged GLB/FBX.)*
- **Drag-and-drop motion library** — reusable motion clips retargeted onto an asset's rig (lowest-risk, high-value lever).
- **AI-assisted motion** — (a) **replicate movement from a video** (monocular mocap → retarget onto the rig); (b) **describe the scene/motion in text**, AI proposes keyframes. An AI helper threaded through the studio.
- **Templates** — per-use-case starting points (signage, menu items, mascots, product turntables).

**AR scene / page editor** *(NEW, D42)* — a page where the owner composes **what the viewer sees in AR**: placement/layout of one or more assets plus **backgrounds / themes** for use cases like **restaurant menus** (themed menu boards, specialized backdrops). The published-experience composer — distinct from per-asset authoring; it produces the scene the un-anchored/anchored viewer renders.

**Splatting (made easy) — re-prioritized (D42):** static splats for hero/photoreal assets, kept easy to use; **animated splats stay the hard/fragile tier (D34)** — finish the D33/SPIKE animated-splat POC (GPU/browser validation) before exposing it. Mesh-first runtime still governs (D34). *(D46: the "Photoreal" lane = self-hosted **TripoSplat (MIT)**, single-image feed-forward — neither Meshy nor Tripo sells splatting via API (D45), so GS is self-host regardless; mesh "Standard" lane = TRELLIS.2 default.)*

- **Custom marker design** — style your QR/link shape, or use a **branded image marker** (logo/sign registered as the trackable target — prettier + doubles as branding).
- **Quality tiers** — every asset ships with detail levels so weak devices keep frame rate.
> Rendering guidance baked into the tools (carries D34): **mesh-first**, static splats only as hero elements, and (anchored) design that respects depth/occlusion.

### 0.6 Architecture components
- **Authoring web app** — no-code editor + asset manager.
- **Asset store** — versioned assets w/ quality tiers (glTF/mesh, optional static splat, animations, interactions).
- **Map service** — ingests owner scans → re-localizable per-location maps; indexed by coarse GPS + marker ID.
- **Anchor registry** — lightweight records (pose, asset ref, map ID, GPS, **owner, permission scope, spatial claim**).
- **Marker service** — mints/styles markers (QR or image targets); resolves marker → {map ID, asset ID}.
- **Viewer runtime** — on glasses/phone: read marker, fetch map+asset, localize, render; handle SLAM persistence + device-specific projection.
- **Delivery/CDN** — compression + streaming of assets and maps.

**Build vs rent localization:** MVP — **rent** a map service to ship fast (**Immersal** = closest fit: REST + owner-seeded mapping + private deploy; or **ARCore Geospatial API**, free but Street-View-coverage-only + Android-only), *or* run the **marker-pointer + per-location map** path. **BUILD the owner-seeded map (COLMAP + hloc) as the long-term moat** — landmine (per D39): hloc's default SuperPoint/SuperGlue weights are **non-commercial → swap for LightGlue/DISK/R2D2 before ship** (a §4a-class trap). Azure Spatial Anchors is **retired (Nov 2024)** — don't design on it.
> **Engine — CORRECTED (D38, supersedes the earlier "open-sourced 8th Wall engine" claim):** the 8th Wall **SLAM engine was NOT open-sourced.** Only a scene-framework + Face/Image/Sky *glue* was released MIT (`github.com/8thwall/8thwall`); the actual SLAM/image-target binary (`github.com/8thwall/engine`) ships under a **restrictive, revocable "XR Engine License"** (5-day revocation, no decompile) whose **anti-compete clause explicitly targets products "whose value derives substantially from the Software" / "substantially similar or competitive" — i.e. a no-code AR-builder SaaS exactly like ours.** → **Do NOT put the 8th Wall binary at the core.** Replacement stack: un-anchored = `<model-viewer>` (Apache-2.0) + **Scene Viewer (Android) / AR Quick Look + a pre-baked USDZ (iOS — which has NO WebXR at all)**; anchored web-MVP in-browser SLAM = **Blippar or Zappar** (MindAR/AR.js as free lower-quality marker fallback); native ARKit/ARCore = the phase-two premium tier (LiDAR occlusion + durable persistence) and the glasses on-ramp. **8th Wall's VPS/Maps were NOT open-sourced either** — so owner-seeded per-location maps remain the defensible piece *we* build (moat thesis CONFIRMED, even stronger than §0.9 stated). Verified hard constraint: vanilla WebXR cannot localize against a custom cloud map on any browser, and iOS has no WebXR → the anchored surface forces WASM-SLAM or native.

**Cross-device rendering (carries §2a/D34):** don't render to a display size — describe assets in **world units (meters)** and let each device's **OpenXR/WebXR** runtime handle FOV/resolution/lens/IPD. Our job = design/budget discipline: (1) **min-FOV-safe layout** (keep key content in a central angular safe zone for narrow-FOV glasses), (2) **LOD/quality tiers** (weak HW picks cheaper tier; never drop a frame — a dropped AR frame reads as nausea; static-first, splats only where budget), (3) **additive-display design** (see-through glasses can't render black → favor bright, saturated, high-contrast assets).

### 0.7 The honest hard problem — relocalization robustness *(primary technical risk)*
Owner scans at noon in summer; passerby arrives at dusk in winter with a truck out front. A casual one-pass scan often won't re-localize. Mitigations: **guided multi-condition capture**, robust server-side map processing, **ongoing re-capture** (every marker scan is a fresh ground-truthed mapping sample that improves the map), graceful fallback to marker-only local pose when the map can't lock. Bounded engineering, not a planetary data problem — but it IS *the* technical risk to invest in.

### 0.8 Occlusion + spatial governance (build into the data model from day one)
- **Occlusion/depth** — the map must carry enough 3D geometry so assets are hidden by real walls (a sign floating *through* its wall breaks the illusion).
- **Spatial rights** — anchor records carry **owner + permission scope + a claim on a region of real space**, decided at registration, to stop competitors anchoring over a shop and AR spam at popular corners.
- **Moderation** — we're responsible for content placed onto the real world (often property the owner doesn't control) → review/permission model from day one.
- **Privacy** — owner scans + viewer cameras capture public space → process on-device, discard raw frames where possible, respect regional biometric/privacy law.

### 0.9 Go-to-market (the core strategic bet = sequencing)
1. **Ship un-anchored "view in 3D" + authoring tools FIRST.** Monetizable immediately, works on every phone/browser, proven e-commerce demand ("view in your room" lifts conversion), zero dependency on glasses or maps. Funds the business + builds the asset/creator library.
2. **Capture the 8th Wall migration.** Niantic retired hosted 8th Wall (Feb 2026); thousands of brands need a no-code home *now*. Be that home; out-execute fast-followers (ARLOOPA, HOVARLAY).
3. **Add the anchored layer as an UPSELL** to existing un-anchored customers ("you already made this sign for your site — now anchor it outside your real shop"), launched **marker-first** in dense commercial districts.
4. **Stay glasses-architected** throughout: phone is the bridge surface that funds survival; glasses are the endgame where ambient anchored AR becomes a habit, not a one-off.

**Beachhead:** one dense commercial district + a clear paying use case — storefront signage / promotions / wayfinding for local businesses, plus e-commerce product viewers for the un-anchored surface. A shop can join in minutes with a printer.
**Pricing:** un-anchored = per-seat / per-asset SaaS (proven willingness to pay); anchored = subscription per active location + creation/marker fees; premium tiers for splat capture + richer animation.
**Competitive position:** the authoring/un-anchored layer is crowded and partly free — it's the on-ramp, NOT the moat. Differentiation = **(a) capturing the 8th Wall migration with the best no-code UX** + **(b) owning the persistent, marker-indexed, owner-seeded geospatial anchoring layer** — the exact capability the market keeps proprietary and the incumbent walked away from serving brands with.

### 0.10 MVP (v1)
**In scope:** no-code authoring with **1–2 pipelines** (2D canvas + product-image = cheapest, highest-demand) · **un-anchored "view in 3D"** publishing (web embed + shareable link + print QR) · **anchored** publishing via marker-pointer + owner-seeded per-location map, viewable on **phone WebAR** first (glasses-ready architecture, phone is the day-one viewer) · **guided capture flow** w/ quality checks · **anchor registry** with owner/permission/spatial-claim fields · **cross-device output** in world units with ≥2 quality tiers.
**Out of scope (phase two):** splat capture pipeline (premium, later) · full glasses viewer app (architecture-ready, not the launch surface) · discovery / "anchors near me" browsing · rich multi-user shared experiences.
**v1 success test:** a local business can, unaided, create an asset, publish it as a web "view in 3D" widget *and* anchor it outside their shop with a printed marker, and a passerby's phone reliably shows it welded to the storefront across normal lighting conditions.

### 0.11 Key risks
- **Relocalization robustness** *(primary technical)* — must re-localize across lighting/season/occlusion/device. Mitigate: guided capture, ongoing re-capture, marker-only fallback.
- **Glasses timing** *(primary strategic)* — ambient-AR endgame depends on glasses at scale; mitigated by the phone bridge funding the wait.
- **Stickiness** — AR has been campaign-deep, not habit-deep; bet is anchored + glasses makes it ambient/recurring (unproven until glasses exist at scale).
- **Crowded authoring layer** — differentiation must come from the anchoring moat + migration capture + execution, not the authoring tools alone.
- **Spatial rights / moderation / privacy** — governance built into the data model from day one.

> *Caveat (mid-2026): AR platform pricing, 8th Wall open-source licensing terms, and geospatial API capabilities are moving fast — verify current terms + competitor feature sets before committing architecture.*

---

## 0a. Continuity map — how the prior vision (§1–§9) is repurposed under D35

| Prior work | Status under the pivot |
|---|---|
| **Generation pipeline** (TRELLIS→UniRig→bind/skin; studio; §9c, D24–D29) | **Repurposed as the authoring tools' image-to-3D / animation backend** (§0.5). Still valid + working; now serves businesses, not glasses-summoning. |
| **Asset library + retrieval / dedup moat** (§4, D32) | **Repurposed as the asset store + creator library** (§0.6). Retrieval is internal tooling; the *product* moat shifts to the geospatial anchoring layer (§0.9). |
| **Render-cost reality: mesh-first, splats = hero/luxury** (D34) | **Carried verbatim as rendering guidance** baked into the authoring tools (§0.5, §0.6). Stronger fit here than before. |
| **Cross-device / additive-display / world-units discipline** (§2a, §5.3) | **Carried as §0.6 cross-device rendering rules.** Unchanged. |
| **Animated splats spike** (D33, poc_splat_*) | **Demoted to premium/later "static splatting for hero assets"** (§0.5) — animated splats explicitly deferred as expensive/fragile. The spike work is preserved but off the critical path. |
| **Stealth smart-glasses HW + agent track** (§1, §2, §6b, §7, Track A) | **DEFERRED to the glasses endgame** (§0.9 step 4). Phone WebAR is the day-one viewer; architecture stays glasses-ready. The autonomous-agent track is OUT of the current product's scope. |
| **Physics (§5), humanoid/telepresence (§6)** | **Out of v1 scope**; may return as authoring features much later. |

> Everything below this line (§1 onward) is **PRIOR FRAMING** — kept for the reasoning trail. Where it conflicts with §0, **§0 wins**.

---

## 1. Vision  *(PRIOR FRAMING — superseded as the lead product by §0/D35; the glasses are now the deferred endgame surface, the agent track is out of current scope. Kept for the trail.)*

AI **stealth smart glasses**: look like normal glasses, offer a new experience. Two goals drive everything:
1. **Stealth** — discrete I/O (bone-conduction audio out, wearer-only monocular HUD, discreet good camera). Looks/feels like normal glasses.
2. **Autonomous agent** — full-context AI that manages daily life (scheduling, reminders, shopping, conversations) and can drive phone apps (e.g. open Spotify, pick a song).

Layered on top: **real, physics-enabled 3D objects** rendered into the world via Gaussian splats, and **humanoid telepresence / AI companion** avatars.

### Build order: SOFTWARE FIRST, hardware LAST (committed)
**Strategy A confirmed.** Build software → prototype on the user's iPhone → physical hardware is the LAST step (much later). Hardware constraints are studied now only to derive the requirements the software must respect (see §2a), NOT to build hardware yet. **The phone is both the prototype display AND the real offload-compute device — so phone-prototype work IS the architecture, not a throwaway.**

### Two separable tracks (build independently)
- **Track A — Agent glasses.** Valuable with ZERO splatting: mic + HUD + LLM + phone control. This is the daily-driver value.
- **Track B — 4D splat generation + rendering.** "Summon a photoreal animated object into my room." The flagship demo; the riskiest, least-proven tech.

**Decision:** Tracks are architecturally independent. Track A doesn't depend on Track B. (Originally Track A was "build first"; current focus has shifted to de-risking Track B's rendering core — see §9 Next Steps.)

---

## 2. System Architecture (3-tier compute)

```
[ CLOUD / EDGE SERVER ]  — NVIDIA GPUs. Heavy OFFLINE generation, user mgmt, global library.
        │  (streams: .splatmesh asset ONCE, then tiny live bone matrices)
        ▼
[ PHONE ]  — compute offload. Runs SLAM, physics, lighting estimation, splat RENDERING.
        │  (streams frames / poses to glasses; glasses do late-stage reprojection)
        ▼
[ GLASSES ]  — sensors + discrete I/O. Camera, depth/LiDAR, HUD, bone conduction, IMU.
              Lightweight accessory to the phone. Never does heavy lifting.
```

**Rigid split:** cloud GENERATES (heavy, one-time), the **offload device** RENDERS + ANIMATES + simulates physics (cheap, realtime), glasses DISPLAY + reproject.

### Adaptive offload device (the "render tier" is not fixed)
The offload device varies by context; the system detects capability and scales quality/LOD/splat-budget to match:
- **Out / commuting → phone** (the baseline, mobile-first). MUST work across a range of phones (see below), not just flagships.
- **Home / office → computer** (stronger GPU). Unlocks more: more complex objects, more on-screen at once, heavier workflows.
- (Future) dedicated wearable compute puck = another tier.

**Framerate tiers (D21):** make 45 vs 60 fps TWO quality tiers, not one global choice. **"Rich" = lock 45fps + max splats** (static/slow hero objects; relies on reprojection to keep head-motion smooth). **"Motion" = 60fps + fewer splats** (animated/fast content). Default to 60 as safe; 45-max-splats is a STRETCH tier trusted only once the warp engine (D19) is built — a stable capped rate always beats a fluctuating one.
**Runtime LOD by decimation (NOT regeneration):** to render fewer splats from an existing file, just SELECT a subset — importance-order once by (opacity×size), render top-N. Generate the asset ONCE at high quality; precompute a few discrete LOD levels offline (500k/250k/100k = cheap selection, seconds); switch dynamically at runtime by budget/distance/object-count. Random subsample = holes/popping; importance-order = graceful. (Advanced later: hierarchical merge of dropped splats.) This IS the adaptive runtime LOD the design assumes — never regenerate for performance.
**Benchmarking fairness:** splat render is FILL-RATE-bound → render resolution dominates cross-device comparison AND defines the budget (resolution ↔ splat count trade directly). `Res: 1280x720` toggle = a FAIRNESS BASELINE only (arbitrary, NOT a glasses spec) — use it to settle GPU-vs-GPU + a rough ceiling. **Real glasses res is PER-EYE and TBD** (hardware phase), ~1000–1920 px/eye → dual ~1200×1200 ≈ 2.9M px total vs 720p's 0.92M, so glasses push 2–4× more pixels → the TRUE budget is LOWER than 720p shows. Also: current stereo mode splits the canvas (each eye = half width), so 720p-stereo per-eye is optimistically low. TODO: add a "glasses preset" (dual ~1200×1200/eye) to measure the real product budget. Resolution itself = an adaptive budget knob (render below native + upscale = more splats). Cross-browser (Chrome vs Safari) = minor variable; bigger confounds = resolution/DPR + phone power/thermal throttle. (Note: modern Ryzen iGPU can genuinely beat a thermally-limited phone — trust the data over the earlier A15>iGPU assumption.)
**Per-object count reality:** a single AR object is object-scale, not scene-scale — **~100–300k splats looks high-res for one object** (~100k often plenty; 750k = diminishing returns, hero/close only). Budget = TOTAL on-screen splats (one 300k hero OR three 100k objects). Good news: modest phone budgets support convincing objects.

**Consequence:** the splat budget is a **tiered/adaptive budget**, not one number. Render path must support runtime device-capability detection + per-tier LOD/quality scaling. **Mobile is the from-the-get-go optimization target**; desktop is an *enhanced* mode, never a *required* one.

---

## 2a. Software requirements derived from hardware constraints
Whatever glasses we eventually run on (Snap, Meta, or our own), they share these traits. Software must respect them from day one:
1. **Render for an ADDITIVE display** — never rely on occlusion to look solid. Full opacity, saturated color, contact shadows/AO (biggest "it's really there" cue), avoid bright backdrops. (D12/D13)
2. **Respect a tiered splat budget + per-tier LOD** — ~100–300k splats target; benchmark sets exact per-device numbers. (D9/D10)
3. **Tolerate phone-offload latency** — stream POSES not geometry; on-device reprojection/timewarp for low motion-to-photon. (§2/§3)
4. **Work within narrow FOV (~50°)** — small viewport, no full-peripheral assumptions.
5. **Additive-only scope** — place virtual objects; NO diminished reality. (D11)
6. **Phone = prototype display AND offload compute** — the iPhone AR app built first IS the architecture; what runs on phone now streams to glasses later.

## 3. The Two Latencies (do not conflate)

| | **A: Time-to-first-render** | **B: Steady-state render + animate** |
|---|---|---|
| When | Once per *new* object | Every frame, forever |
| Requirement | Tolerable load wait | **Realtime 60–90 fps** |
| Where | Cloud GPU | Phone SoC + glasses reproject |
| Realistic today | **~15–40s** (optimizable to ~5–15s) | **Feasible at ~100–300k splats** |

The realtime/cheap requirement applies ONLY to B. A is a load screen, not a realtime budget.
**The original "1.5–2.5s for the whole pipeline" claim was a fantasy** — see Decision Log D1.

### Generation pipeline (Latency A, one-time, cloud)
| Stage | Realistic | Notes |
|---|---|---|
| Text → image (skip if image input) | 1–3s | Flux / SDXL-turbo |
| Image → 3D gaussians+mesh | 10–30s | **TRELLIS / TripoSR / LGM** (all MIT). AVOID InstantMesh (Zero123++ = NC) & Hunyuan3D-2 (geo/MAU-restricted). Output splats directly. See §4a. |
| Auto-rig (animatable only) | ~0.5s | **UniRig (MIT)** — AVOID RigAnything (Adobe NC); Make-It-Animatable = Mixamo-data taint. Arbitrary topology = open problem. |
| Bind splats → mesh triangles | <1s | Geometric nearest-triangle, NO per-asset training |

**Key lever:** use a feed-forward generator that emits Gaussians directly + bind geometrically. This avoids GaussianAvatars-style per-asset photometric optimization (minutes–hours), which is NOT a realtime baker.

### Runtime (Latency B, per-frame, phone)
`joint angles → LBS deform mesh + bound splats → rasterize → display`
- Deform + binding: negligible cost.
- **Gaussian rasterization is the real bottleneck.** Budget **~100–300k splats** for stereo 60–72 fps on phone-class SoC. "Millions of splats" breaks it. Use LOD + culling + foveation.
- **Motion-to-photon:** phone renders, glasses do **late-stage reprojection (timewarp)** to keep latency < ~20ms.

### Animation source (separate one-time cost)
AI-generated motion (AnimaX-style) = seconds–minutes per clip, then playback is free. Mocap/physics/pre-canned = instant.

---

## 4. Asset Library Architecture (the economic core & data moat)

**Principle:** generation is NOT a runtime step. Build a growing, deduplicated library; at runtime do RETRIEVAL with generation as the rare miss path. Library compounds with usage → marginal cost → ~0. This is the network-effect **moat**.

### Data model: Category → Class → Instance
- **Category** — semantic, for browse/search ("chair"). Contains many classes.
- **Class** — shared **mesh + rig + physics/collision params** ("wooden 4-leg dining chair"). The geometry unit.
- **Instance** — an **appearance splat** on a class + optional metadata (brand/price/SKU) + owner.

> A couch and a rocking chair are SEPARATE CLASSES under the "chair" category — different geometry & physics, can't share a mesh. "Versions of a chair" = separate classes, not instances.

**Class match ≠ appearance shared:** when a scan matches a shared class, reuse the class (mesh/rig/physics) but the captured splat stays a **private instance** pointing at that class. *Shared geometry, private appearance.*

> **Critical constraint:** splats are bound to mesh triangles, so "same mesh, different splat" only works when variation is APPEARANCE-ONLY (Doritos flavors, book covers, colorways). For variable geometry (furniture, organic) it breaks. For STATIC props (most everyday objects), skip the shared mesh entirely — store per-instance splats + a cheap collision proxy; "class" is then just a retrieval/dedup grouping. Mesh-sharing's real payoff is ANIMATABLE classes (rig genuinely reused).

### Scopes = ONE pool, two metadata flags (NOT three databases)
- **Visibility:** `global` | `marketplace` | `private`
- **Curation:** `hand-curated` | `provider-verified` | `user-generated`

| Scope | = | Contents |
|---|---|---|
| Global | (global, hand-curated) + auto-grown generics | generic everyday objects |
| Marketplace | (marketplace, provider-verified) | buyable products, providers self-upload, commerce metadata (price/SKU/checkout) |
| Private | (private, user-generated) | user scans/creations + ALL humanoids; premium-tier feature |

### Write-time ROUTER (replaces naive "discard/keep")
On every scan, one classifier (you already need a humanoid detector — extend it) routes:
- **Person/face →** private humanoid profile
- **Branded / copyrighted / IP →** block, or keep private-only (never shared)
- **Buyable product →** marketplace scope
- **Generic non-branded object →** flat shared pool, **dedup-on-write** (similar → reuse existing, different → keep)

> Dedup-on-write IS the flywheel. But "no search" is an illusion — the similarity check IS the retrieval layer, moved to write-time where it needs HIGHER precision (a wrong discard deletes user data). Still needs embeddings + ANN index.

### Other library decisions
- **Cold-start:** seed offline from **Objaverse-XL (~10M) / ShapeNet** converted to our format. Don't launch empty.
- **Multi-view capture:** on a miss, exploit that glasses see a VIDEO stream — capture multiple frames as the user looks around the object. Far better than single-snapshot→3D (which hallucinates unseen sides).
- **Library janitor:** dedup + quality-scoring + curation, or retrieval DEGRADES with growth. Self-improving requires self-pruning.
- **Cross-scope retrieval ranking policy** (business decision): private (personalization) vs marketplace (revenue) vs global (fallback).
- **Predictive device cache:** glasses cache assets predictively via agent context (location/time) — the context engine is the prefetcher.
- **Progressive enhancement on the FOCAL object:** show retrieved proxy immediately → hot-swap accurate generated asset when ready. Silent substitution ONLY for ambient/background props.
- **"Costs nothing" caveat:** marginal cost → 0, but standing CDN/storage/index + long-tail gen remain. Reality = "cheap, dominated by serving," not "free."

---

## 4a. Software stack licensing (commercial-use audit — VERIFIED)
For a COMMERCIAL product. Several popular models/datasets are non-commercial landmines; the clean stack below avoids all of them. (Engineering summary, not legal advice — counsel to confirm before ship.)

**🟢 CLEAN COMMERCIAL STACK (use these):**
- **Generation:** TRELLIS (MIT), TripoSR (MIT), LGM (MIT)
- **Auto-rig:** UniRig (MIT) — the safe rigging choice
- **Motion:** Wan2.1 (Apache-2.0) — safest base; AnimaX unreleased, re-audit at release
- **Render:** mkkellogg/GaussianSplats3D (MIT), PlayCanvas + SuperSplat (MIT)
- **Compress:** Draco (Apache), meshoptimizer (MIT), KTX2/Basis (Apache); splat = PlayCanvas **sogs / splat-transform / PLAS** (Apache/MIT)
- **Retrieval:** CLIP (MIT code), DINOv2 **current Apache-2.0 weights** + vector DB
- **Seed data:** Objaverse **filtered to CC0/CC-BY only** (persist per-asset license + attribution)
- **Parametric (humanoid, future):** FLAME **2023 Open (CC-BY-4.0)** or Meshcapade-licensed

**🔴 LANDMINES (do NOT ship):**
- **Original 3DGS (Inria) + diff-gaussian-rasterization** — explicit NON-COMMERCIAL. Never ship this code or derivatives; use permissive renderers + clean-room optimization. **⚠️ Hidden trap (D41): TRELLIS's textured `to_glb` imports this Inria rasterizer to render the gaussian for texture baking** — so the obvious "textured GLB" path is a landmine. Clean fix: monkeypatch the render step to **gsplat (Apache-2.0)**, then bake with **nvdiffrast** (permissive). See `generation/gsplat_render.py`.
- **SOG research repo (Fraunhofer)** — inherits Inria NC → use PlayCanvas sogs/PLAS instead.
- **InstantMesh** — Apache code but Zero123++ weights = CC-BY-NC → blocked; swap multi-view stage.
- **Hunyuan3D-2** — NO rights in EU/UK/South Korea; >1M MAU needs a Tencent license.
- **RigAnything** — Adobe non-commercial → hard blocker; use UniRig.
- **ShapeNet** — non-commercial, no path → exclude; use filtered Objaverse.
- **SMPL (+ SMPL-X/AMASS/most body rigs)** — non-commercial AND taints models trained on it → Meshcapade commercial license or avoid.
- **FLAME (non-2023 variants)** — non-commercial → use FLAME 2023 Open or license.
- **DINOv2 pre-Aug-2023 weights** — were CC-BY-NC; pin to current Apache weights; avoid biology variants.
- **Make-It-Animatable** — MIT code but Mixamo/Adobe skeleton data → retarget to owned skeleton or diligence.
- **Tripo platform/API** — free tier = non-commercial output; need paid tier for commercial rights.

## 5. Physics (EASY TIER ONLY — current scope)

**Mental model:** physics runs on a hidden **collision proxy** (box/convex hull/compound), NOT the splats. Splats are drawn wherever the proxy lands. Engine = Bullet/Jolt/PhysX on the phone, realtime.

**In scope (all feasible, realtime today):**
- Move / rotate anywhere ✅
- Float midair (mark body **kinematic**, gravity off) ✅
- Drag around midair ✅
- Drop / fall (mark **dynamic**, gravity on) ✅
- Bounce, settle, rest (restitution + friction) ✅
- Throw, knock over, stack, collide with other virtual objects ✅

**Caveat — collide with the REAL world:** needs the environment inside the physics sim.
- Floor/table **plane detection** → easy, no special HW.
- Arbitrary furniture/edges → needs real-time room mesh from **depth/LiDAR**.

**Physics params live on the CLASS** (mass, restitution, friction, collision proxy).

**OUT OF SCOPE for now (hard tier — deferred):** breaking/fracturing, cutting, soft-body squish/bend. Reason: splats are a **hollow shell** — breaking exposes interior never captured (no appearance data); also requires pre-authored fracture pieces. Revisit later as a per-object authored "hero" effect only.

---

## 6. Humanoid / Telepresence (the emotional core)

- Each user has a **private humanoid avatar profile**.
- Add **family/friends** as AR avatars.
- **Shared visual sessions** on a call → "feel like you're in the same room."
- **AI companion avatar** that walks/sits with the user and converses naturally.

**Clear-eyed cost note:** humanoids are kept PRIVATE (correct for privacy) but are ALSO the most expensive to produce (GaussianAvatars-style per-subject optimization + live face/body tracking to drive them) and the LEAST dedup-able (every person unique → no flywheel, full cost per user). **The real cost center is the humanoid pipeline, not the object library.**

**Build order within this track:** AI companion avatar first (single asset, optimized offline once) → then live human↔human telepresence (real-time capture + drive on both ends = hardest).

---

## 6b. Input / Interaction (HOW the user drives the HUD)
**Was a gap — now spec'd.** Output/content was fully designed; input wasn't. Camera hand-gestures are feasible (outward camera + phone CV) BUT as PRIMARY HUD navigation they fight the stealth core: air-waving is the most conspicuous possible interaction, causes gorilla-arm fatigue, and draws power. Wrong default for a stealth device.

**Key distinction — two different jobs, different inputs:**
- **HUD/menu/agent navigation** (all-day, in-public) → must be DISCREET.
- **3D object manipulation** (grab/move/rotate a splat object) → hand gestures genuinely shine here; conspicuousness justified by deliberate engagement.

**DECIDED — dual-mode (two contexts):**
- **Stealth mode = SILENT SPEECH.** Discreet, all-day/public. Command grammar ("open Spotify", "next", "summon X"). Limited expressiveness, invisible to bystanders.
- **Power mode = FULL HAND MOTION.** Spatial/direct manipulation — point midair & interact, grab/move/rotate objects. Fast & comfortable, conspicuous → used in private (home/office). Covers BOTH 3D object manipulation AND general HUD control.
- They complement (different contexts), not overlap. User accepts the tradeoff (stealth=limited, power=visible).

**RISK + mitigation:** silent-speech IN (subvocal/EMG) is the LEAST-proven tech in the project (AlterEgo = lab; Meta neural = wrist not throat) — highest-risk input bet. **Mitigation: prototype stealth mode with ORDINARY VOICE as stand-in** — identical command grammar/agent intent, only capture differs (mic now → EMG sensors at HW phase). Entire stealth UX buildable today; risk lives in production HW, not current software. **Optional cheap backstop: keep temple touch** as a discreet fallback if silent speech underdelivers.

**Open sub-decision (non-blocking):** mode switching = auto-by-context (private/public via location) vs manual toggle. Settle during HUD UX design.

**HW implications (all production-phase):** hand-tracking = existing camera + phone CV (CV runs on phone; activate contextually, not always-on); silent-speech = +EMG/subvocal sensors (immature); temple-touch backstop = cheap.

**Where hand tracking is processed = PHONE, not glasses.** Principle: TWO kinds of real-time. Head-pose reprojection = HARD real-time (<10ms → must be on glasses, wireless round-trip blows it). Hand interaction = SOFT real-time (~50ms tolerance, usable to ~100ms → phone round-trip fits). So hand-pose CV + gesture + interaction run on the phone alongside SLAM (cameras already stream there); glasses stay minimal (camera + display + warp). The two budgets LAYER: phone sets object world-position at hand-latency, glasses reprojection keeps it head-locked at <10ms → no swim even with phone-latency hand updates. Failure mode: bad link → grabbed object trails hand (rubber-band); mitigate with fast link + optional on-glasses prediction for the actively-held object. Optional later optimization: on-glasses hand-keypoint detection to stream keypoints not frames (bandwidth+privacy). Prototype = all phone anyway (iPhone camera + ARKit).

## 7. Hardware (Track A / glasses)

Target: looks like normal glasses; discrete I/O; comfortable; ~8h battery; no heat issues.
Components: bone-conduction audio out, wearer-only monocular HUD (waveguide), discreet camera, depth/LiDAR (for physics + occlusion), IMU.

### Glasses-side compute = split/distributed AR (thin glasses, heavy phone)
Our model independently = Qualcomm Snapdragon AR2 design philosophy. Glasses do only: (1) receive phone stream, (2) drive microdisplay + electrochromic dimming (the D12 work), (3) **reprojection/timewarp** (warp last frame to current head pose via onboard IMU so world-locked objects don't swim). That reprojection is the "one hard job" — display-adjacent, NOT full rendering. Heavy splat render stays on the phone.

**Component spectrum:**
- **Dumb tethered display (Xreal/Rokid class):** display driver + USB-C only, no compute/reprojection. World-lock weak (floating screens OK, anchored objects swim). **~$300–500 whole glasses, off-the-shelf TODAY.**
- **Thin AR + co-processor (Snapdragon AR2 class):** + sensor hub + onboard reprojection; solid 6DoF; offloads render to phone. = our target tier. NOT a hobby part — reference-design/ODM/NDA/volume, a months-long hardware program. Belongs in the LAST phase.
- **Standalone (Snap dual-SoC):** everything onboard, $$$ — what we avoid.

**Prototyping decision:** phone-only now; defer glasses board to production. Cheap "feel it in-glasses" shortcut = drive an off-the-shelf Xreal/Rokid USB-C display (~$300–500) from the phone, zero HW engineering (imperfect world-lock, feel-prototype only). Real co-processor = final hardware phase via ODM.

**Can the phone do ALL the display compute? Almost — reprojection is the irreducible glasses-side sliver.** Split the "display" into: (1) physical optics/panel/dimming = always on glasses (hardware, not compute); (2) heavy compute (render, SLAM, apps) = 100% phone ✅; (3) **reprojection/timewarp = MUST stay on glasses** to match Specs' world-lock (7ms motion-to-photon). Physics: warp must happen at the very END of the chain using the freshest pose — if the phone does it, the result travels back over the link and re-adds the latency the warp exists to hide. Zero glasses compute (dumb USB-C display) still works but drops to floating-screen tier (objects swim). KEY WIN: Specs uses dual Snapdragons because it does EVERYTHING onboard (standalone, no phone); we keep ONLY the warp local → **Specs-grade display quality with a tiny fraction of Specs' chip** (cheap FPGA/warp ASIC, not a GPU/SoC). Irrelevant to the phone prototype; production-phase part.

**CHIP-AGNOSTIC — NOT locked to Snapdragon (either end).** The glasses never do heavy render (phone does), so they only need a modest display+warp+sensors engine. Options, cheapest→turnkey: small **FPGA / display-warp ASIC** (Lattice/AMD — best fit, glasses' job is fundamentally an image-warp, lowest latency, no NDA) → general **ARM SoC** (MediaTek/Rockchip RK3588/NXP i.MX — cheap/open, more DIY pipeline) → **dumb USB-C display, no SoC** (Xreal/Rokid, weak world-lock) → **Snapdragon AR2** (turnkey but pricey + NDA + lock-in). Phone side also not Snapdragon (prototype = iPhone/Apple silicon; production = any phone). Silicon choice = production-phase decision, changes nothing about phone software now.

### Display capability ceiling: ADDITIVE-ONLY (decides what's renderable)
The stealth HUD is **optical see-through** = it ADDS light, cannot SUBTRACT. Consequences:
- ✅ Can do: place/add virtual objects, labels, HUD, *brighter* overlays.
- ❌ Cannot do: hide/remove real objects, darken or repaint real surfaces, full occlusion. Virtual objects look slightly ghostly over bright backgrounds.
- **Environment manipulation ("change wall color / hide my bed / replace real objects") = Diminished/Mediated Reality, which REQUIRES video passthrough (opaque goggles, editable per-pixel) — the OPPOSITE of stealth.** You can't have full redecoration AND stealth optical glasses.
- DR is also a research-grade lift even on passthrough (real-time segmentation + background inpainting + temporally-stable relighting).
- Exotic middle ground: segmented dimming (Magic Leap 2 style) = partial darkening/occlusion, but bulky/anti-stealth, not arbitrary removal.
- **Verdict:** stealth track is ADDITIVE-ONLY. Full environment manipulation = a separate **video-passthrough product (v-future fork)**, not a feature on this track.

### Making objects look SOLID (not ghostly) in a stealth form factor
Problem: additive optics make virtual objects translucent/faded. Goal: objects look solid/opaque, but small & discrete.
- **True per-pixel occlusion (opaque) in a small form factor = UNSOLVED frontier.** It's physics, not sourcing: a near-eye occlusion mask is heavily defocused; making it sharp needs relay optics → bulk. That's WHY segmented dimming (Magic Leap 2) is bulky. No compact off-the-shelf version exists; compact occlusion (holographic/phase-SLM, soft-occlusion) is years-out research.
- **Shippable compact recipe = CONTRAST-based solidity (not true occlusion):** (1) dynamic **global electrochromic dimming** (small LC tint, no relay optics, darken whole view a bit so content pops), (2) **high-brightness microLED / laser-beam-scanning** light engine (tiny, high nits), (3) **rendering tricks** — full opacity, saturated color, contact shadows/AO, avoid thin edges & bright backdrops.
- **Result:** convincingly solid INDOORS; weaker over bright windows / sunlight (real light overwhelms additive).
- **Trade:** dimming = tinted lenses = "sunglasses indoors" look → fights stealth. Mitigate with DYNAMIC dimming (only where/when content shown, only as much as needed).
- **Decision:** design around contrast-based solidity; treat true compact occlusion as a research line to MONITOR, not a dependency.

**Known contradictions to resolve BEFORE industrial design:**
- Wearer-only HUD = waveguide display = bulky, narrow-FOV, hard to hide. **Display vs stealth is the core contradiction.**
- Display + LiDAR + good camera + 8h battery + "looks normal" are **mutually exclusive today**. Must drop/trade something.
- Heat on a face-worn device with camera+display+LiDAR+radio is a hard limiter; 8h is optimistic.
- "Silent speech by bone conduction" conflates two techs: bone conduction = audio OUT; silent-speech IN = subvocalization/EMG (separate, less mature). Don't assume one component does both.

---

## 7b. DEFERRED — Hardware Industrial Design (develop LATER, placeholder)
Not designing hardware now (software-first). These are captured as TODO topics to flesh out in the hardware phase:
- [ ] **Bill of Materials (BOM)** — full component list: waveguide + light engine (microLED/LBS), electrochromic layer, warp chip (FPGA/ASIC), connectivity radio, IMU, camera(s), depth/LiDAR, mics, bone-conduction transducers, EMG/silent-speech sensors, battery, PCB/SoM, housing. With cost estimates per component.
- [ ] **Frame/industrial design** — form factor, geometry, single-lens integration, hinge/temple layout to hide electronics, weight distribution.
- [ ] **Material** — TR90 polymer (per Snap) vs alternatives; durability, weight, skin contact, RF transparency.
- [ ] **Thermal** — heat dissipation on a face-worn device (camera+display+radio); passive vs active; duty-cycling; comfort limits.
- [ ] **Battery** — capacity vs weight vs ~8h target; in-frame vs temple vs charging-case top-up; safety.
- [ ] **Connections** — glasses↔phone link (wireless 60GHz/Wi-Fi6E vs wired USB-C), bandwidth/latency budget for stream + reprojection, charging/data port.
- [ ] **Sensor placement & ergonomics** — camera FOV vs hand-tracking zone, eye-relief, fit range, comfort, all-day wear.

## 8. Open Questions / Decisions To Make
- [x] ~~Primary input modality~~ DECIDED (D18): dual-mode = silent speech (stealth) + full hand motion (power). See §6b.
- [ ] **Mode-switching mechanism** (§6b): auto-by-context vs manual toggle — settle during HUD UX design.
- [ ] **Growth fork** (recommended: consent-gated promotion). Manual-only global = no moat. Auto-promote generic classes via dedup-on-write + IP/privacy filter + curation. Personal splats NEVER promoted. → leaning toward the router model in §4.
- [ ] **Retrieval layer spec:** embedding choice (CLIP-on-renders vs DINO vs 3D-native), vector DB, two-tier thresholds (geometry for class, appearance for instance), cross-scope ranking policy.
- [ ] **Splat-count / fps budget** on real target hardware (the load-bearing unknown for Track B).
- [ ] **Hardware contradiction resolution** (display vs stealth).
- [ ] **Asset format spec** (`.splatmesh` container: mesh + skeleton + skin weights + splat coefficients + physics params + metadata).
- [ ] **Provenance / licensing** for commercial library (Wan2.1 / CC-BY / CC0 base where possible).
- [ ] Compression: Draco+meshopt+KTX2 (mesh), SOG + LOD (splat).

---

## 8c. Dev resources
- **Server (`shnri`):** NVIDIA **RTX 5090, 32GB VRAM** (Blackwell — needs recent PyTorch + CUDA 12.8), 36 cores, 94GB RAM, Docker, Python 3.12, Node 18. → generation (TRELLIS etc.) runs LOCALLY, no cloud GPU needed to start.
- Test devices: iPhone 14 (60Hz), Windows laptop (AMD Radeon iGPU). Render budget validated ~200k splats/object (D22).
- `.ply` test fixtures preserved in `benchmark/assets/` (Shu2 splat, Hammer point cloud).

## 9. Next Steps (proposed roadmap)
See conversation recommendation. Highest-leverage first:
1. **De-risk the rendering core (Track B foundation).** Vertical slice: take/generate ONE rigged splat asset, render it with rigid-body physics (drop/bounce/move) and measure the **splat-count vs fps** curve in stereo. **Mobile-first**, benchmarked across a **device matrix spanning ~last 3 years of phones** (low/mid/flagship — must not be flagship-only), to produce a **per-tier splat budget**, plus a desktop run as the *enhanced* upper tier. This validates the load-bearing assumption ("realtime animated splats on cheap hardware") AND establishes the adaptive-quality tiers.
2. **Spec + prototype the retrieval layer** (embeddings + ANN + write-time router + dedup-on-write).
3. **Offline generation pipeline** (image/text → splats+mesh → geometric bind), seeded from Objaverse.
4. In parallel, **Track A agent loop** (mic + HUD + LLM + phone control) — independent of B.

---

## 8b. Competitive landmark: Snap Specs ($2,200, 2026)

Snap Inc.'s consumer AR glasses. **Validates our reasoning, doesn't kill us.**
- Specs: see-through waveguide, 51° FOV, 132g, **electrochromic lenses** (clear→tinted 10s), 7ms motion-to-photon, **~4h battery** (20h w/ case), onboard/standalone compute, $2,200.
- **They did NOT solve occlusion.** Waveguide + electrochromic = additive optics + global dimming = EXACTLY the D12 contrast-based-solidity recipe. No true per-pixel opaque occlusion exists; D12 confirmed by a billion-dollar competitor. No magic display to "adopt."
- **Local rendering cost them battery (4h vs our 8h goal)** — confirms our phone-offload bet is a legit different design point (lighter/cooler/longer), not a deficiency.
- **What Snap actually solved = INTEGRATION/miniaturization, not new optics** — single clean integrated lens (not chunky multi-element), self-contained/wireless (not tethered). The dual Snapdragons enable untethered operation AND cause the 4h battery AND drive the cost. Their cleverness and their weakness share one root cause.
- **COST CORRECTION (earlier "$2,200 me-too" was wrong):** the $2,200 is mostly the dual flagship SoCs + sensors, NOT the display. Our phone-offload architecture DELETES the onboard compute → meaningfully cheaper than Snap. Display optics (waveguide+light engine+electrochromic) are the big remaining single cost. Evidence: Meta display glasses = $800 by leaning on an external device. So a phone-offloaded device plausibly targets the **~$800 class, not $2,200** — same display category, lower price, lighter, longer battery. (Hardware is later anyway.)
- **Strategic conclusion: do NOT compete on hardware.** Matching their display/price = a $2,200 me-too vs Snap/Meta/Apple (platforms, ecosystems, scale we can't beat) AND loses stealth. Our moat = SOFTWARE: the agent + the asset-library content flywheel.
  - **Strategy A (preferred): be the software/content LAYER**, platform-agnostic — run the agent + asset library ON Snap/Meta/whoever wins. Sell shovels. A $2,200 competitor arriving = hardware commoditizing = good for this play.
  - **Strategy B: build a DELIBERATELY DIFFERENT device** — stealth, agent-first, longer-battery (phone-offloaded), cheaper, less flashy-AR. Different segment than Snap's dev-flashy showpiece.
  - **Avoid the middle:** a $2,200 stealth-ish device competing on AR rendering loses on every axis.

## 9a. Rendering Benchmark Slice — spec (Next Step #1, detailed)
> **BUILT (compare viewer):** `benchmark/viewer.html` — toggles Splat / Mesh / Bones, Combined vs Side-by-side. Mesh+bones from `assets/crab_rigged.glb` (decimated 50k-face GLB w/ armature, via `generation/prep_viewer.py`/bpy); splat as colored points (crisp splat stays on splat.html). Makes the 1-bone skeleton bug visible. Three.js (esm.sh), no mkkellogg (avoids two-three-instances conflict).
> **BUILT (real assets):** `benchmark/real.html` — loads real `.ply`s with per-asset toggles. `Shu2Edited.ply` = real 3DGS splat (~66k); `Hammer_point_cloud.ply` = plain colored point cloud (665k pts, NOT a splat). Still uses the approximate additive/no-sort renderer (loader+toggle+rough perf, not pixel-accurate). Faithful sorted-3DGS render (mkkellogg) = next step for exact sort-cost. Assets in repo (~24MB; move to LFS/external if it grows).
> **BUILT:** `benchmark/index.html` (self-contained WebGL2 page, no deps) + `benchmark/README.md`. v1 uses synthetic gaussians + additive/no-sort blending = render-throughput budget (slightly optimistic); refine later with real renderer (mkkellogg) on a real asset. Untested by author — user runs it. Awaiting first results to fill the per-device budget table.
> **HOSTING:** repo = github.com/yousef-yy4u/AI-SG; deployed on **Railway** via `Dockerfile` (caddy:2-alpine serves `benchmark/` on `$PORT` per `Caddyfile`). Open the Railway `*.up.railway.app` domain on the phone. (Cloudflare quick-tunnel abandoned — QUIC instability on the server, error 1033.)
>
> **✅ RESULTS (first real data, 2026-06-22; size 1.2, stereo, sustained):**
> | Device | Res | 60fps budget | ~45–50fps | Throttle |
> |---|---|---|---|---|
> | iPhone 14 (Apple GPU) | 720p (0.9M px) | ~200–250k | 350k→47 | stable |
> | **iPhone 14** | **glasses 2400×1200 (2.88M px)** | **~200k** | ~250–300k est | 350k→35→27 🔴 |
> | Laptop (AMD Radeon iGPU, RDNA3-class) | 720p | ~750k | 1M→55 | stable |
> | Laptop | glasses (2.88M px) | ~350k | 500k→50 | stable |
>
> **HEADLINE: iPhone 14 holds ~200k splats stereo @ locked 60fps @ glasses res, thermally stable → a single photoreal object (D21 ~100–300k range) runs realtime on a mid-2022 phone. Core assumption VALIDATED.** Insight: ≤200k is splat-COUNT-bound (res barely matters); fill-rate only bites at 350k. CAVEATS: benchmark is optimistic (additive, NO per-frame sort) → real 3DGS budget somewhat LOWER; iPhone 14 is 60Hz so true edge is between 200k–350k (test 250k/300k); newer/Pro phones do more. Laptop AMD iGPU beats the phone (~1.75× at glasses res) — confirms D13/fairness note.

**Goal:** produce a per-device splat-budget table that validates "realtime splats on cheap hardware" and seeds the adaptive tiers (D9).

**Devices available now:** iPhone 14 (A15, **mid anchor**) + Windows laptop w/ integrated graphics, no dGPU, Ryzen 3/5, 16GB RAM (**low anchor — likely WEAKER than the iPhone for splats**). True desktop dGPU tier = TODO (no hardware yet).

**Stack decision:** **Web, WebGL2 first** (one build runs Safari/iOS + Chrome/Edge + any phone). Renderer = existing **Three.js + mkkellogg/GaussianSplats3D** (don't write a rasterizer). WebGPU = later upside test. Web perf is a **conservative floor** — native will be ≥ web.

**Scope (tight):** static splat cloud + **rigid-body** transform + physics proxy only. NO per-splat skeletal skinning (rigid object = ONE transform matrix; skinning only needed for animated characters = later slice). NO full AR/SLAM yet (render fullscreen + single ground plane).
> Insight: every easy-tier physics interaction (move/drop/bounce/float/throw/stack) is rigid-body → whole cloud transforms by one matrix. Cheap.

**Asset:** one object splat `.ply`, decimated into a sweep (content irrelevant, COUNT is the variable). Decimate via SuperSplat "reduce" or top-K-by-(opacity×scale). Sweep: **25k, 50k, 100k, 200k, 350k, 500k, 750k, 1M**.

**Methodology gotchas:** (1) **Sustained not peak** — median fps over a 5-min run + record fps-after-5min (thermal throttle is the mobile killer, esp. face-worn). (2) **Stereo ≈ 2× cost** — test mono for the clean curve, then a render-twice stereo pass (the real AR target).

**Table columns:** Device | GPU/SoC | Browser/API | Splat count | Resolution | Mode (mono/stereo) | FPS sustained (p50, 5min) | Frame time p95 (ms) | Mem (MB) | FPS after 5min | Verdict.
**Verdict (mono):** 🟢 ≥60 (≥72 ideal AR) · 🟡 30–60 · 🔴 <30. Largest 🟢 per device = that tier's budget; halve-ish for stereo.

## 9c. Generation pipeline — WORKING (image → splat, on the 5090)
**Status: end-to-end working.** image → TRELLIS → gaussian `.ply` → importance-decimate to budget → render.
- **Env:** venv `/home/sov2/projects/gen-venv` (Python 3.12), torch **2.11.0+cu128** (Blackwell sm_120), TRELLIS clone at `/home/sov2/projects/TRELLIS`.
- **Deps that worked on Blackwell:** `xformers 0.0.35` (from pytorch cu128 index — sm_120 flash-attn-2 op works but ONLY fp16/bf16, not fp32), `spconv 2.3.8` (spconv-cu126 wheel), `utils3d`, `plyfile`. **kaolin STUBBED** (`gen-venv/.../site-packages/kaolin/utils/testing.py` → no-op `check_tensor`) since it's only imported for the mesh path, which gaussian-only never runs.
- **Required env vars at run:** `PYTHONPATH=/home/sov2/projects/TRELLIS XFORMERS_DISABLED=1 ATTN_BACKEND=xformers SPARSE_BACKEND=spconv SPCONV_ALGO=native`. `XFORMERS_DISABLED=1` forces **DINOv2** (image encoder) to native attention (it ran fp32 → xformers fa2 rejected it); TRELLIS's own sparse attn still uses xformers (runs fp16 → works).
- **Scripts (in repo `generation/`):** `run_trellis.py` (image→`out/output.ply`, formats=['gaussian'] only), `gen_batch.py` (loads TRELLIS ONCE → emits BOTH `<name>_mesh.obj` + `<name>.ply` per image, formats=['mesh','gaussian']), `decimate_ply.py` (top-N by opacity×volume → LOD), `clean_mesh.py` (open3d rig-readiness pass: weld + drop floaters by connected-component size; reports surface-area/bbox density; `--watertight` voxel-remesh option). `prep_viewer.py` (rigged FBX → decimated GLB for the viewer).
- **First result:** robot-crab sample image → **859,328 splats in ~19s** (pipeline load ~43s first time), decimated → `benchmark/assets/robot_crab_200k.ply` (200k, 13MB), wired into `real.html` (3rd toggle "Generated crab").
- **MESH output also WORKS** (formats=['mesh']) with the kaolin stub — flexicubes extracts fine without real kaolin. Crab → 464,708 verts / 929,372 faces. So pipeline gives aligned mesh+gaussians (needed for rig→bind). GLB *texture* export still needs nvdiffrast (deferred — raw mesh geometry is enough for rigging).
- **UniRig setup (in progress):** venv `/home/sov2/projects/unirig-venv` (**Python 3.11.15** via uv — bpy needs 3.11), torch 2.11+cu128, bpy 4.2, spconv 2.3.8, all bulk deps ✅. Clone at `/home/sov2/projects/UniRig`. Crab mesh exported → `generation/out/crab_mesh.obj` (464k verts).
  - **✅ SKELETON WORKS** — `crab_skeleton.fbx` generated, NO compilation. How (reproducibility): (1) set `_attn_implementation: sdpa` in `configs/model/unirig_ar_350m_1024_81920_float32.yaml` (was flash_attention_2); (2) pure-Python STUBS in unirig-venv site-packages: `torch_cluster` (real fps), `torch_scatter` (real segment_csr/scatter), `flash_attn[.modules.mha.MHA]` (raises if called). Skeleton uses michelangelo encoder (only fps runs); PTv3/skin imports are satisfied by stubs but not executed.
  - **✅ SKIN model RUNS too — no flash_attn compile.** How: (1) native sdpa-backed `MHA` in the flash_attn stub matching flash-attn's exact params (Wq/Wkv/out_proj) so the skin ckpt loads; (2) set `enable_flash: False` on the PTv3 mesh_encoder in `configs/model/unirig_skin.yaml` → PTv3 uses its built-in manual-attention path (no flash_attn_varlen); (3) `torch_scatter.segment_csr` stub (real impl); (4) `sitecustomize.py` forces `torch.load(weights_only=False)` (ckpt has python-box objects). Skin inference produced `predict_skin.npz`.
  - **✅ RESOLVED (D25): 1-bone skeleton is NOT an env/stub bug — the environment is PROVEN GOOD.** Root-caused 2026-06-24 via a clean giraffe A/B:
    - **The giraffe-test "npz_dir path bug" was a red herring: `examples/*.glb` are git-LFS POINTER files (132 bytes), never pulled** (git-lfs not installed). Extract had no real mesh → no `raw_data.npz`. Fetched the real `giraffe.glb` (6.3MB) directly from GitHub's LFS batch API (`info/lfs/objects/batch` → S3 href). The committed `examples/skeleton/*.fbx` references ARE real files.
    - **Giraffe through OUR exact pipeline (sdpa + fps stub + all stubs, seed 12345) → 44-bone skeleton.** Reference `examples/skeleton/giraffe.fbx` = 30 bones; both are full healthy skeletons (count differs only by AR sampling: our sdpa+seed vs the authors' flash_attn). → **fps stub, sdpa attention, and every stub are CORRECT.** The user's "verify the fps rewrite" is closed: the stub is a textbook FPS and during inference runs deterministically (random_start=False).
    - **The crab specifically makes the AR decoder collapse.** Instrumented `generate()`: giraffe emits varied coord triples (traces the shape); the crab emits the SAME triple (`128,142,114` ≈ object center) ~12× → coincident joints that dedup to 1 bone (`bone_11`). Deterministic across seeds 42/7/2024/999 (all collapse to center). cond is non-NaN, similar stats to giraffe — it's the *content* of the encoding that's uninformative for the crab.
    - **Why the crab encodes badly = it's a poor rigging INPUT, not a pipeline fault.** Crab raw mesh is geometrically clean (no NaN/degenerate faces, sensible Z-up, unit-norm normals) BUT: surface-area/bbox ratio **6.79 vs giraffe 2.26** (≈3× more surface packed in → TRELLIS/flexicubes internal geometry + crinkle), 85 disconnected components, and it's a **radially-symmetric crab (8 legs + claws, no spine)** = out-of-distribution for ArticulationXL (trained on spined bipeds/quadrupeds/characters). The model can't find an articulation structure → predicts the mean (center).
  - **CONCLUSION: the rigging stack works end-to-end. The fix lives on the GENERATION side, not rigging.** Next actions to get a real rigged asset: (1) pick a **rig-friendly subject** for the next gen test — a clear biped/quadruped/character, not a radially-symmetric crab; (2) **clean the TRELLIS mesh before rigging** — keep largest connected component(s), strip internal shells, optionally remesh/make-watertight to kill the internal-surface sampling; (3) re-run skeleton → it should produce a real multi-bone armature, THEN skin (machinery already runs) → bind splats → animate. Repro: `bash launch/inference/generate_skeleton.sh --input examples/giraffe.glb --output <out>.fbx` from `/home/sov2/projects/UniRig` (PYTHONPATH=. , unirig-venv). Extract segfaults on bpy *exit cleanup* AFTER writing `raw_data.npz` — harmless, output is valid.
  - **✅ END-TO-END PROVEN (D26, 2026-06-24): generation→cleanup→rig→skin works for clean subjects.** Ran two fresh subjects through the full pipeline:
    - **Human** (TRELLIS example `typical_humanoid_dwarf.png`): mesh 704k faces, **raw density 2.13** (already healthy, like the giraffe's 2.26 — humanoids are in-distribution) → cleanup 9→1 component → **28-bone skeleton, 28 weight groups, 100% of verts skinned**.
    - **Butterfly** (Wikimedia *Morpho didius* dorsal specimen, CC): mesh 278k faces, density 0.84 (flat/thin) → cleanup 5→1 component → **42-bone skeleton, 38 weight groups, 100% skinned**.
    - **This confirms D25's diagnosis**: the crab failed because it was an OOD/noisy input (density 6.79), NOT the pipeline. Clean, in-distribution subjects rig perfectly.
    - **Mesh-cleanup learning:** `open3d.remove_non_manifold_edges()` HANGS for minutes on thin double-sided surfaces (butterfly wings) AND would shred them — `clean_mesh.py` deliberately omits it; weld + connected-component floater removal is enough and fast (<1s).
    - **Skeleton→skin handoff gotcha (UniRig CLI):** `run.py` forces `user_mode=True` for ANY `--output`/`--output_dir`, which SKIPS writing `predict_skeleton.npz`. So **skin must take the SKELETON FBX as its `--input`** (its extract step reads the armature → joints into `raw_data.npz`); feeding the original mesh.obj gives `joints=None` → `TypeError`. Repro: `generate_skin.sh --input examples/<name>_skeleton.fbx --output <name>_skinned.fbx --data_name raw_data.npz`.
    - **NEXT:** bind splats→rig (geometric nearest-triangle) + drive the skinned rig with motion (procedural/AnimaX) + ElevenLabs audio. Skinned GLBs are Three.js-animatable.
  - **VIEWER (`benchmark/viewer.html`), now MULTI-SUBJECT:** bones "didn't render / not a single dot" because `THREE.SkeletonHelper` only draws bone→child line SEGMENTS — a single childless/zero-length bone = zero segments = nothing visible. Added **joint spheres at every bone** (always visible, even for 1 degenerate joint). Refactored into a **subject selector (Crab / Human / Butterfly)** — each loads its rigged GLB (mesh + bones-with-spheres) and its splat `.ply`; Splat/Mesh/Bones/Layout toggles act on the active subject. Directly shows the contrast: crab=1 bone (the pre-cleanup bug), human=28, butterfly=42. Assets built via `prep_viewer.py <skinned.fbx> <glb>`; web splats via `decimate_ply.py`. (`giraffe_rigged.glb` from the D25 A/B is still in `assets/` as a reference.)
    - **Two viewer render bugs fixed (D26b):** (1) **distorted "noise blob" mesh** — the GLB mesh is a `SkinnedMesh`; Three.js `.clone()` breaks its skeleton binding so the skinning shader explodes the verts. Fix: render the baked REST geometry as a plain non-skinned `THREE.Mesh` (the bones/joint-spheres were always fine because they read bone transforms directly). (2) **mismatched orientations** — TRELLIS emits its splat **Y-up** but its mesh **Z-up** (verified via the GLB's raw POSITION accessor: human height on Z), and the rig keeps the mesh frame; so GLB-derived groups are rotated −90° about X to stand upright matching the splat. Mesh + bones now also share ONE normalization (from the mesh) so the skeleton sits correctly inside the mesh. (Residual left/right mirror between TRELLIS's two frames is invisible on bilaterally-symmetric subjects.) Follow-ups after first visual check: **splat flipped 180° about X** (TRELLIS splat was upside-down + facing back vs the upright mesh); **bone LINES rebuilt from baked joint positions** instead of `THREE.SkeletonHelper` — the helper recomputes lines relative to its root each frame, cancelling the wrap rotation, so the lines rendered 90° off from the (correct) joint spheres. Verified upright & consistent.
- **DEMO TARGET (user pick): splat-quality animated** (not mesh) → needs faithful renderer (done, splat.html) + rig + splat-to-rig binding + skinning. Hardest path.
- **✅ STUDIO (D27): one-click image→rigged-3D web app.** `generation/server.py` (Flask, runs in gen-venv) keeps TRELLIS RESIDENT (pay the ~40s load once) and orchestrates the whole chain per upload: image(s) → TRELLIS (`run` for 1 image, `run_multi_image` for 2-4) → `clean_mesh` → UniRig skeleton → UniRig skin → rigged GLB + decimated splat → rendered in-browser. `generation/studio.html` = drag-drop upload (multi-file), live progress bar, embedded viewer reusing the D26b rendering (baked rest mesh + −90°X / splat 180°X orientation + joint spheres/lines). One GPU job at a time (`PIPE_LOCK`); UniRig stages shell out to unirig-venv. **Tested end-to-end via the server:** single image (elephant → 19 bones) and 3-view multi-image (character → 12 bones) both complete. Run: `PORT=8077 PYTHONPATH=/home/sov2/projects/TRELLIS XFORMERS_DISABLED=1 ATTN_BACKEND=xformers SPARSE_BACKEND=spconv SPCONV_ALGO=native gen-venv/bin/python generation/server.py` → open `:8077`. (Port configurable via `PORT`; 8000 was occupied.) Scratch dirs `studio_out/`, `studio_uploads/` gitignored.
  - **Multi-view = more accurate (answered):** TRELLIS fuses 2-4 views to reduce hallucination of unseen sides (PROJECT.md §4 multi-view capture). Video would be sampled to frames → multi-image (TODO). Text→prompt needs a t2i model or TRELLIS-text weights (not installed) — TODO.
  - **clean_mesh.py gotcha #2:** `open3d.is_watertight()` is ALSO pathologically slow (minutes) on messy multi-view-fused meshes — removed from the stats readout (it was only diagnostic). (First gotcha was `remove_non_manifold_edges`, D26.)
- **✅ D28 — studio hardened + MESH animation.** (a) **Robust failures:** rigging is now non-fatal — if UniRig produces no FBX or a degenerate (≤1-bone) skeleton, the studio falls back to a plain-mesh GLB and reports the reason (`rig_ok`/`bones`/`note` in the job result) instead of hard-breaking. (This is the real fix for the user's "2/3/5 images break, 1/4 work" report — NOT a count bug: 1-5 example views all succeed; it's content-dependent — certain real-photo view sets fuse into a degenerate TRELLIS mesh the rig model can't handle. Now it degrades gracefully + says why.) (b) **Animation in studio:** "▶ Animate" adds the LIVE gltf.scene SkinnedMesh (NOT a clone — cloning breaks binding→blob) and rotates its bones each frame; Three.js GPU-skins the mesh. Controls = procedural sway (speed + amount sliders), **Drift** (whole-asset bob/turn), and **manual per-bone rotation** (bone dropdown + X/Y/Z sliders) — that's the current control level. NOTE: auto-rig bones are unlabeled (bone_0..N), so semantic motion (walk cycles) needs bone-labeling/retargeting (future); and the SPLAT does not deform yet (rigid Drift only) — splat→rig binding is next.
- **✅ D29 — splat BOUND to rig (animated splats) + viewer fully assembled.** `generation/bind_splat.py`: registers the splat (TRELLIS gaussian frame) INTO the mesh's geometry-local frame via a 48-candidate signed-permutation + chamfer search (robust to TRELLIS's splat-vs-mesh frame/scale mismatch), then copies the 4 bone indices + 4 weights from the nearest mesh vertex. Verified numerically (dwarf/quadruped: chamfer ~0.045, scale ~1.94≈2×, mean splat→surface dist ~0.02, weight sums =1.0). Output = PLY in mesh frame with `j0..3`/`w0..3` columns. Server runs it after rigging (`result.splat_skinned`). Viewer (`studio.html`): a custom GPU **point-skinning ShaderMaterial** (same LBS as Three's mesh skinning: `bindMatrixInverse·Σwᵢ·boneMatrixᵢ·bindMatrix·p`), boneMatrices shared-by-reference from the live skeleton so it auto-updates each frame; the skinned Points are parented to the live SkinnedMesh so they deform WITH it. **This also fixed the alignment complaint** — the skinned splat is now in the mesh frame, so splat + mesh share one transform (previously splat was separately normalized + a stray-point-inflated bbox made it look small/inside). NOTE (user to visually verify): shader correctness, point size (`uSize`), and that in Combined view the opaque mesh OCCLUDES the co-located splat (toggle Mesh off / use Side-by-side to see the splat) — that's expected once aligned, not a bug.
- **Bone labeling (answered):** UniRig names bones `bone_0..N` = indices only, NOT semantics. Structure (positions+hierarchy) is enough for skinning/posing/physics (no labels needed — what we do now). Semantic motion (walk cycles) needs labeling via topology heuristics (root=central, spine=longest chain, limbs=leaf chains, L/R by X-sign) or motion retargeting — deferred until we add canned animations.
- **NEXT:** (1) verify/​tune the animated splat in-browser; (2) richer motion (procedural gaits via bone-labeling, or AnimaX) + ElevenLabs audio; (3) consider single-image→multi-view front-end (see D30 analysis). Single-image quality: one view hallucinates the back — multi-view mitigates. Note: ElevenLabs = audio only; movement is a SEPARATE source (procedural/motion model). First MVP demo likely = rigged MESH animated in Three.js + ElevenLabs audio (splat-binding fidelity is a later upgrade). Render fidelity caveat: real.html's isotropic-additive viewer makes splats look sparse — validate true quality via superspl.at or integrate mkkellogg.

## 10. Decision Log (idea evolution — newest last)

- **D1 — "1.5–2.5s whole pipeline" rejected.** GaussianAvatars is per-subject optimization (600k iters), NOT a sub-second baker. Make-It-Animatable's 0.5s is humanoid-only. A³-GS is multi-view optimization, not feed-forward. → Reframed into the **two-latency model** (§3): one-time generation ~15–40s, steady-state render realtime.
- **D2 — Architecture = retrieval, not runtime generation.** Adopted the library/cache + generation-fallback model as the economic core and data moat (§4).
- **D3 — Library refinements.** Ambient-vs-focal substitution; static-vs-animatable tiers; seed from Objaverse; multi-view capture; janitor; privacy/brand caveats.
- **D4 — Data model: class (geometry) vs instance (appearance).** Same model unifies everyday objects AND commerce SKUs. Splat-binding constraint surfaced (appearance-only variation).
- **D5 — Three scopes = one pool + flags.** global/marketplace/private as visibility×curation, not 3 DBs. Added **category → class → instance** (fixed "chair is a class" bug). Surfaced growth fork.
- **D6 — Simplification debate.** User proposed flat "everything-shared + dedup-on-write, only humanoids private." Verdict: drop the deep nesting (good), but DON'T flatten scopes — everything-shared = IP-takedown firehose + deletes commerce. **Synthesis: keep dedup-on-write, replace binary keep/discard with a write-time ROUTER** (§4). Humanoid telepresence vision added (§6).
- **D7 — Physics scoped to EASY TIER only** (§5). Hard tier (break/cut/soft-body) deferred — splats are hollow shells.
- **D8 — This file created** as single source of truth.
- **D9 — Adaptive offload tiers + mobile-first benchmark.** Offload device is context-dependent: phone when out (baseline), computer when home/office (enhanced — more complex objects/workflows). Splat budget is therefore TIERED, with runtime device-detection + per-tier LOD. Benchmark across a phone matrix spanning ~last 3 years (not flagship-only); desktop = enhanced upper tier, never required. Mobile optimized from the get-go. (§2, §9)
- **D20 — Licensing audit done; stack swapped to commercially-clean (§4a).** Verified landmines: Inria 3DGS code + diff-gaussian-rasterization (NON-COMMERCIAL — never ship), SOG research repo, InstantMesh/Zero123++ (NC), Hunyuan3D-2 (no EU/UK/SK, 1M-MAU cap), RigAnything (Adobe NC), ShapeNet (NC), SMPL/FLAME-non-2023 (NC + training-taint). Clean stack: TRELLIS/TripoSR/LGM + UniRig + Wan2.1 + mkkellogg/PlayCanvas + Draco/meshopt/KTX2/sogs/PLAS + CLIP/DINOv2-Apache + Objaverse(CC0/CC-BY) + FLAME-2023-Open. (§4a, §3) + §7b deferred hardware-ID placeholder added.
- **D32 — ASSET LIBRARY + RETRIEVAL v0 (the moat, §4) — WORKING.** Pivoted from generation-polish to the library per D31. `generation/library.py`: storage (`generation/library/{index.json, embeddings.npy, assets/<id>/}`), **CLIP embeddings** (openai/clip-vit-base-patch32, MIT — image+text in one space; an asset = mean of its view-image embeddings), **dedup-on-write** (new asset with nearest-neighbor cosine ≥0.94 = duplicate → reuse, the flywheel), **text/image search** (cosine top-k). Server routes (`/library/list|search|add|asset/<id>/<f>`, page `/library`) + studio integration ("＋ Add to library" with dedup, `?asset=<id>` loads a library asset into the viewer). `library.html` = browse/search grid. **Verified:** seeded 5 generated assets → the repeat character DEDUPED (cosine 0.989); text search ranks correctly ("elephant"→elephant, "four legged animal"→quadruped, "a person"→humanoids); endpoints + file serving 200. Uses transformers 5.x CLIP via vision_model+visual_projection (get_image_features changed to return an object in v5). NEXT: render proper thumbnails (currently the input image), category→class→instance structure, write-time ROUTER (person/brand/product/generic), image-query upload, cold-start ingest from Objaverse(CC0/CC-BY). (§4, §9 step 2)
- **D31 — Two renderers (the "dots vs beautiful" reality) + STRATEGIC pivot question (DECIDED → pivot, see D32).** Clarified for the user: `benchmark/splat.html` looks beautiful because it uses **mkkellogg/gaussian-splats-3d** (real anisotropic depth-sorted 3DGS). The **studio/viewer use a crude `THREE.Points` (dots)** renderer ON PURPOSE — mkkellogg (a) conflicts (2× three.js instances) and (b) renders only STATIC gaussians (can't deform). So: beautiful STATIC splat = solved (mkkellogg); **beautiful ANIMATED splat = hard** (needs a custom skinned-gaussian rasterizer — research-grade "animatable gaussians"). Current animated splat = dots only. Fixes shipped: splat-mesh alignment via ICP/Umeyama (chamfer 0.045→0.021, scale now exact); studio defaults to splat-first (mesh hidden — splat is the hero/product, mesh is an inspection layer); stronger visible sway. **STRATEGIC DECISION — DECIDED (2026-06-24): PIVOT to the curated 3D library + retrieval** (the §4 moat). Generation already works well enough for us to curate; bad user-gen is handled by the write-time ROUTER + a quality scorer (geometry/rig sanity, multi-view coverage) → reject/flag/fallback-to-retrieval = LIBRARY work. Animated-beautiful-splats + live user-gen deferred. → implemented as D32.
- **D30 — Single-image → multi-view front-end (PROPOSED, analysis).** User idea: feed ONE front image → an image-to-image model invents left/right/back views → feed all to TRELLIS multi-image for a more complete splat+mesh (avoids requiring the user to walk around the object). Verdict: technically the standard "novel-view-synthesis → 3D" pattern, BUT (a) **TRELLIS single-image ALREADY does internal multi-view reasoning** — bolting an external generator in front only helps if its views are 3D-CONSISTENT; inconsistent invented views fuse into a *worse/degenerate* mesh (the exact failure D28 hardens against). (b) **Licensing:** the best-known single→multiview models are landmines — Zero123++/InstantMesh = CC-BY-NC (already flagged §4a); cleaner options = SV3D (research/non-commercial too — check), Stable Zero123 (NC), or MV diffusion with permissive weights (scarce). Must license-audit before adopting. (c) **Cost/time:** adds another diffusion model resident in VRAM + ~5-20s/inference per generation; modest $ but real latency + memory. **Recommendation:** keep TRELLIS single-image as the default; pursue *genuine* multi-view capture (the user uploading 2-4 real angles, already supported) as the quality path; only add a synthetic-view generator if we find a commercially-clean, 3D-consistent one AND single-image quality proves insufficient. Revisit at generation-quality-refinement phase. (§9c)
- **D29 — Splat bound to rig (animated splats).** `bind_splat.py` registers splat→mesh frame (signed-perm + chamfer) and transfers nearest-vertex skin weights; viewer GPU-skins the points with the live skeleton (parented to the SkinnedMesh). Also unified splat+mesh into one frame (fixed alignment). Verified numerically; shader pending in-browser check. (§9c)
- **D28 — Studio hardened + in-browser mesh animation.** Rig failures now non-fatal (plain-mesh fallback + `rig_ok`/`bones`/`note` reasons) — addresses the "2/3/5 views break" report, which is content-dependent degenerate fusion, not a count bug (1-5 example views all pass). Added "▶ Animate": live SkinnedMesh bone-sway + Drift + manual per-bone rotation sliders. Control level = procedural + manual low-level (semantic motion needs bone labels; splat doesn't deform yet). Next: splat→rig binding for animated splats. (§9c)
- **D27 — STUDIO: one-click image→rigged-3D web app.** `generation/server.py` (Flask, TRELLIS resident) + `generation/studio.html` (drag-drop multi-image upload, progress, embedded viewer). Orchestrates image(s)→TRELLIS(`run`/`run_multi_image`)→clean_mesh→UniRig skeleton→skin→GLB+splat→render. Tested via server: elephant (1 img→19 bones) + character (3 imgs→12 bones). Multi-view fusion reduces unseen-side hallucination (§4). Found 2nd open3d trap: `is_watertight()` also hangs on messy meshes → removed. TODO: video→frames, text-prompt (needs t2i/TRELLIS-text). (§9c)
- **D26 — Rig+skin PROVEN END-TO-END on clean subjects (confirms D25).** Added `clean_mesh.py` (open3d: weld + connected-component floater removal; skips the hang-prone/destructive non-manifold step) and `gen_batch.py` (one TRELLIS load → mesh+splat per image). Ran a human (TRELLIS dwarf example) and a butterfly (Wikimedia Morpho specimen) through image→TRELLIS→cleanup→UniRig skeleton→skin: **human = 28 bones, butterfly = 42 bones, both 100% of verts skinned.** The crab's 1-bone failure was purely its OOD/noisy mesh (density 6.79 vs human 2.13 / giraffe 2.26) — clean in-distribution subjects rig perfectly. Learned: skin's `--input` must be the SKELETON FBX (CLI forces user_mode → no `predict_skeleton.npz`; extract pulls joints from the fbx armature). Viewer refactored to a Crab/Human/Butterfly subject selector. Remaining for demo: splat→rig binding + motion + audio. (§9c)
- **D25 — 1-bone skeleton ROOT-CAUSED: rigging env is good; the crab mesh is the problem.** Clean giraffe A/B proves the pipeline (sdpa + fps stub + all stubs) produces a full 44-bone skeleton. The earlier giraffe-test failure was just unpulled git-LFS pointers (fetched the real glb via GitHub's LFS batch API). fps-stub correctness CONFIRMED (textbook FPS, deterministic at inference). The crab makes the AR decoder collapse to the object center (same coord triple repeated → 12 coincident joints → 1 bone after dedup), deterministically across seeds — because the TRELLIS crab is a poor rigging input (3× surface/bbox density from internal flexicubes geometry, 85 components, radially-symmetric/no-spine = OOD for ArticulationXL), NOT because of any stub/attention. Fix is on the GENERATION side: rig-friendly subject + mesh cleanup, then re-run. Viewer bones fixed (joint spheres + known-good giraffe-rig toggle; `SkeletonHelper` draws zero segments for a childless bone). (§9c)
- **D24 — Generation pipeline WORKING end-to-end on the 5090.** image → TRELLIS → 859k-splat `.ply` in ~19s → importance-decimate to 200k budget → rendered in real.html. Blackwell deps solved (xformers cu128 + spconv-cu126 + kaolin stub + DINOv2 native attn). Full reproducibility in §9c. Keystone achieved — next: rigging (UniRig), text→image front-end, then retrieval/library. (§9c)
- **D23 — Moving to PRODUCT; first build = GENERATION PIPELINE (TRELLIS on local RTX 5090).** Keystone: produces the assets retrieval/library/agent all depend on. Target loop: image/text → TRELLIS → 3D gaussian splat → decimate to ~200k budget → render in real.html. Setup wrinkle: 5090 is Blackwell → needs recent PyTorch + CUDA 12.8. TRELLIS = MIT, outputs gaussians natively (clean per §4a). (§8c, §9)
- **D22 — Benchmark FIRST RESULTS: core assumption validated.** iPhone 14 = ~200k splats stereo @ locked 60fps @ glasses res (2400×1200), thermally stable → one photoreal object (D21 range) runs realtime on phone-class HW. Laptop AMD iGPU ~350k @ glasses 60fps (beats phone). ≤200k is count-bound not fill-bound. Caveats: benchmark optimistic (no per-frame sort → real budget lower); 60Hz cap hides true edge (test 250k/300k); validate later with real renderer. (§9a)
- **D21 — Framerate = two tiers (45 rich / 60 motion); per-object counts are modest.** Stable capped framerate beats fluctuating. 45fps+max-splats (static heroes, reprojection-dependent) = stretch tier; 60fps = safe default until warp engine proven. Single AR object looks high-res at ~100–300k splats (not millions — that's scene-scale); budget = total on-screen splats. Splat size: hold fixed for count sweep (correct), do ONE size-sensitivity pass at budget count to bound fill-rate vs count-bound. (§2)
- **D19 — Hand tracking processed on PHONE, not glasses.** Two kinds of real-time: head reprojection = hard (<10ms, local/glasses); hand interaction = soft (~50ms, phone round-trip fits). Hand CV runs on phone w/ SLAM; glasses stay minimal. Budgets layer (phone = object world-pos, glasses reprojection = head-lock → no swim). Failure: bad link → object trails hand; mitigate fast link + optional on-glasses prediction. (§6b)
- **D18 — Input DECIDED: dual-mode = silent speech (stealth) + full hand motion (power).** Silent speech = discreet all-day command grammar; hand motion = spatial direct-manipulation for private use, covers objects AND general HUD. Complementary by context. RISK: silent-speech IN is least-proven tech → mitigate by prototyping stealth mode with ordinary VOICE (identical grammar, swap to EMG at HW phase); optional temple-touch backstop. Mode-switching = open sub-decision. (§6b)
- **D17 — Input/interaction was a gap; now spec'd (§6b).** Camera hand-gestures feasible but fight stealth as PRIMARY nav (conspicuous + gorilla-arm + power). Split: HUD nav = DISCREET vs hand-gestures for 3D OBJECT manipulation. *(Primary modality resolved in D18.)*
- **D16 — Architecture is CHIP-AGNOSTIC (not locked to Snapdragon).** Glasses never render (phone does) → only need display+warp+sensors. Options cheapest→turnkey: FPGA/warp-ASIC (best fit) → general ARM SoC (MediaTek/Rockchip/NXP) → dumb USB-C display (no SoC) → Snapdragon AR2 (turnkey, pricey, NDA). Phone side also any-chip (iPhone prototype, any phone production). Silicon = production-phase decision, irrelevant to current phone software. (§7)
- **D15 — Glasses-side compute scoped = split/distributed AR (= Snapdragon AR2 philosophy).** Glasses do only stream-receive + display-drive + reprojection (the "one hard job"); phone does heavy render. Target = thin-AR-co-processor tier, but it's reference-design/ODM/NDA = a months-long HW program → LAST phase. Prototype phone-only now; optional cheap feel-prototype = drive off-the-shelf Xreal/Rokid USB-C display (~$300–500) from phone. Don't spend on the glasses chip yet. (§7)
- **D14 — Software-first committed + cost claim corrected.** Path: software → iPhone prototype → hardware LAST. Phone = prototype display AND real offload compute (not throwaway). Snap's win = integration/miniaturization (single integrated lens, untethered via dual SoCs), not new optics. CORRECTION to D13: $2,200 is mostly the onboard dual SoCs, not the display; phone-offload deletes that compute → our target is the ~$800 display-glasses class, not $2,200. Derived §2a software requirements from hardware constraints. (§1, §2a, §8b)
- **D13 — Snap Specs ($2,200) = validation, not kill shot; compete on SOFTWARE not hardware.** Their "advanced display" is waveguide + electrochromic = the D12 contrast recipe (no occlusion miracle). Local rendering → 4h battery (our phone-offload = legit differentiator). ~~Adopting their display = $2,200 me-too~~ *(cost claim superseded by D14)*. Moat = agent + asset library. Strategy A (preferred): be the platform-agnostic software/content layer; Strategy B: deliberately-different stealth/agent-first/longer-battery device. (§8b)
- **D12 — Solid-looking objects via contrast, not occlusion.** True compact per-pixel occlusion is unsolved frontier optics (defocus + relay-optics bulk = why segmented dimming is bulky). Compact path = contrast-based solidity: dynamic global electrochromic dimming + high-brightness microLED/LBS + rendering tricks (shadows/opacity). Solid indoors, weaker vs bright backgrounds. Dimming mildly fights stealth → use dynamic dimming. True occlusion = research to monitor, not a dependency. (§7)
- **D11 — Environment manipulation is display-incompatible with stealth.** "Recolor walls / hide bed / replace real objects" = Diminished/Mediated Reality, needs VIDEO PASSTHROUGH (per-pixel editable). Stealth HUD is optical see-through = ADDITIVE-ONLY (adds light, can't subtract/remove/darken). So the stealth track is additive-only (add virtual objects ✅, remove/repaint real ones ❌); full redecoration = separate passthrough product (v-future fork). (§7)
- **D10 — Benchmark slice spec'd.** Stack = WEB / WebGL2 / Three.js (mkkellogg renderer), not native — one build everywhere, conservative perf floor. Scope = static cloud + rigid-body only (rigid object = one transform matrix; skeletal skinning & full AR deferred). Devices: iPhone 14 (mid) + integrated-GPU laptop (low anchor, likely weaker than the phone). Measure sustained (5min) fps vs splat-count sweep, mono then stereo → per-tier budget table. (§9a)
- **D34 — RENDER-COST REALITY CHECK + why practical AR glasses don't exist (strategic, 2026-06-25).** Stepped back to pressure-test the splat premise. (1) **Traditional rigged textured meshes are an order of magnitude CHEAPER to render on mobile than splats — not close.** Mesh = rasterize triangles + PBR, z-buffer occlusion FREE, GPU skinning ~free, compact mipmapped textures, 30yr HW maturity. Splat = per-frame depth SORT (O(n log n)) + heavy overdraw (alpha blend) + occlusion NOT free (needs the sort) + bigger memory. **Our rigged-mesh-splat hybrid is the MOST expensive option of all** (mesh skin cost + splat sort + per-splat skin + covariance rotation + re-sort every frame). Splats win on exactly ONE axis: fidelity/photoreal-capture/view-dependent appearance/fuzzy materials (hair/fur/skin) with ZERO material authoring — NOT on render cost. → confirms [[project-goal-4dgs]] "mesh path over splats." **Implication: meshes = default real-time render path; splats = a per-object HERO/luxury for photoreal capture, never the baseline.** (2) **Why no practical daily-wearable phone-offload AR glasses exist: rendering was NEVER the bottleneck.** The wall is (a) OPTICS — bright wide-FOV full-color into a thin eyeglass is unsolved; diffractive waveguides throw away >95–99% of light (hot/power-hungry microdisplays), ~30–50° FOV, rainbow/uniformity artifacts; (b) OCCLUSION — optical see-through is additive, can't subtract light → ghosts not solids (§2a/D12); (c) REPROJECTION POWER — you CANNOT offload everything; head-lock needs <20ms motion-to-photon so the glasses MUST do onboard tracking+timewarp = a co-processor drawing watts on your face, fighting the thin form factor (D15/D19); (d) power/heat/social. **Market has already bifurcated along this exact line:** practical phone-offload thin glasses EXIST (Xreal/Rokid/Viture) but DROPPED real AR — they show a floating flat video screen, birdbath optics, weak world-lock, no occlusion. HUD glasses (Even Realities, Ray-Ban Meta) skip spatial rendering entirely. Real spatial AR (HoloLens/Magic Leap/Quest passthrough) stays bulky/hot. Best evidence: **Meta Orion (2024)** = exactly the offload-to-wireless-puck thin-AR-glasses idea, shipped as a PROTOTYPE not product — Meta cited SiC-waveguide cost (~$10k/unit), yield, ~70° FOV = OPTICS+COST, not rendering compute. **Strategic conclusion: cheaper rendering does NOT unlock the product (the wall is a hardware-era optics/power problem), so the splat track is NOT on the critical path. This validates the agent/HUD Track-A-first, hardware-last ordering (D14/§1). Lead with meshes for any real-time render; keep splats as a capture/fidelity hero-feature; don't let either block agent-first.** (§1, §2a, §3, §4a; builds on D12/D14/D15/D19, [[generation-pipeline-reality-check]])
- **D33 — ANIMATED-BEAUTIFUL-SPLATS re-opened with a concrete path (deep research, 2026-06-24; partially reverses D31's "deferred").** D31 framed beautiful animated splats as "research-grade, genuinely hard." A fan-out deep-research pass (76 claims adversarially verified, 69 high-confidence) found it's **less hard than framed — we already built the hard 80%.** Key reframes:
  - **The "dots" are a RENDERER gap, not a skinning gap.** `bind_splat.py` (D29) + studio's GPU-skin shader is *already* the 2025–26 SOTA architecture ("per-splat LBS bound to a skinned mesh"). Two specific, fixable gaps remain: (1) studio draws `THREE.Points` (round dots) instead of anisotropic ellipsoids — purely the renderer; (2) the skin shader transforms splat **position only**, not the Gaussian's **rotation/covariance** → splats would smear under articulation even with a real rasterizer.
  - **The LBS rotation bug is named in the literature.** "On the Skinning of Gaussian Avatars" (arXiv 2509.11411): naive LBS is invalid for a Gaussian's non-linear rotation. Fix tiers: (a) apply blended transform's rotation to the splat `R_o = T·R_d` (3DGS-Avatar, CVPR 2024, simplest/proven); (b) weighted **quaternion-averaging** of bone rotations + rotate SH basis (2509.11411, most correct, "patch the LBS step, use any rasterizer"); (c) +pose-dependent corrective MLP for wrinkles (HuGS/SqueezeMe, optional, needs training). If our TRELLIS splats use SH order>0, must rotate SH coeffs with the bone too or shading looks "stuck."
  - **Decisive open-source hit: Gaussian-VRM** (arXiv 2510.13978, SUI 2025, github.com/naruya/gaussian-vrm) — three.js skinned-splat avatars **built directly on top of mkkellogg/GaussianSplats3D** (the renderer we already use in splat.html) + three-vrm. **MIT** (source; sample assets research-only — doesn't touch our own generated assets). Splats become standard `THREE.Object3D`s → kills the "2× three.js instances" conflict (D31). Key trick: *sort on canonical positions, render on skinned positions* (skips per-frame re-sort, the expensive part). Caveat: the paper has NO formal eval section — its FPS numbers (iPhone 40–50, Quest3 3-avatars@72) are author claims, not benchmarks (the only thing the adversarial verifier refuted). Treat method as sound, numbers as "validate ourselves."
  - **Renderer = a LAYER decision, not a product pick.** 3 layers: (1) renderer/engine, (2) skinning method, (3) rig format. Spark, mkkellogg, Visionary, UniMGS's rasterizer are all Layer 1; gaussian-vrm is Layer-2 glue (bundling mkkellogg); UniMGS is Layers 1+2 fused. Pick ONE renderer + ONE skinning recipe.
    - **Spark** (World Labs, github.com/sparkjsdev/spark, **MIT**) = the strategic Layer-1 bet: an *engine* (not a model/generator), `SplatMesh extends THREE.Object3D`, programmable per-splat GPU "dyno" modifiers, optional `SplatSkinning` with **dual-quaternion** skeletal anim (structurally avoids the LBS-rotation bug), + raycasting/lighting/particles/painter/reveal/transitions/env-maps. Bigger rewrite; write the rig→splat glue ourselves (or use SplatSkinning). "Spark + VRM" = re-implement gaussian-vrm's binding on top of Spark — but we don't even need VRM (our GLB rig + `bind_splat.py` weights already do it).
    - **Visionary** (arXiv 2512.08478, **Apache-2.0**) = WebGPU+ONNX, SMPL-X FK+LBS in ONNX, claims ~135× SparkJS at 6M splats — future bet, narrower device support, heavier pipeline.
    - **UniMGS** (AAAI 2026, arXiv 2601.19233) = NOT humanoid-specific (corrected a user assumption); its edge is **single-pass mesh+splat co-rasterization with correct occlusion/transparency** = our exact AR compositing need. Research code, not a web renderer → a SPIKE only if mesh+splat compositing visibly breaks. For humanoids specifically the avatar papers (3DGS-Avatar/SqueezeMe/SV-GS) beat it.
  - **Licensing (extends §4a/D20):** clean = gaussian-vrm (MIT), Spark (MIT), Visionary (Apache-2.0), MaGS (CC BY 4.0). ☠️ non-commercial (Inria-rasterizer-derived, never ship the academic code): SuGaR, GaMeS, D-MiSo; ⚠️ mixed: 3DGS-Avatar (own files MIT, rest Inria) — use as math reference only. **Our renderer touches none of this** — our splats are TRELLIS, our skinning is our own shader, our candidate renderers are all permissive.
  - **Decisions taken (2026-06-24):** subject = **both/mixed** → generic per-splat LBS on our own GLB rig is the tool; humanoid priors stay optional. Renderer = **decide after spikes.** Plan = **gaussian-vrm proof-of-concept first** (cheapest, ~1–2 days, de-risks the concept on our existing bound PLY) → **Spark spike** (the likely ship target, ~3–5 days) → **UniMGS only if compositing breaks.** Don't run all three in parallel; each result decides whether the next is needed. (§3, §4a, supersedes D31's "deferred" for the rendering question; builds on D29)
  - **Spike step 1 DONE (2026-06-24) — inspected gaussian-vrm source (MIT) vs our pipeline (`generation/SPIKE_animated_splat.md`).** Findings: (a) gaussian-vrm's whole trick = mkkellogg `SplatMesh` + a vertex-shader patch (`gsCustomizeMaterial`) that skins each splat's **center + covariance** (`rotatedVrk = transpose(R)*Vrk*R`, R from `skinMatrix*inverse(skinMatrix0)`) — exactly our Gap-2 fix. (b) **Patch is compatible with mkkellogg 0.4.7, the version splat.html already pins** (all shader anchors + `splatDataTextures`/`updateDataTexturesFromBaseData`/`dynamicScene` present) → NO renderer migration. (c) SH concern moot — our skinned PLY is SH order 0 (`f_dc` only). (d) gaussian-vrm is VRM/humanoid-coupled (three-vrm, `J_Bip_*`, hardcoded bone idxs, `.gvrm` zip, binds splats to mesh VERTICES reading mesh skin-weights) — we DON'T need that path: `bind_splat.py` already bakes per-splat j0..j3/w0..w3, so we skin each splat directly (simpler, works for arbitrary rigs). We lift only the MIT cov-rotation snippet + rest-pose boneTexture0 capture. (e) **BUG found:** `bind_splat.py:113-131` transforms only x,y,z into mesh frame but copies `rot_0..3`/`scale_0..2` UNCHANGED — invisible for dots, but a real anisotropic renderer would mis-orient every ellipsoid by the registration rotation R; must rotate quats by R + scale by s before step-2 PoC. Decided NOT to run gaussian-vrm's own VRM demo (humanoid path we won't use; needs gated sample assets).
  - **Spike step 2 IN PROGRESS (2026-06-24): bind fix DONE+verified, PoC BUILT, awaiting browser validation** (`generation/SPIKE_animated_splat.md`). (a) **`bind_splat.py` fixed** to bake the registration rotation+scale into each splat's covariance (was position-only → the Gap-2 latent bug); numerically verified (clean similarity, per-splat cov scaled by exactly the registration factor 1.985 with zero variance, anisotropy preserved). (b) **`split_by_bone.py`** (new) splits the skinned PLY into one plain-3DGS PLY per dominant bone → 25 scenes for gen_3594c97a. (c) **`poc_splat_anim.html`** (new): mkkellogg DropInViewer (real ellipsoids) with per-bone-group RIGID skinning `D_b = bindMatrixInverse·boneMatrices[b]·bindMatrix` (the exact transform studio's working dot-shader uses → correct-by-construction; seams at joints = known rigid limit, removed later by the per-splat covariance-skinning shader). Served at **/out/poc_splat_anim.html** (no TRELLIS restart) + **/poc** route added. **NEXT: user validates in browser (GPU)** — ellipsoids-not-dots, orientation vs mesh-ref (confirms bind quaternion convention), animation tracks skeleton. Then step-2b = per-splat covariance shader for smooth (seamless) skinning (lift gaussian-vrm's MIT `rotatedVrk` snippet).
  - **Spike step 2 VALIDATED in browser (2026-06-24).** Confirmed: (1) renderer now draws REAL anisotropic splats (mkkellogg), not dots — the headline "low-res dots" problem is solved at the renderer level; (2) the splat ALIGNS with / sits on the mesh ref → binding + frame conventions + the bind_splat.py orientation fix are all correct (the riskiest part); (3) it animates with the skeleton. Two separate issues surfaced, NEITHER about the renderer: (a) **the splat ASSET (gen_3594c97a, 4-view TRELLIS) is low quality** — blur/noise/hallucinations baked in at generation time → a GENERATION-quality workstream, orthogonal to this spike; (b) **rigid per-bone skinning breaks at joints** (legs split, parts move independently) — the known limitation, fixed by step-2b per-splat smooth skinning. Also softened the PoC sway (was ~30°/joint compounding → gentle slider, default 0.06 rad) since the aggressive default exaggerated the joint seams. CONCLUSION: the gaussian-vrm-style approach is PROVEN for our pipeline; renderer decision still "decide after Spark spike" but mkkellogg-path is validated end-to-end. NEXT: step-2b (smooth skinning) to remove joint breaking; separately, improve generation quality (more/better views, cleanup) — pick a better asset to demo.
  - **Spike step 2b BUILT (2026-06-24): smooth per-splat skinning + walk, on the dwarf (`generation/poc_splat_smooth.html`, /poc-smooth).** User picked the dwarf (`gen_3a259c3d`, 28-bone clean biped) as the cleaner asset — confirmed it renders + aligns. Re-bound with fixed bind_splat (note: dwarf registration is a REFLECTION, det −7.6 — the covariance-reextraction method handled it). Skeleton analyzed from geometry (UniRig bones unnamed): legs thigh=20/24 shin=21/25, arms upper=7/14, spine 1-4, head 5. Smooth viewer: loads the FULL skinned PLY as ONE mkkellogg scene; uploads per-splat j/w as RGBA-float textures (indexed by splatIndex = PLY order); patches the SplatMaterial vertex shader (`onBeforeCompile`, anchors verified in 0.4.x vendored module) to skin each splat by its 4 weights — center `splatCenter = D·rest` and covariance `Vrk -> R·Vrk·Rᵀ`, `D = bindInverse·Σ(w·boneMatrix)·bindMatrix` (same geometry-local convention as the proven rigid PoC). Walk cycle driven from geometry-derived per-bone swing axes (world-lateral X in each bone's rest frame); UI sliders for speed/leg/knee/arm + "flip knee". **NEEDS BROWSER VALIDATION (user):** /out/poc_splat_smooth.html — success = limbs bend SMOOTHLY (no joint gaps) under walk. Known iteration risks: splatIndex==PLY order assumption (else weights scrambled), covariance-rotation convention (transpose/sign), GLSL scope of `splatSkinRot` across the two shader anchors, knee bend direction (flip button).

- **D37 — Market + GTM research report produced ([research/market-and-gtm.md](research/market-and-gtm.md), 2026-06-25).** Deep web-sourced research on the D35 platform (landscape, failures, e-commerce ROI, GTM/pricing, market size/timing). Key findings validating/refining D35: (1) **Anchored AR is the moat AND the market is withdrawing from it** — Niantic retired hosted 8th Wall (access ended Feb 28 2026) and did NOT open-source VPS/Maps/Hand-Tracking, redirecting its VPS to defense/robotics; Adobe killed Aero (Dec 2025); Meta killed Spark AR (Jan 2025, 600k creators). Confirms hosted un-anchored WebAR is a thin standalone business → the owner-seeded per-location anchoring layer is the only candidate moat (§0.9 reaffirmed). (2) **E-commerce 3D/AR ROI is partly real but vendor-inflated** — the famous "94% conversion lift" (Shopify), "11x" (Houzz), "65%" (Rebecca Minkoff) are selection-biased vendor marketing, NOT causal lift; the credible, mechanism-backed effect is **~20–40% return reduction for high-uncertainty categories** (furniture/home/eyewear/footwear/cosmetics) + a modest single-to-low-double-digit conversion lift. Build the un-anchored business case on returns + a conservative lift, demand a real A/B test. (3) **The "free anchored, convert on analytics" hypothesis is mis-sequenced** — storefront/awareness QR scan rates are low single digits, so a scan-analytics dashboard will often show a number too small to justify a charge (kills the upsell). **Convert anchored on CAPABILITY gates** (branded image-marker, persistent per-location map, multi-location, watermark removal), analytics as support only. (4) **8th Wall migration wedge is real but crowded + time-boxed** (≥8 vendors already running migration campaigns; refugees skew enterprise) → treat as a launch accelerant/SEO+credibility play, not the franchise. (5) **Timing: phone-now is right, glasses-later slips to ~2028–2032 for world-anchored AR** — display-LESS AI glasses (Ray-Ban Meta ~9M sold) can't render anchored AR; the endgame needs display+world-tracking glasses (Orion prototype-only, Snap Specs $2,195 niche). Underwrite on phone-only economics. (6) **Bottoms-up SAM ≈ $0.5–1.5B/yr ARR** (online-store + local-business beachhead), 2–3 orders of magnitude below top-down "$2–3T AR" hype; ~$10–50M obtainable ARR at 1–5% penetration over 5 yrs. **Pricing rec:** un-anchored = charge day-one, card-gated trial, **unlimited views (never meter on views)**, Starter ~$19–29/Growth ~$99–149/Enterprise; anchored = free seeding tier (one location, watermark, generic marker) → ~$29–79/location/mo on a capability trigger. Refines §0.9/§0.10 GTM + pricing; no spec change yet, flagged for the user to fold pricing/sequencing into §0.9. (§0.9, §0.10; builds on D35)
- **D36 — Glasses spun out into [GLASSES.md](GLASSES.md); two projects kept in parallel (2026-06-25).** User articulated a distinct **wearer-facing product**: AI smart glasses as a **personal AR productivity / dev cockpit** — binocular AR screens/widgets (PDF/docs, IDE + agentic coding, web, AI chat, live transcribe/translate), voice commands, **phone-offload** (consistent w/ §2), **hands-free** nav. Decisions taken: **keep BOTH products** — this file stays the SSOT for the D35 B2B platform; the glasses get their own SSOT [GLASSES.md](GLASSES.md) (same directory, same maintenance rule). v1 **input = eye-tracking cursor + a discreet confirm ladder** (dwell-to-click → silent-speech "select" → temple-tap → optional sEMG wristband); **"think-to-click" BCI = research line to MONITOR, not a v1 dependency** (in-frame EEG is noisy/unproven; NextMind shelved; only Meta's sEMG-wrist ships). Hand input dropped. This **reframes the original stealth Track A (§1)** toward a less-stealthy binocular cockpit and **supersedes D18's input model for the glasses product** (D18 still stands for any stealth-agent framing). Full spec + feasibility grading + decision log (G1) in GLASSES.md. (§1, §2, §2a, §6b/D18, D34, D35; [[stealth-agent-glasses-product]])
- **D35 — PRODUCT PIVOT: Marker-Anchored AR Asset Platform (2026-06-25). New lead product, full spec in §0; continuity map in §0a.** The project reframes from "stealth agent glasses + summon-an-animated-splat-into-my-room" to a **no-code platform where any business publishes a 3D asset two ways: (1) an instant un-anchored "view in 3D" web/print widget, and (2) a real-world anchor placed via a physical MARKER that points to an owner-seeded, cloud-hosted per-location map.** Core insight = the marker carries a POINTER not a map (global localization → trivial local "load *this* map"), and the map is owner-seeded per-location (the "global" map assembles one paying shop at a time → launches today, no planetary scanning, no Google/Niantic dependency). Two surfaces are sequenced by difficulty: **un-anchored ships FIRST** (works on every phone/browser today, proven e-commerce demand, funds the business, builds the creator library) → **capture the 8th Wall migration** (Niantic retired hosted 8th Wall Feb 2026; brands need a no-code home now) → **anchored as an upsell** (marker-first, dense commercial districts) → **glasses as the architected endgame** (phone is the bridge viewer). Moat = NOT the crowded authoring layer but (a) best no-code migration capture + (b) the owner-seeded, marker-indexed geospatial anchoring layer (the piece 8th Wall did NOT open-source). Primary technical risk = **relocalization robustness** (re-localize across lighting/season/occlusion/device → guided multi-condition capture + ongoing re-capture + marker-only fallback); primary strategic risk = **glasses timing** (mitigated by the phone bridge). **What carries forward (§0a):** the working generation pipeline (D24–D29) becomes the authoring image-to-3D/animation backend; the library/dedup work (D32) becomes the asset store; **D34's mesh-first / splats-as-hero-luxury becomes the baked-in rendering guidance** (animated splats explicitly DEFERRED to premium/static-only); cross-device/additive-display/world-units discipline (§2a) carries as §0.6. **What's deferred/out of current scope:** stealth-glasses hardware, the autonomous-agent Track A, physics (§5), humanoid telepresence (§6). MVP (§0.10): no-code authoring (2D canvas + product-image) · un-anchored publish (embed/link/print QR) · anchored via marker-pointer + per-location map on phone WebAR · guided capture · anchor registry with owner/permission/spatial-claim · cross-device world-units output w/ ≥2 quality tiers. Engine basis = open-sourced 8th Wall engine + WebXR/three.js; localization = rent VPS where coverage exists OR the no-VPS marker-pointer path (the differentiator). Governance (spatial rights / moderation / privacy) built into the data model from day one. (§0, §0a; supersedes §1's lead framing; builds on D32/D34, [[stealth-agent-glasses-product]], [[asset-library-architecture]])
- **D38 — 8th Wall due-diligence: the "open-sourced engine" assumption is WRONG; corrects §0.6 ([research/8thwall-deep-dive.md](research/8thwall-deep-dive.md), 2026-06-25).** Primary-source dig (repos + LICENSE files): (a) hosted 8th Wall (editor/Studio/hosting) retired **Feb 28 2026**; already-published experiences run until **Feb 28 2027** then die; the project moved to 8thwall.org / github.com/8thwall as "community-driven" with **no Niantic maintenance commitment.** (b) **The valuable SLAM/world-tracking engine was NOT open-sourced** — it ships only as a **closed-source binary** (`github.com/8thwall/engine`) under a restrictive **"XR Engine License"** (NOT MIT): revocable on 5 days' notice, no modify/decompile, and it **prohibits products "whose value derives substantially from the Software" and forbids building "a substantially similar or competitive product"** — a no-code AR-builder SaaS (our §0 shape) is the named likely-prohibited case. The MIT repo (`github.com/8thwall/8thwall`) is only the ECS/scene framework + Face/Image/Sky glue + tooling; **SLAM is explicitly excluded.** (c) **VPS / Lightship Maps / Hand-Tracking were not released at all** → owner-seeded per-location maps remain genuinely defensible (D35 moat thesis CONFIRMED, even stronger). **Decision: do NOT build on the 8th Wall binary as the core** (revocable + anti-compete clause aimed at us); if used at all, keep it abstracted/replaceable + get written license clarification. Replacement stack folded into §0.6. Context: Niantic acquired 8th Wall Mar 2022; sold games to Scopely ($3.5B, closed May 29 2025); 8th Wall now sits in the spun-out **Niantic Spatial Inc.** **NOTE: this contradicts D39's tech-stack report, which mistakenly assumed 8th Wall SLAM is "now MIT" — D38 (read the actual LICENSE) is authoritative; trust D38 over D39 on this point.** (§0.6; corrects §0.6 + the "Engine basis = open-sourced 8th Wall engine" line in D35; builds on D35/D37)
- **D39 — Tech-stack + architecture research ([research/tech-stack-architecture.md](research/tech-stack-architecture.md), 2026-06-25).** Component-by-component architecture (no code) for both surfaces, web-sourced + cited. Key resolutions: (1) **Un-anchored = web forever** — `<model-viewer>` (Apache-2.0) → Scene Viewer on Android, **AR Quick Look + a pre-baked USDZ on iOS** (native ARKit, which sidesteps the verified **iOS-Safari-has-no-WebXR** wall). The glTF↔USDZ split is real → **bake a curated USDZ per asset at publish** (auto-conversion is lossy: one animation track). (2) **Anchored = web (WASM-SLAM) for MVP → native ARKit/ARCore as a phase-two premium tier** (LiDAR occlusion + durable persistence) and the OpenXR-glasses on-ramp. Hard constraint: **vanilla WebXR cannot localize against a custom cloud map on any browser** → anchored forces WASM-SLAM or native. (3) **Map = the build-vs-rent crux:** RENT **Immersal** (or ARCore Geospatial) to ship; **BUILD owner-seeded COLMAP+hloc as the moat** — landmine: hloc's default SuperPoint/SuperGlue weights are non-commercial → **swap LightGlue/DISK/R2D2 before ship**; Azure Spatial Anchors retired Nov 2024. (4) **Existing pipeline plugs in unchanged** — `generation/server.py` (TRELLIS-resident + UniRig + `bind_splat`, RTX 5090) becomes the authoring image-to-3D backend behind an async job queue; mesh-first (D34) is the authoring default, static splats = hero tier, animated splats deferred. (5) **Biggest technical risk = relocalization robustness** (one-pass owner scan often won't re-lock) → guided multi-condition capture + every marker scan as a fresh re-mapping sample + map-baked occluder mesh + marker-only fallback (= §0.7, reaffirmed). Report has two text architecture diagrams + a consolidated stack bill with licenses. **CAVEAT: its "8th Wall SLAM is now MIT" premise is wrong — see D38.** All of this is recorded research, **no §0 spec rewrite yet beyond the §0.6 correction** — flagged for the user to ratify the web-vs-native phasing + map build-vs-rent into §0.6/§0.10. (§0.6, §0.7, §0.10; builds on D35, corrected-by/corrects D38)
- **D40 — Phased implementation plan created ([IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md), 2026-06-25).** Companion build plan for the D35 platform, sequenced by the §0.9 GTM bet + the corrected stack (D38/D39). Six phases: **0 Foundations** (auth/store/CDN/Stripe/job-queue fronting the existing `generation/server.py` pipeline) → **1 Un-anchored "View in 3D" MVP** (the revenue wedge — product-image + 2D-canvas authoring, `<model-viewer>` with Android-WebXR/iOS-USDZ split, embed/link/print-QR, charge day-one, never meter views) → **2 8th Wall migration capture** (time-boxed acquisition into the paid un-anchored tier) → **3 Anchored MVP** (marker service + RENTED map [Immersal] behind a swappable `MapService` iface + anchor registry with owner/permission/spatial-claim + guided capture + abstracted web WASM-SLAM, NOT the 8th Wall binary; free-seed → capability-gated ~$29–79/loc/mo) → **4 Anchored moat** (own COLMAP+hloc map with LightGlue/DISK/R2D2 not NC weights + occlusion + native ARKit/ARCore = the glasses on-ramp) → **5 Glasses-ready** (deferred ~2028–2032, underwrite on phone-only economics). Asset format = canonical GLB (mesh-first, Draco/meshopt/KTX2) + baked USDZ per asset. Cross-cutting: governance-in-schema-from-day-one + a licensing release-gate checklist. **iOS clarification recorded** (answers a user question): all iOS browsers run on WebKit → none support WebXR (Chrome-WebXR is Android-only) → iPhone un-anchored AR goes through native AR Quick Look/USDZ, not WebXR (verified via web search, June 2026). Assumptions = small/solo team + revenue-first; open decisions (pricing ratification, lead authoring pipeline, resourcing, Immersal-vs-ARCore) listed in the plan §11, none blocking the first sprint. (§0.6, §0.9, §0.10; builds on D35/D37/D38/D39)
- **D41 — Textured GLBs for the un-anchored product, via a commercially-clean gsplat+nvdiffrast bake (2026-06-26, BUILT+verified).** Product viewers need COLORED assets; the worker's rigging path emits geometry-only (gray) GLBs. TRELLIS's textured `to_glb` bakes a texture but **renders the gaussian via `diff_gaussian_rasterization` (Inria, NON-COMMERCIAL — the §4a/D20 "never ship" landmine)** — a hidden licensing trap in the obvious path. User chose the high-fidelity clean route over a vertex-color baker. Fix: **monkeypatch TRELLIS's `GaussianRenderer.render` to use gsplat (Apache-2.0)** (`generation/gsplat_render.py`; wxyz quats, normalized-intrinsics→pixels, black bg, SH degree-0), then nvdiffrast bakes a real 1024² UV atlas. New worker path `generation/server.py:/generate_static` + `pipeline_static` (image→TRELLIS mesh+gaussian→gsplat 100-view render→nvdiffrast bake→textured GLB, no rig); platform un-anchored creation now calls it. **Blackwell note:** the 5090 is sm_120 but the box's CUDA toolkit is 12.5 (nvcc maxes at sm_90) → build PTX `9.0+PTX` and let the 580 driver JIT to Blackwell; `CUDA_HOME=/usr/local/cuda-12.5` + `TORCH_CUDA_ARCH_LIST=9.0+PTX` baked into server.py. nvdiffrast 0.4.0 + gsplat 1.5.3 both compiled+ran on Blackwell this way. **Verified:** crate image → real wood/metal 1024² texture (not garbage) → USDZ → serves at `/v/crate`. Extends §4a clean stack: **nvdiffrast (permissive) + gsplat (Apache-2.0) are the clean texture-bake renderers; never the Inria rasterizer.** (§4a, §0.5, §0.6; builds on D40, [[licensing-landmines]])
- **D44 — TRELLIS.2 benchmarked locally on the 5090 — quality is EXCELLENT (game-ready PBR), decisively beating our gsplat-bake; recommend adopting it as the generation backend (2026-06-28).** Per the user's "prototype both" call (D43), did a full local self-host build of **TRELLIS.2-4B** (MIT, native-3D PBR) and ran the dwarf/butterfly/dragon through it. **Result: crisp, bright, correctly-wrapped PBR textures + clean geometry — essentially matching the input images** (vs our muddy/dark gsplat-bake). This is the Meshy/Tripo-class quality the user wanted, self-hosted under a clean license. **Build reality (the cost):** a multi-hour Blackwell (sm_120) slog — had to (1) use torch 2.11+cu128 + xformers 0.0.35 (the only Blackwell-working combo; torch 2.8/xformers 0.0.32 crashes attention with an uncatchable CUDA error; dodge `pypi.nvidia.com` DNS via `--extra-index-url pypi.org`), (2) build 4 custom CUDA exts (nvdiffrast/cumesh/flexgemm/o_voxel) with CUDA-12.5 `9.0+PTX`, (3) patch transformers-5.x API drift (`model.model.layer`), (4) work around TWO gated deps: DINOv3 (commercial-OK, attribution; user got access) and **briaai/RMBG-2.0 (NON-COMMERCIAL → bypassed by feeding RGBA pre-cut with clean u2net `rembg`; the pipeline skips RMBG when input has alpha).** **Latency:** ~80–145 s/asset at 512³ on the 5090 (fine for our async job queue; not interactive). **Cost:** $0/asset (own GPU). Setup lives in conda env `trellis2` + repo `/home/sov2/projects/TRELLIS.2`; bench script `generation/bench_trellis2.py`. **RECOMMENDATION (updates D43): adopt TRELLIS.2 as the geometry+texture backend** (replacing v1 TRELLIS + the gsplat-bake), keep **UniRig** for rigging + the **transfer_rig** step (skin the TRELLIS.2 textured mesh) for the animated path; production needs the RMBG→u2net swap baked in (commercial-clean) + "Built with DINOv3" attribution. **Tripo (buy) comparison still pending credits** — but TRELLIS.2 quality already clears the bar, is self-hosted, clean-license, and $0/asset, so it's the front-runner; Tripo remains the lower-ops fallback. **INTEGRATED + verified (2026-06-28):** new worker [generation/server_t2.py](generation/server_t2.py) (conda `trellis2` env) replaces the v1 server.py backend, **same platform job contract on :8077** so the platform/`gen-worker.ts` is unchanged. **Static path** (`/generate_static`): u2net bg-cut → TRELLIS.2 → 1024² PBR GLB (~6MB, base-color + metallic-roughness) → renders beautifully in the product viewer (shiny gauntlets, reflective anvil — full PBR). **Animated path** (`/generate`): TRELLIS.2 textured mesh → clean_mesh → UniRig skeleton/skin → prep_viewer (bake motion) → **transfer_rig skins the TRELLIS.2 textured mesh onto the rig** → animated PBR GLB (verified: rig_ok, 22 bones, plays in model-viewer + USDZ). Run the backend with: `PORT=8077 conda run -n trellis2 python generation/server_t2.py` (needs ~24GB VRAM; replaces v1 server.py). v1 server.py retained for the studio/animated-splat experiments. (§0.5; builds on D41/D42/D43, [[generation-texture-method]], [[licensing-landmines]])
- **D43 — Texture quality is critical; our gsplat-render→bake is the WRONG method; recommend wrapping a commercial generation API (deep-research, 2026-06-28, [research/generation-texture-rigging.md](research/generation-texture-rigging.md)).** A colourful input (a dwarf, a head) bakes out muddy/dark in our TRELLIS pipeline. Root cause CONFIRMED by research: we render TRELLIS's Gaussian splats from many views and bake that → a soft, view-averaged, shadow-contaminated single-colour texture. **The commercial tools (Meshy/Tripo/Rodin/Hunyuan3D) use a fundamentally different method: mesh-conditioned MULTI-VIEW PBR DIFFUSION** — a diffusion model conditioned on the mesh's geometry (normal/coord maps) synthesises view-consistent images (reference-attention for fidelity + multi-view attention/sync + 3D-aware RoPE for consistency), as **decomposed, delit PBR maps** (albedo/metallic/roughness, illumination-invariant), back-projected to UV + super-res + seam-inpaint. No amount of tuning our splat-bake closes that gap — it's the wrong method. **Rigging:** our **UniRig (MIT) IS already the right modern learned auto-rigger** (Skeleton-Tree-Tokenization transformer + Bone-Point Cross Attention, arbitrary categories) — keep it. **Licensing landmine:** the best OPEN PBR-texture models (Hunyuan3D-2/2.1/Paint, MaterialMVP) ship under a non-OSI **Tencent Community License** (commercial cap at 1M MAU, **void in EU/UK/South Korea**) → **do NOT ship**. The only unambiguously MIT/Apache self-hosted texture upgrade is **TRELLIS.2** (native-3D PBR, no bake) — but quality is so far only the authors' un-benchmarked claim. **Build-vs-buy:** commercial APIs give best-in-class texture+rig at ~$0.10–0.40/asset (Meshy/Tripo; Rodin $0.50–1.50+), snappy, with **commercial/resale rights on PAID tiers** (Meshy Premium = full rights no attribution; Rodin/Hyper3D all tiers; Meshy FREE = CC-BY, do not use). **RECOMMENDATION (pending user ratification): wrap a commercial API (Meshy or Tripo) behind our existing async job interface** (`submitGeneration→poll→finalize` — a contained worker-backend swap; the platform doesn't care where the GLB comes from), since generation is the authoring BACKEND not the moat (§0.5/§0.9) and this instantly hits the quality+snappiness bar; keep self-hosted TRELLIS+UniRig as a fallback/cost-lever; revisit TRELLIS.2 for a clean self-host texture upgrade after benchmarking. Pricing/ToS are blog/vendor-doc sourced (mid-2026) → verify live before committing. (§0.5, §0.9; builds on D41/D42, [[licensing-landmines]], [[generation-pipeline-reality-check]])
- **D42 — Direction: deepen the no-code AUTHORING/GENERATION suite as the lead workstream; the four Phase-1 plumbing gaps are recorded + deferred (2026-06-27).** Asked "what's next," the user chose to invest in the **creation experience** (the surface customers actually touch) over revenue/delivery plumbing. **Recorded + deferred (all still valid, do later):** (1) **Stripe billing + card-gated trial** — the "monetizes day one" exit criterion, currently ABSENT (plans per D37: Starter ~$19–29 / Growth ~$99–149, unlimited views, never meter); (2) **production asset delivery** — R2/CDN + Draco/meshopt/KTX2 + LOD (storage is local-disk today); (3) **analytics** — PostHog/self-host, track returns/engagement, NEVER meter views (D37); (4) **2D-canvas authoring** (the second pipeline). **New active direction = build §0.5 into a real no-code studio:** video AND image input → 3D object OR 3D animation; a no-code **animation editor** (keyframe/timeline) + **drag-and-drop motion library** + **AI-assisted motion** (replicate movement from a video via monocular mocap→retarget; or describe-the-scene/motion in text); **templates**; an **AR scene/page editor** with **backgrounds/themes** (e.g. restaurant menus) = the published-experience composer; and **splatting made easy** (static-first; finish the D33/SPIKE animated-splat POC before exposing animated splats). **What already exists to build on:** the `/generate` rigged pipeline (TRELLIS→UniRig→`bind_splat`) + [studio.html](generation/studio.html)'s working in-browser GLB skinning + the bound skinned-splat PLY — but **only `/generate_static` is wired into the platform**; the rigged/animation path lives in the standalone studio and must be productized. **Honest flags:** (a) §0.9 calls authoring "the on-ramp, NOT the moat" and the plan's principle #1/#2 is "ship the paying surface first / don't build the moat before the wedge" — deepening authoring pushes revenue plumbing later; defensible because **best-no-code-UX IS the Phase-2 8th-Wall-migration wedge** and video→animation + AI-assist is genuinely differentiated vs the crowded static-viewer field, but the revenue trade-off is held consciously; (b) **video→3D-animation and text/video→motion are research-grade** for arbitrary subjects (humanoid/simple rigs first; arbitrary creatures hard) — [[animax-reimplementation-findings]] (AnimaX animated-mesh gen has commercial-licensing constraints), [[generation-pipeline-reality-check]]; (c) D34's mesh-first runtime + "animated splats deferred/expensive" still governs — splatting-made-easy = static-splat authoring UX + finishing the POC, NOT making animated splats the baseline. Expands §0.5 + adds the AR scene/page-editor component; refines IMPLEMENTATION_PLAN Phase 1. (§0.5, §0.6, §0.9; builds on D34/D35/D37/D40/D41, [[animax-reimplementation-findings]], [[generation-pipeline-reality-check]])
- **D63 — CUSTOMIZATION architecture: disentangled swappable/recolorable LAYERS on a FIXED body+face base (refines D57's compositional design); one-page avatar spec written (2026-07-13).** User asked whether per-avatar library assets lock in hair/clothing/eyes, or whether those can be user-customizable on the SAME body/face. Answer: **the layered design already gives this** — keep **body shape + face identity FIXED per avatar** (the "base" = who the avatar is), and make **hair, clothing, eyes, accessories separate attachable gaussian LAYERS** on top. **Two distinct kinds of customization (different difficulty):** (1) **RECOLOR** — color is a per-gaussian attribute, so changing it is cheap math on that part's gaussians → **hair color, eye color, skin tone, clothing color = instant, on-device, unlimited**; (2) **SWAP** — attach a different **pre-made** part rigged to the same body → **hairstyle, garment, accessories = a parts library ("wardrobe"); user picks one** (generating a brand-new arbitrary part from a prompt is a separate, harder generative track, not needed for a curated library). Both rely on each part being a **labeled/separate group of gaussians**, which the layered construction gives for free (hair is its own asset, iris its own group). **Why fix the base:** body+face constant → hair/clothing **fit once**, no re-fitting; a varying body shape would force auto-refitting per garment/hairstyle (hard) — so fixing the base is the simplification that makes customization tractable. **Fits the puppet/data-packet model (D62):** the packet also carries **which-parts + what-colors** (composition), alongside motion (joint angles) + expression (ARKit coeffs) + audio. **Bonus — shrinks the dataset:** disentangling means you train **bodies+faces + a separate parts library**, NOT every avatar×outfit×hairstyle (kills the combinatorial explosion — the core reason layers beat a monolith, D57). **Honest hard bits:** clothing that **drapes/moves** with body motion (the MM2 layer), **hair as gaussians** (fine/translucent/moving), and **seams** (hairline/collar/neck) within the phone budget. **Clean-stack mapping:** body=Anny, face=LAM, clothing=MM2, motion=GEM-X/Kimodo, lip-sync=Audio2Face/LAM_Audio2Expression, expression vocab=ARKit, render=gsplat. Full one-pager: **[docs/avatar-architecture.md](docs/avatar-architecture.md)**. (§0.5, §2, §3; builds on D57/D60/D61/D62, [[rigged-gaussian-splatting]], [[asset-library-architecture]])
- **D69 — Motion licensing VERIFIED by reading the actual license texts verbatim (3-agent research, 2026-07-18); BONES-SEED REJECTED, and the clean path is the NVIDIA-Open-Model motion family by VARIANT. Corrects D67's wrong "BONES-SEED = CC-BY-4.0 commercially-friendly" call.** Triggered by the user pasting the HF gate: *"I agree… and I qualify as an Academic User (non-commercial…) or… my company's current annual gross revenue is less than $1,000,000 USD."* **⛔ BONES-SEED (`bones-studio/seed`) — HARD REJECT** (agent retrieved & read [bones.studio/info/seed-license](https://bones.studio/info/seed-license) verbatim): a **custom proprietary contract** (AIDVISORY Sp. z o.o., Poland), NOT CC-BY-4.0 (attribution strings appear but the governing instrument is bespoke; Licensor asserts sui-generis DB rights + treats data as trade-secret confidential info). Free ONLY to non-profit "Academic Users" or a "Qualifying Startup" (annual gross revenue **< $1,000,000**). Fatal clauses for a scaling product: **(1)** "Results" is defined to sweep in **trained models, rendered animations, derivative works** → baked frames + optimized gaussian avatars + a motion library ALL fall under the license; **(2) Sec 4 auto-termination** — must notify within 30 days if revenue >$1M **or a Change-of-Control** (merger/acquisition/IPO/>50% voting transfer); "license… shall automatically terminate 30 days after such event"; continued use = "material breach and intellectual property rights infringement"; **Sec 12** forces you to "cease all… commercialization of any Results… and withdraw such Results from any products" + "permanently delete… the raw Dataset" = **rip the motion back out of your shipped product** (self-destruct for a scaling platform); **(3) Sec 6 "No Competing Datasets & Generative Models"** bars using it to "train… any generative model whose primary purpose… is to generate motion capture data, human motion sequences, animation data, or any output that functions as a commercial substitute for the Dataset" (safe-harbor is only for **non-data** outputs like robot control policies) = **our exact use case, disqualifying even while <$1M**; **(4)** no-redistribution, Poland exclusive jurisdiction, $100 liability cap, broad indemnity of Licensor, unilateral term-change on 14 days notice. → Treat like AMASS/SMPL/Tencent; the only compliant route at scale is a negotiated PAID license (licensing@bones.studio). **✅ CLEAN PATH = NVIDIA Open Model License family** (license read verbatim from the June-2024 PDF: Sec 2.2 grant "perpetual, worldwide, non-exclusive, no-charge, royalty-free… create derivative works… sell… distribute… import"; Sec 2.4 "NVIDIA claims no ownership rights in outputs"; genuinely **NO revenue/MAU cap**; only conditions = Trustworthy-AI/guardrails + patent/copyright-litigation-defense termination). CAVEATS (verbatim): license is **REVOCABLE** (already-generated outputs survive → generate the corpus while active) + **AS-IS** disclaiming title/non-infringement (Sec 5) + **Sec 7 indemnity flips onto YOU for third-party claims arising from OUTPUTS**. **TAINT IS BY VARIANT (the key correction — my earlier "NVIDIA-Open = clean regardless of training data" was TOO LOOSE):** ✅ **GEM-X** (video→motion, `NVlabs/GEM-X`) trained on **NVIDIA-OWNED** data (RigPlay-1 = "350,000 mocap sequences owned by NVIDIA", Audio2Gesture NVIDIA-owned, RenderPeople, HDRI Haven) — **NOT Bones-SEED at all → cleanest**; but it's a pose ESTIMATOR needing your **own video** input; ✅ **Kimodo-RP** (RigPlay variants, SOMA/G1) trained on NVIDIA-owned proprietary RigPlay (~560–700h) → clean text→motion (**use SOMA/G1 + RP ONLY**); ⛔ **Kimodo-SEED** (trained on the gated 288h Bones-SEED) → outputs arguably a "Result"/derivative of BONES-SEED + Sec 6 targets exactly a motion-substitute → **RADIOACTIVE, never**; ⛔ **Kimodo-SMPLX** = non-commercial (SMPL-X/MPI). Note (unsettled, flagged): NVIDIA's own right to train a generative model on the *public* SEED subset is inferred not documented; RP/GEM-X provenance (NVIDIA-owned) is the safe anchor. 🟡 **CMU MoCap** (`mocap.cs.cmu.edu/faqs`, verbatim): "may be copied, modified, or redistributed" + "You may include this data in commercially-sold products, but you **may not resell this data directly, even in converted form**" → no revenue cap, ML-training + baking OK, add NSF/mocap.cs.cmu.edu attribution; but the resale clause makes it **bake-into-avatars ONLY, NOT the standalone redistributed library**. 🟡 Truebones/Mixamo = same bake-yes/redistribute-no shape (Mixamo also bars ML-training corpora, D67). **PRODUCT DISTINCTION that changes what's usable:** baking motion INTO shipped avatars (looser — CMU + Kimodo-RP fine) vs selling a STANDALONE motion library (stricter — needs GEM-X-on-own-video or own mocap capture). **RECOMMENDED CLEAN CORPUS (ranked):** (1) **GEM-X on own self-shot video** — the only path clean for ALL uses incl. redistribution at unbounded scale; (2) **Kimodo-RP** for text-driven motion; (3) **CMU** as free bake-only bulk; (4) **own-capture** (mocap suit / work-for-hire actor) = bulletproof zero-dependency hedge against NVIDIA's revocability — all retargeted to Anny (Apache). **For SEEING natural motion now (clean, no GPU): CMU walk → CMU→Anny retarget (D67 kernel) → render locally.** Strategic note surfaced to user: clean scalable motion is a genuine hard MOAT of this project, not an afterthought (3rd licensing wall: Mixamo→BONES-SEED→now). (§2, §3, §0.5; corrects D67, builds on D61/D64/D68, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D68 — Runtime AI-control architecture verified (survey of NVIDIA ACE / Inworld / Convai + primary sources, 2026-07-18): the puppet/data-packet model is the mainstream "two-clock director/animator" pattern; the user's LLM-director, separate-specialists, and DeepSeek-MTP intuitions are all confirmed correct.** Answers the user's "how does the AI drive the avatar at runtime?" question. **THE TWO-CLOCK SPLIT (the reference architecture, verified across every surveyed system):** (1) **DIRECTOR = the LLM/dialogue-manager**, runs **per turn** (never per frame), streams out: reply TEXT (sentence-by-sentence → TTS) + one EMOTION label + sparse gesture/action/clip-ID TAGS — it **never emits joint angles or blendshapes**; (2) **ANIMATOR = on-device @ 30–60 fps**: the **FACE is driven by the AUDIO** (audio→blendshape net) and the **BODY by the sparse tags** (clip library + state machine). "Audio-drives-face / tags-drive-body / LLM-drives-neither." **FACE (solved, clean, on-device):** every system emits ARKit blendshapes — primary pick **LAM_Audio2Expression** (`aigc3d`, Apache-2.0, Wav2Vec2 ONNX, real-time on CPU, audio→52 ARKit) which is already on the D57/D62 stack; higher-fidelity option **NVIDIA Audio2Face-3D** (code MIT / weights NVIDIA Open Model License, 30 fps ARKit + 11 emotions, v2.3 CNN & v3.0 2-step diffusion); **AVOID NeuroSync open weights (CC-BY-NC = non-commercial)**. Blend the director's emotion label onto the audio-driven face. **BODY (the honest reality):** **NO shipping product LLM-generates joint angles** — body = curated real mocap (D67, retargeted to Anny), triggered by the director's tags, blended by a state machine (NVIDIA ACE Animation Graph exposes exactly this via `posture_state`/`gesture_state`/`facial_gesture_state`/`position_state` HTTP knobs; Inworld/Convai fire `<action>`/emotion tags → predefined clips). Generative co-speech gesture (EMAGE, DiffSHEG, LiveGesture, MotionGPT) is **authoring-time desktop-GPU research, NOT phone-real-time**, and **almost all are trained on non-commercial data (SMPL-X/FLAME/AMASS/BEAT2) → cannot ship** — the **only clean generative body model is Kimodo-SOMA-SEED-v1.1** (NVIDIA Open Model, trained on the *open* 288h Bones-SEED; note the "700h" figure was the proprietary RP variant — the clean set is 288h; offline ~2–5 s/clip on a 3090 = an authoring tool, not a runtime). **Non-robotic look comes from technique, not a bigger model:** INERTIALIZATION on state transitions (decays the pose offset, preserves momentum — a mid-wave→idle stops naturally instead of "finishing the wave") + MOTION MATCHING for foot-planted locomotion + always-on ADDITIVE breathing/idle-sway + procedural gaze/head-IK. **LATENCY (two distinct clocks, don't conflate):** response-START latency (endpointing + STT + LLM TTFT + TTS-first-chunk; target user-stops→avatar-starts p50 300–500 ms) vs streaming THROUGHPUT (decode tok/s once talking). **The dominant, usually-omitted latency term is ENDPOINTING** — rule-based trailing-silence VAD waits 200–700 ms; semantic/"smart-turn" VAD cuts confident endpoints to ~300 ms → this is the #1 lever, ahead of model choice. **DeepSeek-V3 MTP / speculative decoding (user's idea — directionally RIGHT + 1 correction):** a small draft proposes K tokens, the big model verifies in ONE parallel pass, provably **LOSSLESS** (identical output distribution) → full strong-model quality at ~1.8× (V3 MTP: predict-next-2-tokens, 85–90% 2nd-token acceptance). **Correction:** it speeds SUSTAINED STREAMING (keeps TTS fed so long turns don't stall), **NOT** the initial response-start (first token still needs a full target forward pass), and it is **LANGUAGE-LAYER ONLY** — motion/lip-sync are frame-clocked diffusion/regression nets, nothing to speculate. DeepSeek-V3 is a 671B cloud MoE (doesn't run on-device) — the *technique* transfers to any LLM. **SPECIALISTS-over-monolith (validated):** each model on its own clock; **latency isolation** — the audio→face net runs straight off the audio and never waits on the LLM, so lips stay synced even while reasoning is slow; independent swap + clean per-component licensing; tiny deterministic on-device models fit the frame budget where a joint end-to-end model can't. **LLM placement:** cloud reasoning-LLM as director (turn spec-decoding ON) + on-device SLM as instant-ack/offline fallback — clean-license picks **Phi-3.5-mini (MIT)** or **Qwen2.5-1.5B (Apache)**; AVOID Qwen2.5-3B (custom Qwen license), treat Llama-3.2 (Community) + Gemma (Gemma Terms) as non-OSI; **Nemotron-4 4B** = purpose-built on-device digital-human SLM. **THE DATA PACKETS, made precise:** per frame ≈ 52 ARKit floats + 163 joint angles ≈ <1 KB → **~25–30 KB/s** total control signal = 3 orders of magnitude below streamed video = the entire quantitative justification for the on-device standalone-gaussian (§2/D50) thesis. **Full data flow:** Mic → on-device VAD/endpoint → STT → text → [cloud] LLM director (spec-decoding) streams text + emotion + clip-ID tags → TTS (ElevenLabs) audio → on-device LAM audio→52 ARKit + motion controller {clip ID + emotion}→163 joint angles → phone renders the splats. (§2, §3, §0.5; builds on D50/D57/D62/D67, elaborates [docs/avatar-architecture.md](docs/avatar-architecture.md) §2, [[licensing-landmines]])
- **D67 — Motion source decided (license-verified research, 2026-07-18): replace hand-authored curves with a real clean-license motion library/generator — Kimodo/GEM-X → SOMA-X → Anny — via a near-drop-in `SOMALayer` that outputs posed Anny vertices.** Triggered by the user correctly flagging the hand-written sine-wave walk as broken (inverted knees) + doll-like. **Root cause of the doll-look (biomechanics research):** the toy rotates only 8 limb bones on a frozen, non-translating torso → it's missing everything that makes gait read as real — **#1 root forward translation locked to foot contacts** (mine walks in place → foot skate), pelvis yaw(±4–8°)/obliquity(±5°)/tilt, COM lateral sway(~5cm, 1×/stride) + vertical bounce(2×/stride, high at mid-stance), heel-strike→toe-off foot roll, **contralateral** arm swing (mine were wrong-phase), thorax-vs-pelvis spine counter-rotation, and a **double-peak knee** (loading ~15–20° + swing ~60–70°) not one symmetric sine. **Knee inversion = flexion-axis sign error:** a real knee bends POSTERIORLY (heel rises up-and-back toward the glute); fix by negating the mediolateral-axis flexion, verifying BOTH knees (L/R bones mirrored → handedness differs, a global flip can fix one and break the other). **Real mocap contains ALL of this automatically** (captures the real skeleton + per-frame root trajectory) — which is why hand-authoring can't compete. **Motion sources, license-verified (primary):** ✅ **Kimodo** (`nv-tlabs/kimodo`, TEXT→motion diffusion, 77-joint SOMA skeleton, 30fps, npz = joint pos + rotation matrices + foot contacts + root traj; Apache code + NVIDIA-Open-Model weights for the SOMA/G1 variants; trained on ~700h commercial non-AMASS mocap; **promptable** — the recommended PRIMARY; synthetic so can foot-slide → use its foot contacts to clean) — **AVOID its SMPL-X checkpoint (NVIDIA R&D license)**; ✅ **GEM-X** (`NVlabs/GEM-X`, monocular VIDEO→SOMA motion; Apache + NVIDIA-Open; NVIDIA-owned data, non-AMASS) for capturing a specific real motion from own video; ✅ **CMU MoCap** (`mocap.cs.cmu.edu`, ~2,500 free real clips, commercial-OK — re-verify faqs.php before ship; older ~31-joint skeleton → needs retarget); ⛔ **MIXAMO = RESTRICTED** (royalty-free for game/film, but its FAQ/TOS **bars using content to train ML models / bulk-download for ML** — our pipeline renders frames that train gaussian weights → disqualified; prototyping-only); ⛔ **AMASS + all SMPL/SMPL-X-derived = non-commercial** (MPI/Max-Planck license) → always drive Anny through its **`soma` or `anny` topology, NEVER `smplx`** (Anny marks smplx NC). **Retarget path (the win):** SOMA-X's `SOMALayer(identity_model_type="anny")` consumes SOMA motion (poses (N,77,3) axis-angle + root_translation) and **outputs posed Anny `out["vertices"]` directly** — `03_render_motion.py` already consumes exactly that, so it's a contained swap of `walk_pose()`; **`py-soma-x` is already installed** (Anny dependency); install extra via `uv pip install "soma-x[anny]"` / `pip install "py-soma-x[anny]"`; code Apache-2.0. **Fallback** (only if you must render the exact DEFAULT 13,718-vertex mesh / reuse UV/splat assets keyed to it): `anny.Anny(rig="mixamo")` + the verified delta-rotation retarget kernel `ΔR_world = R_cur·R_restᵀ; P[b,:3,:3] = R_rest_world[b]ᵀ·ΔR_world·R_rest_world[b]` (the generalization of our swing-axis trick; re-derive bone indices from `bone_labels` since the mixamo rig is NOT (1,163,4,4)) — feed Kimodo's `global_rot_mats` (no FK needed) or a BVH via FK; also flagged: `rig="mixamo"` confirmed, `game_engine` rig UNCONFIRMED (verify the repo rig-asset dir before relying on it). **Caveat regardless of source:** retargeting reintroduces foot-skate/knee-pop from leg-length mismatch → apply foot-contact locking / foot-IK on the Anny target. **Minimum procedural fallback recipe** (ordered), if ever needed: root-translation-locked-to-foot-plants → correct knee direction/timing → pelvis yaw + lateral weight-shift → heel/toe foot roll → contralateral arm swing → 2×/stride vertical bounce. **Next:** install SOMA-X[anny] + a Kimodo-SOMA checkpoint, generate a walk/idle/gesture set, swap `03_render_motion.py`'s `walk_pose()` for `SOMALayer` output → real natural motion in the render pipeline + Blender playback. (§2, §0.5, §3; builds on D57/D60/D61/D64, corrects the SESSION_HANDOFF "native mixamo/game_engine" note, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D66 — CLARIFICATION/CORRECTION of D65's realism framing: SYNTHETIC photorealism is valid (the user's Omniverse vision works); "real capture" is the ceiling, NOT the only path. The user's unified vision is coherent and adopted (2026-07-18).** The user pushed back on D65 reading as "photoreal ⇒ real capture only." Correction: **the one rule is `output realism = training-image realism`.** Photoreal training images can come from (a) real capture (max realism, needs a rig, privacy) OR **(b) high-quality SYNTHETIC renders (Omniverse/Cycles/MetaHuman-class) — realism ceiling = asset/shader quality (skin scans, strand hair, path-traced light, cloth sim); fully automated + privacy-clean.** D65's "render the motion set GRAY" was a COST shortcut **specific to the real-capture branch** (photoreal render ≈ 100–1000× costlier/frame than gray, so if appearance comes from capture you render motion cheaply). **It does NOT apply to the user's chosen path** → on the synthetic-photoreal path we render **photoreal-AND-moving in one pass** (appearance + motion baked together), NOT gray. So D65's gray recommendation is conditional; D66 selects the synthetic-photoreal branch. **"Appearance is separate from motion" was misleading** — in the user's plan they come together in the same render; the ONLY thing genuinely "separate" is the **customization LAYERS** (D63): to SWAP a garment/hairstyle each part must be its own gaussian group → optimize **base body + each garment + each hairstyle SEPARATELY** (photoreal, multi-view, multi-pose), compose + recolor at runtime on the phone. **This is what makes synthetic-photoreal affordable:** you render a handful of base bodies + a wardrobe of parts *each once* (NOT body×outfit×hair combos) — the D63 combinatorial-collapse. **"The model" clarified:** there is NO single feed-forward net avatars pass through — it's **PER-SUBJECT OPTIMIZATION** run once per avatar/part → gaussian set → library (why not feed-forward: muted/soft standalone gaussians that need a runtime neural renderer, D60 — kills on-device). **Driving:** finished avatar = skeleton + ARKit blendshapes → ElevenLabs/voice → **Audio2Face/LAM = lip-sync DRIVER (not an appearance source; it animates the face, the render supplies its look)** + LLM picks motions; phone renders, tiny control signals stream (the puppet/data-packet model, D62). **Cost check (per-subject, synthetic-photoreal):** ~300–600 poses × ~8 cams Cycles-rendered ≈ tens of GPU-hrs ≈ ~$7–30/subject on a 4090 — fine for a CURATED hero+wardrobe library (not 100k avatars; you don't need that). **Net product direction (adopted):** fixed base (body+skin+rig) + swappable/recolorable clothing/hair/eyes → riggable avatars driven by an LLM/voice agent; user picks body+look then wardrobe, game-character-creator style. Updated [docs/avatar-architecture.md](docs/avatar-architecture.md) §8. (§2, §3, §0.5; corrects/builds on D65, builds on D60/D62/D63, [[rigged-gaussian-splatting]], [[asset-library-architecture]])
- **D65 — Training economics + realism VERIFIED (5-agent primary-source research, 2026-07-18): the per-subject-library path is cheap, no full dataset needed to start, Omniverse is out, and photorealism comes from capture not the renderer.** Answers the user's cost/time/GPU/quality questions. **(1) COST/GPU/TIME (minimum):** the cost fear assumed the *feed-forward* regime; D62's **per-subject-optimized library** fits each avatar independently on **ONE cheap consumer GPU** — measured: GaussianAvatars 11GB/2080Ti/~40–60min, 3DGS-Avatar ~30min, GART 30s–2.5min; VRAM 11–16GB so a **24GB RTX 4090/3090 is ample, NO A100/H100**. Cheapest: RTX 4090 ~$0.25–0.35/hr (3090 ~$0.12–0.20, A6000 48GB ~$0.40) on **Vast.ai/TensorDock spot**. **Per avatar ≈ $1–3 GPU / $5–15 all-in** (fit is NOT the cost driver — data render + tuning re-runs are); a **100-avatar library ≈ $100–1,500**. The dropped feed-forward path (regime B, e.g. GS-LRM) = **8–24× A100/H100 for days = $1.5–8k/run** — correctly out of the main path. **(2) NO full dataset needed to see results** (per-subject = one-in-one-out; overfitting one first de-risks every failure mode cheaply): **Milestone 0 (~$1–3, ½ day)** = fit a *static* 3DGS to ONE already-downloaded Anny-One scene on a rented 4090 (proves gsplat builds/trains/renders; verify a scene's views share one static pose); **Milestone 1 (~$5–15, a few GPU-hrs)** = render 1 subject × ~8 cams × ~300–600 pose-diverse frames → bind gaussians to the 163-bone Anny rig (LBS + face blendshapes) → optimize (~10–60min) → drive with a **held-out** motion clip = **first ANIMATABLE avatar** (and the true unit cost). Data/subject ≈ 8 cams (→16 for the face) × 300–600 poses ≈ 2.5k–10k images; binding constraint = POSE COVERAGE (echoes D54 motion=coverage). **(3) OMNIVERSE = hard NO on the AMD-iGPU laptop** (official reqs: NVIDIA RTX ≥3070, CUDA/OptiX/RT-cores — no AMD path) → local viewer = **Blender viewport / browser gsplat viewer (SuperSplat/Three.js)**; and even on the rented NVIDIA box **Replicator is the wrong tool** (forces a USD migration + a render-engine change vs the Blender-native Anny-One domain) → use **headless Blender Cycles + OptiX**, which gives the same photoreal look AND depth/normal/segmentation passes + exact camera poses. **(4) REALISM CRUX (the key mental model):** photorealism = f(the input pixels the gaussians are optimized against). The per-gaussian degree-3 SH color is fit by an L1+D-SSIM photometric loss = a *reproduction* mechanism, not a source; **real capture in → photoreal, CG renders in → CG look.** → **MAIN texture path = (b) PER-SUBJECT DENSE CAPTURE** of real people (multi-view photos/video, even neutral light) → real appearance baked into per-gaussian SH on the Anny rig = simultaneously the photoreal source AND the exact standalone on-device deliverable (GaussianAvatars/3DGS-Avatar/GART/Codec-Avatars lineage on gsplat). **Anny-One's Cycles/HumGen3D renders = geometry/rig/coverage supervision ONLY — never the skin-appearance source** (caps at CG + sim-to-real gap). Secondary lanes: **(c) mesh-conditioned multi-view PBR texture diffusion** (D43; TexDreamer-class) + **(d) UV skin libraries** = generate capture-free *fictional* library members (premium/R&D on-demand path) and supply albedo priors for unseen regions + a later *relightable*-PBR-gaussian upgrade. Relighting caveat: captured appearance is baked under capture light → v1 = capture neutral + bake; relightable = later. **(5) RENDER OUR MOTION DATASET GRAY** (pyrender/nvdiffrast, ~$3 or free-on-laptop for a 100-subject set) — its job is the geometry/motion/coverage signal (silhouette/depth/normal/camera/pose→deformation GT; gray is a *cleaner* geometric signal than a lit render). **Cycles is ~100–1000× costlier** (~$1.8–3.7k for a 100-subject set) to bake appearance that's discarded on this path → reserve Cycles for a *small photoreal validation set* and the *explicitly-funded fully-synthetic premium path*; **Omniverse essentially never** on the D62 path (its domain-randomization value is for the dropped feed-forward regime). **CONDITION the whole gray-render rec rests on:** the trainer must route **appearance from the capture/canonical source** and **deformation from the rig**, NOT force an RGB photometric loss on the gray animated frames (else → gray avatar). **STRATEGIC FORK surfaced for the user:** a *photoreal* library ⇒ **real per-subject capture** is required; a synthetic-only Anny/Anny-One library will look CG, not photoreal — a conscious product choice (real captured humans vs stylized synthetic characters). (§2, §3, §0.5; builds on D54/D57/D62/D64, [[rigged-gaussian-splatting]], [[generation-texture-method]], [[asset-library-architecture]])
- **D64 — 4D-dataset build kicked off: platform = pure-Python headless (no game engine); Anny-One verified clean-but-static → our contribution is the 4D/animated set only (2026-07-18).** Lost access to the shared RTX-5090 box (SESSION_HANDOFF §5/§6 box is gone) → **rent a GPU** (RunPod/Vast/Lambda; SSH-only, headless). **Platform decision (resolves the user's "Unreal? Blender? engine?" question):** the dataset pipeline is a **pure-Python headless render script, NOT a game engine** — Anny is a PyTorch parametric body (`naver/anny`, `pip install anny[warp,examples]`, class `anny.Anny` v0.5, uses NVIDIA Warp; **default/`soma` topology ONLY, `smplx`=NC per D56**), so posing + multi-cam rendering is code, not GUI work, and SSH-only is the native environment (no engine to download, no viewport). Dataset renderer = **pyrender + trimesh (EGL offscreen)** — non-differentiable is fine because differentiable rendering is a *training-time* gsplat concern, not dataset gen; PyTorch3D / Blender-Cycles / Omniverse are deferred realism upgrades that don't change the rest of the pipeline. **Anny-One VERIFIED** (resolves the SESSION_HANDOFF §8 open question): **Apache-2.0, ungated** ([download.europe.naverlabs.com/humans/AnnyOne](https://download.europe.naverlabs.com/humans/AnnyOne/)), **800k images / 1280² / Blender-Cycles / ≤40 cameras per scene / FOV 30–130°**, annotated with camera params + 3D pose/shape + segmentation — **but STATIC, no temporal data** (body poses sampled from **AMASS**, hands from **GRAB**; characters are **HumGen3D**-clothed with **Anny params as the GT label**), per the paper (arXiv 2511.03589). **Reframe of "the dataset we build":** Anny-One already supplies the static multi-view appearance/shape/pose backbone → **our contribution is the 4D (animated) multi-view sequences ONLY** — same subject, same cameras, moving over time (`subjects × motions × cams × frames`, far smaller than 800k). **Honest limit:** raw Anny is untextured → our pure-Python renders are **geometry/motion supervision** (multi-view silhouettes / depth / normals / deformation-over-time), **not photoreal RGB** — photoreal appearance stays with Anny-One or a later textured/Cycles path (matches D54 "dense views + mesh GT = fidelity levers; motion = coverage"). Clean-motion rule unchanged (self-authored / procedural / audited-Mixamo / D61 GEM-X·Kimodo·SOMA-X — **never AMASS**). **Two license flags to verify before commercial reliance** (though NAVER released the whole set Apache-2.0): Anny-One's poses are AMASS-derived and its characters are HumGen3D — we sidestep both by driving OUR animation with clean motion and rendering raw Anny geometry, not Anny-One's HumGen3D pixels. **Starter artifacts written** (`dataset/`): `README.md`, `00_bootstrap.sh` (rented-box setup — EGL libs + `anny[warp,examples]` + pyrender/trimesh, verifies GPU), `01_probe_anny.py` (introspects Anny's real API + a headless render smoke test — its output grounds the Phase-1 harness since Claude can't reach the box). **Next:** run the probe → render one vertical slice (1 body × 1 motion × N cams → contact sheet) → scale to the dataset + manifest → verify Anny-One license / HumGen3D provenance. **Two-environment split (verified this session): DEVELOP on the user's Windows laptop (AMD Ryzen 5625U iGPU, no CUDA), RENDER-full + TRAIN on the rented NVIDIA box.** The laptop's local device is **CPU-only** — its Vega `gfx90c` iGPU is unsupported by ROCm on *every* OS (RDNA3/4-only) and torch-directml is stale (no cp313 wheel, pins torch 2.2) → CPU is correct and sufficient for Anny posing/skinning (`torch 2.11+cpu` on py3.13 verified working). **Env gotcha:** Python 3.13 fights the graphics wheels (pyrender/PyOpenGL, Blender `bpy`, Open3D all prefer ≤3.12) → dev in a **conda Python-3.11** env. **Same pyrender code runs both places** — Windows uses its default hidden-window GL context (desktop session present; do NOT set `PYOPENGL_PLATFORM=egl`, which is Linux-only and breaks on Windows), the Linux box uses EGL; **Blender `bpy` Cycles-CPU is the fallback** if the AMD GL driver misbehaves. **Where to render:** the dev SLICE (≤ tens of GB) local is fine (uploads in <1 min); the FULL set renders **on the box** — at ~260 GB–1 TB it won't fit in the laptop's 357 GB free and would take ~0.5–5 days to upload (crossover: render-on-box wins past ~50–100 GB). Windows-dev scripts added: `dataset/00_bootstrap_windows.ps1` (conda-3.11 env) + `01_probe_anny.py` made platform-aware. (§7 handoff, §2, §3; builds on D53/D54/D55/D57/D60/D61, [[licensing-landmines]], [[humanoid-4dgs-status]])
- **D62 — FACE/EXPRESSION LAYER decision: the lip-sync/expression ceiling is the FLAME expression basis (a SUPERVISION problem), not the renderer → drive the face from AUDIO + ARKit blendshapes, not FLAME-jaw; faces train the same synthetic-multi-expression way the body uses multi-view-motion; the composite avatar is puppeted on-device by tiny LLM-authored control signals (2026-07-13).** Follows the user's product reframe — go **photoreal + standalone, DROP feed-forward** → per-asset optimization builds a **LIBRARY** of pre-baked animatable gaussian avatars, phone renders on-device, generation-on-demand is a premium async / separate R&D path (matches [[asset-library-architecture]] + D32; and this reframe retires D61's "standalone-at-feed-forward-speed" risk since the library isn't feed-forward). **The face is a SUPERVISION/REPRESENTATION problem:** the current FLAME+mask path caps quality — LAM's own paper (`aigc3d/LAM`, SIGGRAPH-2025) states the limit verbatim ("expressions FLAME does not account for, such as sticking out the tongue" + "reliance on accurately estimated FLAME parameters"), so lip-sync is mushy and cues are missed regardless of gaussian quality. **Key mental model — the face is a body-analog:** body pose = skeleton joint angles (SOMA); face pose = a blendshape "expression vocabulary." The user's insight is CORRECT — faces train the SAME way the body does (many avatars × many expressions → model learns expression→gaussian-deformation), and the ONLY non-obvious part is choosing the expression vocabulary (the "skeleton for the face") + the drive signal. **Decisions:** (1) expression vocabulary = **ARKit's 52 blendshapes** (proper visemes + eye/brow/gaze; phone-native; unencumbered coefficient convention), NOT FLAME's ~50 speech-agnostic scan blendshapes; (2) drive **lip-sync from AUDIO** via **NVIDIA Audio2Face-3D** (`NVIDIA/Audio2Face-3D`, weights `nvidia/Audio2Face-3D-v3.0`, open-sourced Sept-2025, **NVIDIA Open Model License = commercial-clean**; audio→ARKit blendshapes/mesh-deform; authoring-time on NVIDIA GPU) or **LAM_Audio2Expression** (`aigc3d/LAM_Audio2Expression`, Apache, realtime on-device audio→ARKit) — NOT audio→FLAME-jaw retarget; (3) give the **face a disproportionate gaussian + supervision-resolution budget** (uncanny-valley sensitivity); (4) explicitly model **tongue/teeth/inner-mouth + eyes/gaze** (neither FLAME nor ARKit covers the tongue); (5) for LIBRARY hero avatars, **per-subject dense-capture face optimization** (GaussianAvatars/Codec-Avatars/VHAP lineage) sidesteps the synthetic-FLAME ceiling entirely — optimize the real person's expression space, drive at runtime with Audio2Face/ARKit. **Composite avatar stack (all pieces already on disk):** Anny body (GEM-X/Kimodo motion, D61) + **LAM**-style head (Apache; audio/ARKit-driven, not FLAME-jaw) + **MM2** clothing. **Runtime / LLM control plane (the user's "data packets"):** the server streams only tiny CONTROL SIGNALS — body skeleton joint angles + face ARKit blendshape coefficients + audio — authored/selected by an **LLM**; the standalone gaussians live ON-DEVICE and deform to those signals (the §2/D50 motion-data-only streaming). This is exactly WHY standalone-on-device matters: you can't cheaply stream a server-rendered avatar, but you CAN stream a few hundred numbers/frame. **Caveats:** compositing seams (esp. neck) within the phone gaussian budget (D57); tongue/inner-mouth still needs explicit geometry; Audio2Face runs on NVIDIA GPU → authoring-time driver, on-device runtime uses LAM_Audio2Expression or pre-baked ARKit tracks; keep FLAME to **FLAME-2023-Open (CC-BY)** ONLY where the canonical topology still needs it (migrating canonical→ARKit reduces license exposure). (§0.5, §2, §3; builds on D50/D57/D60/D61, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D61 — NVIDIA open-source sweep: a commercially-clean "SOMA ecosystem" covers most of Stages 1–2, `nvdiffrast` (already in use) is NON-COMMERCIAL and must be replaced, and every pipeline stage now has a clean option (2026-07-13).** Triggered by evaluating `NVlabs/GEM-X` (video→whole-body pose): a license-verified fan-out over NVIDIA's 115-project open-source catalog + the `NVlabs`/`nv-tlabs`/`NVIDIAGameWorks`/`NVIDIA-RTX` research orgs (all licenses read from primary `LICENSE`/model-card sources). **License model clarified:** the **NVIDIA Open Model License = permissive COMMERCIAL** (grants reproduce/sell/distribute the model + derivatives; claims NO ownership of outputs → **outputs usable as training data**; obligations only: attribution + license copy on redistribution, don't bypass guardrails, no litigation vs NVIDIA, Trustworthy-AI/export compliance) — NOT to be confused with the **NVIDIA Source Code License** (the non-commercial research license on nvdiffrast et al.). **🎯 KEY FIND — the "SOMA ecosystem" is a deliberately-clean, near-complete Stage 1–2:** **GEM-X** (`NVlabs/GEM-X`; Apache code + NVIDIA-Open-Model weights; "trained on NVIDIA-owned data only"; outputs 77-joint SOMA whole-body motion; no SMPL-X at inference) = clean **video→motion**; **Kimodo** (`nv-tlabs/kimodo`; Apache + NVIDIA-Open-Model for the SOMA/G1 variants; trained on Bones RigPlay/SEED optical mocap, **NOT AMASS**) = clean **text→motion**; **SOMA-X** (`NVlabs/SOMA-X`; Apache) = the topology hub where **Anny is a NATIVE backend** → the SOMA→Anny retarget glue (Warp LBS). This maps directly onto SESSION_HANDOFF §7.3's "Anny→pose→clean-motion" and was engineered to dodge the SMPL/AMASS trap ([[licensing-landmines]]). Clean **IF** you avoid: Kimodo's SMPLX variant (NC R&D license), SOMA-X's SMPL/SMPL-X backends (gated NC files), and `NVlabs/GENMO` (self-labels "GEM", SMPL-based, NC — the name-trap). GEM-X input video must itself be clean (self-shot/public-domain/CC — the tool is clean, the input is not automatically). **⚠️ URGENT existing-dependency landmine:** **`nvdiffrast` = NVIDIA Source Code License (1-Way Commercial) = NON-COMMERCIAL**, and it sits in `generation/server.py`'s texture-bake path → REPLACE before commercial ship (Kaolin **DIB-R** / **gsplat** / PyTorch3D). Same NC family (skip): kaolin-wisp, nvdiffrec/3D-MoMa, diffusion-renderer, GET3D, XCube, LION. **Clean option per stage:** Stage-3 render harness = **Omniverse Replicator** (turnkey multi-cam RGB+depth+seg+normals+camera params; **free-for-production since May-2026**; clean for own-asset renders; NVIDIA-GPU-only) OR lightweight **Kaolin** (Apache DIB-R + differentiable camera API) / **Warp** (Apache) — recommend lightweight-first for the MVP dataset; Stage-4 model = **Lyra 1.0** (`nv-tlabs/lyra`; Apache code + **commercial** NVIDIA-Open-Model weights) = feed-forward image→**standalone `.ply` gaussians, no runtime neural net** = the cleanest reference for our exact target shape (scene-level → add rig+LBS), plus **L4GM** (arch ref, weights NC — retrain clean) + **GAvatar** (paper-only, pose-driven-primitive Gaussians = our skin-to-rig idea); Stage-4 data-gen = **Cosmos**/**GEN3C** (outputs usable as training data; GEN3C carries an unresolved Stable-Video-Diffusion upstream-provenance flag); Stage-5 = **Slang** (Apache — author LBS+splat shaders once → GLSL/WGSL/Metal) + **vk_gaussian_splatting** (`nvpro-samples`, Apache — **sort-free weighted-sum rendering** directly attacks the per-frame depth re-sort cost of moving splats, the D50 Tier-2 gate / [[rigged-gaussian-splatting]] catch). No NVIDIA phone/WebGPU splat runtime exists → the mkkellogg viewer stays the runtime. **Full findings + sequenced plan of action (Phases A–E) in [research/nvidia-open-source-scan.md](research/nvidia-open-source-scan.md).** (§0.5, §3, §2; builds on D53/D55/D56/D57/D60, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D60 — COURSE-CORRECTION: the LHM++ raw splat is muted (neural-renderer dependency) → wrong for on-device standalone photoreal; refocus on the ACTUAL core = building our own training DATASET (not started) (2026-07-04).** After deploying the D59 avatar to the phone, user observed it looks blurred/monochrome + upside-down. Diagnosis: (a) upside-down = display bug (Y-down vs viewer Y-up; fixed by 180°-about-X PLY flip, redeployed); (b) muted color = REAL — **LHM++ is feed-forward + a NEURAL RENDERER** (`featbacksplat`: gaussians carry *features*, a per-frame neural net decodes them → image; that net holds the quality). The exportable **raw splat is soft/desaturated** (RGB per-gaussian std ~0.09 even on a colorful in-distribution input; first test was also a bad input — an all-black anime side-profile). **A neural renderer at runtime = GPU-per-viewer server-side streaming = expensive + doesn't scale + not on-device → REJECTED by user.** User re-anchored the goal (correcting my drift): **on-device splat render, photoreal, animated, ONLY motion/bone data streamed — GS for photorealism** (= §2/D50 architecture; NOT mesh-fallback, NOT neural-streaming). **Key reframe:** the neural renderer is an LHM++ property, not a GS property — GS *can* be standalone-photoreal + on-device-animatable. The muted-standalone problem is the **fast-feed-forward** tradeoff; **per-scene optimization** (minutes/avatar) yields photoreal standalone gaussians (GaussianAvatars/3DGS-Avatar/SC-GS family, rebuild on gsplat+Anny), OR a feed-forward arch *designed* for standalone quality (D55 levers). **So: do NOT retrain LHM++'s arch as-is** (inherits muted standalone); the target model must output good STANDALONE gaussians. **BIGGER correction (user caught the real drift):** we detoured into LHM++ *inference/playback* (Phase 0) for hours and **never built the actual core — our own training DATASET.** Honest state: **dataset-creation setup DOES NOT EXIST** — Anny (parametric body) cloned as a building block, but **Anny-One NOT downloaded** and **zero dataset-gen scripts written.** The LHM++ work wasn't wasted (proved the clean Blackwell stack builds/runs — [[licensing-landmines]], BLACKWELL_BUILD.md — and surfaced the neural-renderer finding that rules out its arch), but it's a detour. **RE-FOCUS = build the dataset setup:** (1) free disk (99% full), (2) download Anny-One (~800k synthetic multi-view humans, clean, STATIC poses), (3) build the **Anny → pose → clean-motion → multi-camera render pipeline** (our 4D temporal-supervision generator; clean motion NOT AMASS), (4) pick the standalone-quality architecture, (5) rent GPUs to train. Full concrete state + pointers in **`SESSION_HANDOFF.md`** (written this turn for the next session). (§0.5, §3, §2; builds on D53/D54/D55/D57/D59, [[licensing-landmines]], [[rigged-gaussian-splatting]], [[generation-pipeline-reality-check]])
- **D59 — LHM++ RUNS END-TO-END on the Blackwell 5090 → first clean-path humanoid avatar generated (160k-gaussian 3DGS PLY, SMPLX-FREE) (2026-07-04).** After a long Blackwell build slog, `to_gs_ply.py` produced a valid **160,000-gaussian standard 3DGS PLY** (xyz/opacity/scale/rot/SH-deg0, 11MB, no f_rest) from a sample image via `LHMPP-700M` — and it **ran in SMPLX-FREE mode** ("DO NOT USE SMPL-X PRIOR ... disabled"), i.e. already the clean-path variant. Full reproducible recipe in `LHM-plusplus/BLACKWELL_BUILD.md`. **The 5 Blackwell keys (reusable for any sm_120 build):** (1) compile ALL CUDA exts for **`TORCH_CUDA_ARCH_LIST=9.0+PTX`** NOT `12.0` — CUDA-12.5 nvcc can't target `compute_120` (D44 lesson; I initially mis-set 12.0 and hit "nvcc fatal: Unsupported gpu architecture"); (2) **`spconv-cu126` (cumm 0.7.11) NOT `spconv-cu120`** — cu120's cumm 0.4.11 SIGFPEs in `implicit_gemm`/`from_numpy` on sm_120 (this was THE final blocker; faulthandler pinned it in the Sonata encoder's sparse conv); (3) **xformers→torch-SDPA monkeypatch** (no sm_120 kernel; reused D52 SV4D fix) via `_compat/sitecustomize.py`; (4) **chumpy numpy-alias shim** (numpy≥1.24 removed bool/int/float aliases) same sitecustomize; (5) **huggingface_hub 0.34.4 forced** + patched LHM++'s `model_download_utils.py` self-downgrade-to-0.23.2 hack. Source-built for 9.0+PTX: pytorch3d, bundled lib/pointops, torch_scatter, diff_gaussian_rasterization (Inria NC — audited clean, eval-only, swap→gsplat for ship), simple-knn. **Prior-bundle gotcha:** the tool's mid-run fetch pulls only small files → must fully `snapshot_download 3DAIGC/LHMPP-Prior` (voxel_grid/cano_1_volume.npz 931MB + voxel_192.pth 779MB). Peak ~20GB VRAM (fits idle window; the crash was spconv, never OOM). **Disk:** freed via removing orphaned sv4d env + generative-models (D58); ~13-20GB free through the build. **STATUS:** Phase-0 asset (a real 160k humanoid avatar splat) is READY at `/tmp/lhmpp_test.ply`; **Phase 2 clean-retrain base validated** (SMPLX-FREE path works). NEXT: deploy the avatar PLY to the D50 GitHub-Pages phone-test repo → on-device fps/appearance check (static first; animated-deformation = the real Tier-2 gate). (§0.5, §3; builds on D58/D52/D44/D50, [[licensing-landmines]])
- **D58 — Phase 0+1 EXECUTION started: LHM++ chosen over LHM, repos cloned, licenses read in-repo; two new landmines + one gift; Phase 0 parked on GPU/disk (2026-07-01).** Per user go-ahead on the D57 roadmap, cloned `aigc3d/LHM-plusplus` + `naver/anny` (code only, no weights). **LHM++ (newer, use it) specs:** 700M params, input 1024, 1/4/8/16-view + video input, **160,000 gaussians** (2.5× L4GM), ~8GB VRAM, 0.79s/single-view, **exports standard `.ply`** via `scripts/inference/to_gs_ply.py`. **LICENSE = two-layer trap (verified in cloned files):** code Apache-2.0; **weights CC-BY-NC-4.0 (`LICENSE_WEIGHT`) → reference/eval-only, retrain mandatory;** **`LICENSE_NVIDIA` = NVIDIA EG3D Source Code License (NC) → an EG3D-derived CODE module is NC at the code level (not just weights) → clean build must AUDIT+REPLACE it, not just retrain** (+ check `arcface` face-ID dep). **GIFT: LHM++ ships an `SMPLX-FREE` variant** (`LHMPP-700M-SMPLX-FREE`; default `PixelShuffle` also SMPLX-free) → the SMPL-X architectural dependency may already be gone → **start the clean retrain from SMPLX-FREE** (swap residual SMPL-X→Anny default/SOMA, train on Anny-One). Anny cloned, Apache-2.0 confirmed, D56 topology trap verbatim in its README (`smplx`=NC-only, `soma`=Apache, native `mixamo`/`game_engine` rigs). **RESOLUTION Q answered:** retraining LHM++ on our data = ~LHM++'s OWN quality clean-licensed, NOT higher (ceiling = 160k gaussians/1024res, baked into arch); higher needs D55 levers (per-primitive textures/adaptive budget/refine); end-to-end, phone LOD budget caps delivered res regardless (D50). **PHASE 0 (playback gate) — I generate the test asset myself** (run LHM++ SMPLX-FREE on a sample photo → `to_gs_ply.py` .ply → deploy to the GitHub Pages phone-test repo → user opens URL on iPhone); NC weights fine for internal eval. **BLOCKED on shared box:** GPU only ~5GB free (shnri's qwen 27GB; LHM++ needs ~8GB → PARKED-and-ready SV4D-style, easy to catch given the small footprint) + **disk 95% full/26GB free** (LHM++ download tight; Anny-One 800k imgs needs cleanup — reclaim candidates: `generative-models` 23G [SV4D, camel.gif backed up, weights re-downloadable], `LAM` 11G, `MM2` 8.4G). **EXECUTION UPDATE (same session):** user disposition — **KEEP `MM2` (8.4G = user's clothing/draping-on-humanoids work → this IS the Phase-4 clothing-layer feeder, cross-ref) and `LAM` (11G = headless human + FLAME lipsync → face/expression-layer feeder); REMOVE SV4D.** Deleted `generative-models` (23G) after salvaging the non-reclonable bits to `4dgs/research/salvage/` (the reusable Blackwell sm_120 attention patch `run_sv4d2_patched.py` + sv4d2 configs + a revive-README; camel results already safe in `generation/sv4d2_out/`). Disk **26G→48G free (90%)**. Rationale: SV4D was the general-object 4D path (deprioritized by the humanoid pivot) + research-license-flagged; the `sv4d` conda env is now orphaned (can reclaim later). **Weights recon:** LHM++ SMPLX-FREE/PixelShuffle/PS-DNA variant weights are **NOT published yet** (only their configs ship — "Hub weights pending") → the clean-retrain SMPLX-FREE base = train-it-ourselves from `configs/LHMPP-anyview-SMPLX-FREE.yaml`, OR wait for publish. **What IS published:** `3DAIGC/LHMPP-700M` (4.53GB, LHM++ base, SMPL-X-dependent), `3DAIGC/LHM-1B` (6.85GB), `LHM-500M` (3.83GB). **Started background download of `3DAIGC/LHMPP-700M` (4.53GB)** → checkpoints/LHMPP-700M, to have a runnable model for the Phase-0 phone-playback test at the next GPU window. Caveat: LHMPP-700M base needs SMPL-X model files (gated/NC) + priors at runtime — fine for internal eval; the SMPLX-FREE path avoids that but needs weights we must train. **Remaining GPU-gated:** Blackwell env reconcile (LHM++ pins torch2.3/cu121 → won't run on sm_120; rebuild on the trellis2 torch2.11/cu128 combo per D44/D51) + prior/SMPL-X deps + run inference → export .ply → deploy to phone-test repo. (§0.5, §3; builds on D57/D55/D50/D51, [[licensing-landmines]])
- **D57 — HUMANOID/AVATAR track CHOSEN as the first 4DGS-generation spike; Anny-One dataset + LHM licenses web-verified; compositional-attribute design adopted; privacy resolved (2026-07-01).** User chose to start with humanoids (over general objects) — justified now that D56 gave a clean foundation. **Two web-verifications that make it viable:** (1) **Anny-One = ~800k PHOTOREALISTIC SYNTHETIC humans** (full-body poses + hands + faces, multi-env, w/ 2D/3D joints, pose params, seg masks, camera intrinsics/extrinsics, hand/face keypoints), positioned privacy/license-unconstrained for COMMERCIAL use → **the clean humanoid training set largely EXISTS** (answers "do I build the dataset?" = mostly NO for humanoids; contrast the object path's ~200-GPU-day render). Caveats: verify the dataset's own download license (may differ from code Apache), and it's HMR/STATIC-pose so **4D still needs MOTION sequences generated by driving Anny with a CLEAN motion source (self-authored/procedural/Mixamo-audited, NOT AMASS=NC).** (2) **LHM (`aigc3d/LHM`, Alibaba, ICCV 2025) = Apache-2.0 CODE**, feed-forward single-image→3DGS animatable avatar (handles face+hands) — **but its released WEIGHTS are trained with SMPL-X + real human video → reference-only/tainted;** clean path = fork the Apache arch, swap SMPL-X→Anny(default/SOMA) in data-gen, retrain on Anny-One. **PRIVACY (user asked): resolved at the foundation** — Anny+Anny-One sever the biometric↔mesh link (synthetic geometry + WHO population averages, zero real scans), so GDPR's "3D vertex cloud of a real person = sensitive biometric" problem doesn't touch the foundation; residual is app-layer only → 3 rules: (a) don't hoard user uploads (process in-memory, delete, no silent retrain w/o consent), (b) train on synthetic (Anny-One) not scraped reality, (c) no re-ID/surveillance use (engineering summary, not legal advice). **COMPOSITIONAL-ATTRIBUTE DESIGN adopted (user idea, endorsed):** user sets hair color/style, clothing, skin tone, body size via described options; build as **disentangled LAYERS / separate modules** (beats monolith for control) — body shape=Anny semantic sliders (free), skin/face=texture layer, **hair=separate module (body models don't do hair)**, **clothing=separate garment layer (the hard one: drape over varied bodies + deform under motion)**, text→attributes=small conditioning model/structured UI. Honest hard parts: garment fitting across shapes, hair/cloth motion deformation, compositing multiple splat layers within the phone gaussian budget (D50). **ROADMAP:** Phase 0 (cheap gate, FIRST) = animated LHM-sample avatar through the phone splat test (D50 Tier-2) — kill/greenlight before training; Phase 1 (no-train) = stand up Anny+Anny-One+fork LHM, run reference weights for the quality bar, close the 2 verifies; Phase 2 = clean retrain (SMPL-X→Anny, on Anny-One) = single-image→animatable avatar; Phase 3 = 4D/clean-motion + on-device playback; Phase 4 = hair/clothing/skin as controllable layers. Start Phase 0 + Phase 1 in parallel. **Still a DEFERRED-PREMIUM R&D track relative to the D35 mesh-first platform MVP; the humanoid avatar generator is not the lead product.** (§0.5, §3, §0.9; builds on D53/D54/D55/D56/D49/D50, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D56 — Anny confirmed clean AND a hidden-NC TOPOLOGY TRAP identified (web-verified, 2026-07-01).** Verified the D55 Anny finding + a critical sub-license landmine the user surfaced. **Anny (`naver/anny`, Apache-2.0, arXiv 2511.03589) is genuinely clean because it's SCAN-FREE** — built from MakeHuman/MPFB2 (CC0) + WHO anthropometric calibration (semantic age/height/weight/muscle sliders, infant→elder, native Mixamo-rig compat), so it never touches the proprietary 3D scans that bind SMPL/SMPL-X/FLAME to Max-Planck NC terms. **THE TRAP (D41-class — headline-clean, downstream-NC):** Anny's **`smplx` interoperability topology is download-for-NON-COMMERCIAL-use-only**; its purpose (drop Anny into existing SMPL-X benchmark codebases) is exactly the tempting path that silently re-introduces NC. **SHIP RULE: build the LHM/HMR pipeline on Anny's DEFAULT (13,380-vert/163-bone) or `soma` topology (Apache-2.0, adapted from NVlabs SOMA-X) ONLY; NEVER enable the `smplx` flag in any code path touching a shippable artifact** (dataset-gen, tracking, or released weights). Verify per-download notices + SOMA-X's own NVlabs terms at integration. **Payoff:** Anny is a fully-differentiable PyTorch mesh → passes gradients to an image encoder like SMPL-X → **Anny(default/SOMA) + clean-retrained LHM = the first fully commercially-clean, end-to-end, single-photo animatable-avatar path this project has found** (still a deferred separate track per D55, but now legally viable end-to-end). ([[licensing-landmines]] updated with the topology trap.) (§0.5; builds on D55, refs D41 hidden-NC precedent)
- **D55 — Deep-research VERIFIED the L4GM numbers, the fidelity levers to beat it, and a clean-license humanoid path (Anny + LHM) (2026-07-01).** 5-angle deep-research, 23 primary sources, 24/25 claims confirmed 3-0. **L4GM exact (verified):** LGM asymmetric U-Net (6 down/1 mid/5 up, ch [64,128,256,512,1024,1024]) + temporal self-attn after each cross-view attn (~0.4–0.5B backbone; papers report architecture NOT a headline param count — param count is not the metric these models optimize); **hard-fixed 65,536 gaussians/frame (128×128×4), 14 params each**; **256×256 input, 512×512 supervision**, 16-frame clips, 0.3s/pass. **License = weights CC-BY-NC-SA-4.0 "research only" + trained on non-commercial Objaverse-4D → commercially DEAD; code Apache-2.0 → architecture copyable, retrain-from-scratch-on-clean-data required** ([[licensing-landmines]] updated). **Blur root-cause CONFIRMED:** 256/512px caps high-freq + fixed 65K budget + single-pass pixel-aligned from a 128×128 feature map. **DATASET SCALE BAR (sobering):** Objaverse-4D = 44K objects × 110K animations × 48 views → 12M videos/300M frames, rendered Blender-EEVEE in **3 days on 200 GPUs** — i.e. ~200 GPU-days just to RENDER before any training (reframes D53/D54 "rent GPUs no problem"). **FIDELITY LEVERS to beat L4GM (verified, = the architecture the dataset must feed):** (1) **untie gaussian budget from a fixed set** — per-pixel unprojection (GS-LRM 2–4 views 0.23s/A100; GRM 4 views ~0.1s) scales budget with input res × view count; (2) per-pixel's own flaw (duplicated gaussians, tied to pixels not scene complexity) fixed by **F4Splat** (predicted per-region densification, budget controllable at inference w/o retraining); (3) **decouple appearance-res from geometry via per-primitive textures — LGTM (Apple+HKU) hits 4K with few primitives, LPIPS −23–75%** (the "raise resolution not gaussian count" lever); (4) **near-optimization quality at feed-forward speed ALREADY EXISTS — DGS-LRM (Meta, NeurIPS 2025) = first feed-forward DEFORMABLE 3DGS from monocular video, "comparable to optimization-based methods," ~+3dB PSNR over L4GM** (validates hybrid init+refine AND confirms **target a DEFORMABLE rep [canonical+deformation], NOT the D53 "per-frame 3DGS then stitch"**; verify DGS-LRM license). → **Dataset spec: per-pixel/adaptive budget, MULTI-VIEW input (2–4), high-res supervision (≥512→4K via textures), deformable target, gsplat (Apache) rasterizer.** **MIXED-MODALITY (your any-combo input goal): decoupled, not monolithic** — multimodal front-end (text/image→synthesize views/video via diffusion) → sparse-view reconstructor (GRM: "4 views can be generated from text or a single image"; L4GM: image→video-diffusion→reconstruct); GaussianAnything (ICLR2025) does shared-latent point+text+image but true shared-latent/modality-dropout training was thinly evidenced → **build ONE reconstructor, swap front-ends per modality.** **HUMANOIDS — verdict UPGRADED from D54's "avoid":** parametric composition confirmed right + SMPL/SMPL-X/STAR (academic-only, commercial via Meshcapade)/FLAME/MANO/AMASS all NC — BUT a **clean path EXISTS: Anny (NAVER Labs Europe, Apache-2.0) = commercially-clean parametric human body model** ([[licensing-landmines]]), and **LHM (single-image→animatable 3DGS avatar, 2–6.5s, 0.5/0.7/1B) = the humanoid-specific feed-forward architecture** to retrain clean on Anny (verify LHM's own license). → **humanoids = a SEPARATE, DEFERRED but now-VIABLE track** (clean foundation exists), still not the v1 starting point (perceptual bar + real build). **Open (unverified in this pool):** exact hybrid feed-forward+refine wall-clock/quality for 4D; exact realistic multi-GPU cost to reproduce the 300M-frame blueprint clean; LHM/DGS-LRM own licenses. **NET recommendation: start NARROW + DEEP** (few object categories, per-pixel/deformable multi-view architecture, hybrid feed-forward+short-refine) to prove quality BEFORE industrializing the 200-GPU-day render — and still behind D49's phone-playback gate. (§0.5, §3, §0.9; builds on D53/D54/D47/D48/D49, [[licensing-landmines]], [[rigged-gaussian-splatting]], [[generation-pipeline-reality-check]])
- **D54 — Multi-modal "full-context" dataset idea + L4GM-quality diagnosis: the two real FIDELITY levers are dense views + mesh GT; quality is capped by ARCHITECTURE not dataset; L4GM is blurry by design, not from a pixel cap (2026-07-01).** User proposed enriching the D53 dataset so each object carries **full context** — self-authored motions, dense angle images (more views than the video), physics (where applicable), 3D mesh, and text describing object/motion/POV — asking if it yields better 4DGS; separately observed **L4GM looks bad** and asked if they "limit pixels to keep motion smooth," concluding a bespoke dataset means "train our own model, rent GPUs, no problem." **DIAGNOSIS — why L4GM is blurry (architectural, NOT a dataset problem, NOT a deliberate pixel cap):** (1) it's a **feed-forward amortized reconstructor** (~1s/asset) → trades fidelity for speed *by design*; the same input optimized per-scene (4D-GS ~30min) looks far better — this is the fundamental quality/speed axis, L4GM sits at the fast/blurry end; (2) **fixed low gaussian budget tied to LGM's limited-resolution pixel-aligned feature grid** → detail is capped *before* data enters; more data can't lift an architectural cap; (3) **single input view** → hallucinated unseen sides come out soft/wrong. **On "limit pixels for smooth motion" → NO:** smoothness comes from **temporal interpolation between keyframe gaussian sets + a temporal-consistency layer**, not throttled resolution (grain of truth: low output res makes things blobby which *hides* flicker — a side-effect of low capacity, not a technique). Sharpness (gaussian budget + supervision resolution) and smoothness (deformation/interpolation model) are governed by different things. **DATASET MODALITIES RANKED by real FIDELITY impact:** 🟢 **dense angle images** = biggest lever (more view coverage → less under-constraint → sharper unseen sides); 🟢 **3D mesh GT** = strong lever (depth/normal/occupancy supervision + regularizes gaussian positions, kills floaters); 🟡 **custom motions** = coverage + clean license (broadens motion distribution, doesn't sharpen a frame); 🟡 **physics** = capability not fidelity (plausible trajectories, orthogonal to splat sharpness); 🟡 **text** = control/conditioning for the text→4D input path, not a quality lever. → of the five, only **two are fidelity levers (views + mesh)**; the rest add capability/coverage. **THE REFRAME (critical):** a rich dataset is **necessary but not sufficient** — L4GM would NOT become sharp fed this data; its ceiling is architecture. Quality is actually capped by **(a) representation & budget** (more gaussians / higher-res or deformable-canonical rep vs per-frame), **(b) supervision resolution** (high-res rendering loss + LPIPS + geometric losses vs mesh GT — supervise at 256px → get 256px quality regardless of data), **(c) input constraint** (multi-view *input* is far easier to make sharp than single-view — decide if the product is single-image→4D [L4GM's hard mode] or multi-image→4D), **(d) optimization vs feed-forward** (biggest jump = **hybrid: feed-forward init + short per-scene optimization refine** — near-opt quality without the full 30min). The hard/uncertain part is designing an architecture that can EXPLOIT dense views + mesh, not collecting the data. **SCALE CHECK on "rent GPUs, no problem":** training GPU-hours are ~a third of the real cost — the underestimated parts are (1) **dataset generation** = a multi-TB rendering+storage pipeline (dense views × motions × frames × physics), itself large compute + months of engineering; (2) **architecture R&D + many training-run iterations** (L4GM was a team+NVIDIA-compute effort, not one run); (3) an **objective eval harness** ("is this actually sharper than L4GM?"). Doable, and the proprietary clean 4D dataset is a genuine moat/asset — but it's a research-team-scale commitment. Runtime/product gates (D47/D48) unchanged → still deferred-premium; D49 playback test still first. **Offered a focused deep-research pass:** (a) L4GM's exact architecture / gaussian budget / training resolution (pin *why* it's blurry), (b) current fidelity levers in feed-forward 3D/4D recon (GRM, LGM successors, hybrid init+refine) — so the model gets designed, not just the data. (§0.5, §3, §0.9; builds on D53/D47/D48/D49, [[generation-pipeline-reality-check]], [[rigged-gaussian-splatting]])
- **D53 — Synthetic-data plan to train a clean-license feed-forward text/image/video→4DGS model EVALUATED: feasible (it's the L4GM recipe) but improves only the generation axis, and the cleanliness bottleneck moves off the render engines (2026-07-01).** User proposed: rig Objaverse (or bespoke) objects, animate them in **Unreal / NVIDIA Omniverse Replicator / Unity Perception**, hang multiple cameras for a multi-view dataset, turn each motion frame into 3DGS, stitch → 4DGS, and train a feed-forward `text/image/video→4DGS` model — motivated by the lack of a clean commercial dataset (vs SV4D's "research purposes" flag D51, L4GM NC D47). **VERDICT — feasible, and it's exactly how the SOTA feed-forward 4D models were built:** **L4GM was trained entirely on multi-view renders of animated Objaverse objects** (TRELLIS/LGM/GRM likewise on Objaverse MV renders) → the user independently re-derived L4GM's methodology (its paper = the blueprint AND the failure-mode list; read first). **The strong insight:** synthetic rendering gives *perfect* ground truth, so multi-view **AND temporal consistency is true by construction** — this dissolves the exact "the research problem" D49 named (SV4D/CAT4D exist *only* to enforce consistency from real monocular video; synthetic makes it free). **Where "commercially clean" actually breaks (the bottleneck moves OFF the engines):** the three tools are the least of it — Replicator is purpose-built for synthetic data (your renders of your assets are yours; avoid NVIDIA SimReady asset libs), Unity Perception = Apache-2.0, Unreal = per-seat for non-game industrial, free pre-revenue (verify current terms). The real exposure: **(1) MOTIONS = the unflagged landmine** — SMPL/AMASS mocap is NON-COMMERCIAL ([[licensing-landmines]]), Mixamo is gray for *training*; procedural/self-authored motion is clean → audit the motion source with render-stack rigor; **(2) Objaverse** must be filtered to **CC0/CC-BY** (§4a; mostly static+unrigged, so auto-rig via UniRig adds quality variance); **(3) the per-frame fit must use gsplat (Apache), not the Inria rasterizer** (D41 trap); **(4) patents ≠ copyright (D48)** — clean data clears copyright, NOT freedom-to-operate on the 4DGS/deformation method. **Technical weak points:** (a) **"per-frame 3DGS then stitch" is the wrong supervision** — independent per-frame fits have no correspondence → flickering flipbook, huge on disk (the D48 63MB/frame failure mode); prefer a **deformable rep (canonical + deformation field)** as pseudo-GT, or better skip pre-fitting and **train with a rendering loss vs the GT views** (LGM/L4GM style — the MV video *is* the supervision); (b) **sim-to-real domain gap** → object-centric, fragile on real user photos/video (L4GM's documented weakness; Replicator domain-randomization narrows but won't close it); (c) **motion diversity/naturalness on arbitrary auto-rigged Objaverse is itself a bottleneck** and caps output quality; (d) **compute** — foundation-grade feed-forward 4D training = multi-GPU-weeks, NOT realistic on the shared single 5090 (can't even fully occupy it without evicting shnri's qwen) → a fine-tune/distill is realistic, from-scratch foundation is not. **STRATEGIC RE-ANCHOR (the crux):** this improves the **generation/licensing axis** (real value — a plausible route to a clean-license 4D generator, which SV4D-research-flag / L4GM-NC don't give) but **touches NONE of the runtime gates that actually decide shipping** (D47/D48: iOS renders-in-canvas-but-can't-world-anchor, 5–30× mesh weight, mesh-first governs) → a perfect clean generator still lands as a **deferred-premium un-anchored web hero feature**, not an MVP unblock. **And D49's action item still comes first: prove ONE 4D output plays back on a real phone BEFORE building any generator.** **RECOMMENDATION:** read L4GM's data pipeline; don't train from scratch — use synthetic to build a clean eval/fine-tune set and distill/fine-tune a backbone; if fitting pseudo-GT target a deformable rep with gsplat (or train with rendering loss, no fit); audit the motion source; sequence the whole thing behind the D49 playback test. Open verify items (offered a deep-research pass): current 2026 commercial terms for Omniverse Replicator / Unreal-industrial / Unity Perception, and L4GM's exact training-data recipe + license. (§0.5, §3, §0.9; builds on D47/D48/D49/D51, [[licensing-landmines]], [[generation-pipeline-reality-check]], [[rigged-gaussian-splatting]])
- **D52 — SV4D 2.0 Stage-A RUNS end-to-end on the 5090 — first generated 4D output (2026-07-01).** During a GPU window (qwen idled out → 31GB free, grabbed it), SV4D2 completed `camel.gif` → **4 novel-view videos** (`generation/sv4d2_out/000000_v001..v004.mp4`, ~7 min, clean exit, VRAM released). Backed up into the repo (durable) + a side-by-side `view.html` viewer (localhost:8098). **The reusable technical win = the Blackwell (sm_120) attention fix:** SV4D2 uses head_dim-512 attention, which xformers CANNOT run on capability-12.0 (flash needs ≤256, cutlass needs ≤sm9.0) → the fix is a monkeypatch routing `xformers.ops.memory_efficient_attention` → torch native SDPA (math backend handles any head_dim), and it must be **rank-aware** (spacetime attn passes 4D `(B,M,H,K)`, sgm CrossAttention passes 3D `(B*heads,M,dim)`). Lives in `generative-models/run_sv4d2_patched.py` — same monkeypatch philosophy as D41's gsplat swap; **applies to any sgm/SVD-family model on Blackwell.** **Stage A = DONE (proof SV4D2 works on our HW).** Remaining: **Stage B** (multi-view videos → dynamic splat, gsplat-rasterized 4D fit — the real deliverable), then the image→video front-end for single-image input. Still gated by the shared GPU (opportunistic windows only; don't evict shnri's qwen). Supersedes D51's "validated to model-load." (§0.5; builds on D51/D41, [[licensing-landmines]])
- **D51 — SV4D 2.0 self-host build COMPLETE on the 5090 (validated to model-load); blocked only by shared-GPU contention; key finding: open release = multi-view VIDEO only, splat needs a Stage-B recon (2026-06-29).** Per user's "build out SV4D" go-ahead, stood up **SV4D 2.0** (Stability, `stabilityai/sv4d2.0`, `generative-models` repo): env cloned from `trellis2` (preserves the D44 Blackwell torch 2.11+cu128 / xformers 0.0.35 combo), inference deps installed UNPINNED (skipped the repo's ancient `triton==2.0.0`/`transformers==4.19.1`/`torchdata` pins that would break Blackwell), `sgm` + deps import clean, `sv4d2.safetensors` (12GB) downloaded (HF token from `generation/.env`, user `yyousef4u`, access OK). Ran end-to-end to model-construction — only failure = CUDA OOM. **KEY FINDINGS:** (1) **SV4D2's open release outputs novel-view `.mp4` VIDEOS, not a splat** — the "4D asset generation" reconstruction is NOT in the repo. Getting a playable dynamic splat needs a **Stage-B 4D-gaussian fit** (joint spatio-temporal opt over the ~5–9 views/frame, since per-frame is too sparse), which most 4D-GS code rides the **Inria NC rasterizer** → must swap in **gsplat (Apache)** like D41. So "SV4D" = Stage A (video→multiview-video, set up) + Stage B (multiview-video→splat, the real remaining work). (2) **License flag:** SV4D2 README says "for research purposes" — verify the model-card's actual commercial terms (Community License vs research-only) before treating it as a production generator; if research-only it's prototype-only (reinforces D48 clean-room/alternative). (3) **Infra constraint:** the box is a **shared multi-user server** (users `shnri`, `sov1`, `sov2`); the single 5090 is occupied by **`shnri`'s live, publicly-tunneled qwen3.6:27b (24GB via Ollama, `epic-birthd-ai`)** — can't coexist with SV4D2 (~14–20GB) on 32GB. Per user: **do not stop/evict anything not ours** (even opportunistic idle-grabbing risks OOM-ing shnri's service on its next reload). SV4D run is **parked, fully ready, resumes instantly** when the GPU frees (a `nvidia-smi` watcher is armed to notify on ≥20GB free). Build lives in conda env `sv4d` + `/home/sov2/projects/generative-models`. (§0.5; builds on D47/D48/D49/D50, [[licensing-landmines]])
- **D50 — Tier-1 phone render-floor test PASSES empirically: 200k-splat object @ DPR 2.0 holds 45–60fps sustained over 2 min on a real phone, no thermal decay (2026-06-29).** Built a phone playback benchmark (`generation/phone_splat_test.html` + a deployable copy at `~/4dgs-splat-test`, repo github.com/yousef-yy4u/4dgs-splat-test) — a real 200k-gaussian 3DGS splat (`benchmark/assets/robot_crab_200k.ply`) rendered via mkkellogg GaussianSplats3D (WebGL/three.js), **auto-orbited to force a depth re-sort every frame** (the documented mobile bottleneck + the per-frame work a moving/4D scene imposes), with a live FPS/ms HUD, ×1/2/3 copy multiplier, and DPR control. **Result (after controlling for a low-battery throttling confound that initially pinned both devices to a stable 30):** laptop = 60fps stable; **phone (iPhone 14, A15, 2022 — mainstream not flagship-latest) = 45–60fps fluctuating, SUSTAINED over 2 min with no decay, at "max settings" = 3× copies (~600k gaussians) @ DPR 2.0 with auto-orbit** (full native res + continuous re-sort = the heaviest case tested). **Interpretation:** the render floor (rasterize + continuous re-sort of ~600k gaussians across 3 objects) is empirically NOT the wall D47 feared — and clears with HEADROOM via plain WebGL (not even WebGPU), on a 3-yr-old phone, at a count that exceeds §2's "100–300k/object, 750k=diminishing-returns" budget. **Refines D48's "rendering wall overstated" from argued to MEASURED.** Caveats (do not over-claim): (1) this is the FLOOR — a true 4D asset adds per-frame deformation eval on top (cheaper than the sort, but nonzero) → real 4D lands somewhat below; (2) ONE device (iPhone 14 / Safari) — weaker Android mid-range will do worse → LOD/quality tiers still mandatory (§2/§2a); (3) streaming/download size (.spz/.sog) untested + separate (a network question, not GPU). **Deploy lesson:** static splat viewers need permissive headers — Railway's default CSP blocked GS3D's blob: web worker AND its WASM sorter; fix = `worker-src 'self' blob:` + `script-src … 'wasm-unsafe-eval'` (see `~/4dgs-splat-test/server.js`), or just use GitHub Pages (no CSP). **NEXT (de-risk ladder): Tier-2 = regenerate a skinned animated splat + run `poc_splat_smooth.html` (per-frame LBS deformation = the real 4D cost); Tier-3 = compare compressed-4D file sizes to the download budget.** Floor passing keeps the D48 verdict intact: 4DGS animation is a credible PREMIUM un-anchored WebGPU/WebGL-device hero feature worth the spike — now with the first empirical evidence the runtime is reachable. (§3, §2a, §0.5; builds on D47/D48/D49, [[rigged-gaussian-splatting]])
- **D49 — Two concrete image/video→4D papers evaluated (SV4D, CAT4D); user's proposed pipeline IS the SOTA architecture but its crux is cross-view consistency; de-risk PLAYBACK before building generation (2026-06-29).** User proposed `single image → multiple views → per-view videos → 4DGS core` and asked "can't we just use" arXiv 2407.17470 + 2411.18613. **Identified:** **2407.17470 = SV4D (Stability AI + Northeastern)** — monocular video/image → *unified* diffusion → novel-view videos → optimize 4D; **weights under Stability AI Community License (free incl. COMMERCIAL under ~$1M ARR, Enterprise license above; non-OSI, revocable, attribution) → actually usable today for a pre-revenue startup (yellow flag, not red); verify exact HF model-card terms + check SV4D 2.0**. **2411.18613 = CAT4D (Google DeepMind)** — single image/video → multiview-video diffusion → per-scene opt (deformable 3DGS); **NO code/weights released (project page only) + rides Google's proprietary unreleased diffusion model → NOT usable, it's a quality north-star not a tool**. Both ride the 4D-GS/Inria lineage (gsplat substitutes for rendering). **User's pipeline shape is CORRECT — it's literally what these papers do — BUT the "videos from each view" step is THE research problem:** animating each view *independently* yields cross-view temporal inconsistency (arm raises at frame 10 in one view, 14 in another) → 4DGS fit produces floating-gaussian garbage. SV4D/CAT4D's whole contribution is enforcing multi-view AND temporal consistency JOINTLY (SV4D = unified diffusion over view×time; CAT4D = alternating MV/temporal sampling) — you can't glue independent off-the-shelf parts; you need a consistency-enforcing core = use SV4D's model OR retrain your own (clean-room, D48 pt2). **Two invariants regardless of model:** (1) these are per-scene OPTIMIZATION, **minutes/asset** (SV4D opt; CAT4D ~25min/A100) — an async premium tier, NOT free/instant feed-forward; (2) generation quality does NOT touch the D48 runtime gates (60fps on a phone + iOS=canvas-viewer-not-AR). **RECOMMENDATION — de-risk in REVERSE pipeline order:** before building any generation, take ONE SV4D output → a WebGPU mobile splat player → **test on a real mid-Android + iPhone** (fps? thermal throttle after 2min? compressed size? AR-placeable on iOS or canvas-only?). Only if playback passes do you build the single-image→consistent-MV-video→4DGS pipeline. Generation is the part that provably works (published); phone playback is what decides if it ships — build the test that can KILL the idea first. (§3, §0.5; refines D47/D48, builds on D34, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D48 — D47 REFINED after user due-diligence (2 GitHub repos + a streaming-cost analysis): the rendering "wall" was overstated, but 4DGS stays a deferred PREMIUM R&D track, not a pivot (2026-06-29).** Three corrections/confirmations to D47: **(1) "Apple SHARP" identified — and it's STATIC, not 4D.** [apple/ml-sharp](https://github.com/apple/ml-sharp) = "SHARP: Sharp Monocular View Synthesis," single-image→**static** 3DGS (<1s) — same category as TripoSplat, NOT dynamic. License = **Apple ML Research Model License, non-commercial ("commercial exploitation prohibited")**. The [AndriiShramko/4DGS-Video-Generator](https://github.com/AndriiShramko/4DGS-Video-Generator) "4D" repo fakes 4D by running SHARP **per-frame** and stacking PLYs (no temporal model → flicker; **~63 MB/frame, 1.18M gaussians/frame → ~19 GB uncompressed for 10s**; the worst-case representation for streaming; MIT app code but NC model → unshippable). **(2) Clean-room "copy the model not the code" = legitimate but a real build, gated on PATENTS not copyright.** Copyright protects code (expression) not the paper's method → reimplementing from scratch is generally non-infringing (precedent: **gsplat Apache = a clean reimpl of Inria's NC rasterizer**). BUT you must retrain weights from scratch on commercially-clean data (real project) AND clear **patent freedom-to-operate** (NVIDIA/Apple file aggressively; "we wrote our own code" is no defense vs a patent). So 4D-gen clean-room = a months-long R&D commitment, not a loophole — the exact thing self-host-TRELLIS.2 + clean components was chosen to avoid. Static splats need none of this (TripoSplat MIT + gsplat Apache already clean). **(3) Mobile rendering — D47 OVERSTATED the wall (correction owed):** WebGPU GPU-radix-sort is the real fix for the CPU-sort bottleneck and is **no longer desktop-only — iOS Safari shipped WebGPU in iOS 26 (2025)**, Android Chrome has it; D47's "WebGPU desktop-only" came from *Gracia's product matrix*, not a platform limit. So dynamic-splat mobile rendering is "hard/bleeding-edge," NOT "impossible." And the streaming-cost nightmare ($10.8k/mo, 13.5 GB/viewer-hour) **models the WRONG usage** — that's continuous volumetric-video streaming; OUR asset is a **short looping object, download-once-cache-loop** (~20–80 MB for a 5s clip with a true 4D codec), so egress is "MB/view" on a free/cheap-egress CDN (Cloudflare R2 ≈ $0 egress, Bunny ≈ $0.01/GB), NOT a per-hour bleed — a CDN-choice problem, not fundamental. **What still gates it to deferred-premium (not pivot):** (a) **iOS can render a splat in a canvas but canNOT world-anchor it in AR** (no WebXR; AR Quick Look = USDZ/mesh) → on iPhone 4DGS = turntable/embed VIEWER, not "view in your space" — fine for un-anchored web, fatal for the anchored-marker surface on ~half of users; (b) asset still ~5–30× a mesh GLB + sustained-60fps/thermals on LOW-end phones unproven → ship with MESH FALLBACK regardless; (c) tooling is assemble-the-research-repos, not an SDK like model-viewer that works on every phone today; (d) generation still NC → see (2). **UPDATED VERDICT (supersedes D47's "research demo, no"):** 4DGS animation is a **credible premium, un-anchored, WebGPU-device hero feature worth a real spike AFTER the mesh MVP ships** — gated on a clean-license gen path (likely self-built), iOS=viewer-not-AR, and a true 4D representation + mesh fallback. **Mesh-first MVP still ships first because it works on EVERY device today off-the-shelf.** D46 plan unchanged; this only re-grades the animated-splat track from "deferred/fragile" to "deferred premium R&D with a clearer worth-it gate." (§3, §0.5, §0.6, §0.9; refines D47, builds on D33/D34, [[licensing-landmines]], [[rigged-gaussian-splatting]])
- **D47 — 4DGS-animation pivot EVALUATED and DECLINED; runtime playback (not generation) is the wall — mesh-first D46 holds (deep-research, 2026-06-29).** User proposed pivoting from skinned-mesh animation to a **pure 4D Gaussian Splatting** generate+play pipeline (text/image→GenAI-video→bg-cut→4DGS engine→"pure 4DGS web viewer/splat asset"), with **L4GM** (feed-forward, ~1s) as a free tier and **"Apple SHARP"/4D-GS** (per-video optimization) as premium. Research verdict (5 converging primary/secondary sources; adversarial-vote step cut short by a token-limit but search/fetch completed): **the proposal optimizes Latency A (generation, on our 5090) when the wall is Latency B (playback on the customer's phone) — the exact trap §3 names.** **Generation = feasible:** L4GM is real (single-view video→4D gaussians, ~1s) but trained ONLY on synthetic Objaverse → object-centric, fragile on arbitrary real user video; output is 4D gaussians not mesh. 4D-GS optimization is **~30 min/scene** (paper figure on a 3090; user's "3–5 min" optimistic) and **built on the Inria `diff_gaussian_rasterizer`** (the D41 non-commercial trap). **Runtime playback = a brick wall for our mobile-browser distribution:** (a) 4D-GS's "real-time" 82fps is 800×800 on a DESKTOP RTX 3090, not a phone; (b) **Gracia AI** (most advanced *commercial* 4DGS streaming) **requires a WebGPU DESKTOP browser — smartphones explicitly unsupported — at ~80 Mbit/s** for 120k splats/frame; (c) the web-splat viewer ecosystem (SuperSplat/PlayCanvas/three.js/Babylon/Gauzilla) is **static-oriented**, no mobile dynamic benchmarks; (d) 2026 industry state: dynamic 4DGS is early-commercial, **confined to film/sports broadcasting, not consumer mobile**; (e) **iOS has NO path** — WebKit-only, no WebXR, AR Quick Look is USDZ/mesh. **Licensing = non-commercial at every layer** (L4GM = NVIDIA, almost-certainly NC; 4D-GS = Inria NC rasterizer; Gracia = proprietary) — **no clean MIT/Apache end-to-end dynamic-splat gen+playback stack exists.** **Could NOT verify (token-limit, don't change verdict):** L4GM's exact license (treat as NC blocker until proven), and **"Apple SHARP" — I could not find a 4DGS framework by that name from Apple; likely a conflation, do NOT architect a tier around it.** **DECISION: do not pivot to pure-4DGS output** — it breaks the every-phone distribution that IS the product and the licensing is dirty top-to-bottom. **Animated splats stay deferred R&D (D33/D34)** — revisit ONLY when BOTH a mobile-capable dynamic-splat web renderer AND a clean-license gen+playback stack exist. **D46 mesh-first plan stands**; the "wow" premium tier available TODAY is **STATIC splats (TripoSplat, single hero object)** which at least render in current web viewers, NOT animated. (§3 two-latencies, §0.5, §0.6, §0.9; builds on D33/D34/D46, [[licensing-landmines]], [[rigged-gaussian-splatting]], [[generation-pipeline-reality-check]])
- **D46 — Generation architecture LOCKED: self-host is the user-facing ENGINE, the API is a back-office spare tire; MVP animation = presets + custom-upload; 2D = billboard + 2D→3D (2026-06-29).** Resolves "which vendor + what do we build" after D45. **The reframe (the real answer to 'which one'):** the user-facing flow (*user uploads → we generate → they publish*) runs ENTIRELY on the self-hosted stack — not just for $0 cost but because routing end-users' generation through Meshy/Tripo IS the "make the Service available to third parties / resell access to the Service" both ToS restrict (D45). So **local = the engine, Tripo API = back-office overflow/quality spare tire on OUR account, never wired into the user path** (and even that wants Tripo's written OK at resale scale). **Meshy is DROPPED** — its only edge was cheap rigging, but its rigger needs textured *humanoids*, while self-hosted **UniRig (MIT) rigs arbitrary categories** for free; Meshy adds nothing the local stack doesn't do better + cleaner. **Final stack:** (1) **Mesh = default "Standard" lane** → TRELLIS.2 self-host (every user-facing gen); (2) **Splat = premium "Photoreal" lane** → **TripoSplat (MIT) self-host**, single-image feed-forward (up to 262k gaussians, no per-asset optimization/cleanup = inherently low human-error), **kept as static hero/premium tier NOT the default** (mesh-first D34 still governs: splats fill-rate-expensive on phone/glasses, animated splats deferred). One knob, two lanes, same single-image input. GS is self-host regardless since neither API sells splatting (D45). (3) **Rig/animate** → UniRig (MIT) self-host. (4) Tripo API = spare tire. **Animation MVP scope (user decision):** **presets + custom upload** — Tier 1 plug-and-play: auto-rig (UniRig) → curated motion library (idle/wave/spin/bounce/walk) + transform anims (rotate/bob/pulse/float) on ANY asset incl. flat 2D signs (no rig needed); Tier 2 escape hatch: users upload their own rigged/animated GLB/FBX (validate+ingest). **Keyframe STUDIO deferred to post-MVP** ("we can build a studio later"); **AI-assist (text→motion, video→mocap→retarget) deferred further** (research-grade per D42/findings). Animation = mesh LBS (cheap to render); animated splats stay deferred (D34). **2D-drawing scope (user decision):** **billboard + 2D→3D** — (a) 2D drawing → flat animated BILLBOARD (transform anims; perfect for the storefront-signage beachhead), (b) 2D drawing → 3D object (image-to-3D) then animate as 3D. **Live2D-style articulated 2D-character rigging DEFERRED** (separate deep feature; revisit only if signage demand pulls it). Refines §0.5 (animation editor → presets+upload for MVP; splatting → TripoSplat two-lane). (§0.5, §0.9; builds on D34/D42/D43/D44/D45, [[licensing-landmines]], [[generation-texture-method]])
- **D45 — The Tripo-vs-Meshy "buy comparison" (D43/D44 open item) is now done via deep-research (mid-2026, 5-angle fan-out, 24/25 claims 3-vote-verified). Two findings reshape the make-vs-buy call; net = KEEP self-hosted TRELLIS.2 as default, buy only for overflow.** Context: user proposed "scrap local generation entirely, offload everything to Meshy or Tripo incl. splatting." **Finding 1 — splatting CANNOT be bought:** neither Meshy nor Tripo sells Gaussian-splatting as a billed API task (both pricing pages are mesh-only). Tripo's **TripoSplat** (single image→3D Gaussians, .ply/.splat, up to 262k gaussians) exists ONLY as **MIT-licensed open-source weights you self-host** (HuggingFace `VAST-AI/TripoSplat`, ComfyUI-native) — clean commercial, but a self-host job regardless of vendor. So "offload everything incl. splatting" is impossible; splatting stays self-hosted. **Finding 2 — our reseller model bumps both vendors' anti-resale clauses:** paid tiers grant the ACCOUNT HOLDER (us) full ownership/commercial rights to output (free tiers = CC BY 4.0, attribution-to-vendor, unusable), BUT **[CC scope clarified from the live ToS, 2026-06-29]: the CC license attaches to the OUTPUT (generated model) and is triggered by the FREE TIER — NOT by uploading. Your INPUT never becomes CC just by entering the platform; uploading grants only a narrow service-operation license (Meshy §3.2 "as necessary to provide the Service"; Tripo §5.2.2 service-provision-only + "will not use Inputs/Outputs as training data"). On a PAID account both input (private) and output (owned, full commercial/resale) are clean — nothing is CC. Free-tier traps: Meshy free output = CC BY 4.0 even for a derivative of YOUR OWN uploaded asset; Tripo free (§5.2.1) is harsher — Tripo retains broad rights over your INPUTS too; and Meshy Community publishing (§3.3) invokes CC0 (gives the asset away). → Production = always a PAID account, never publish to community.** Separately, **Meshy ToU §2.6(v)** bars "resell or redistribute the Service or access to the Service," §2.6(iv) bars competing products, **§2.6(xi) bars using generated assets to train/improve a competing AI model**; **Tripo terms** bar making "the Generative Service available to any third party, incl. end users, without prior written authorization" + bar building a competing product. Reading: we may **resell finished ASSETS**, but our "customer uploads image → we generate → they own it" flow likely needs an **explicit enterprise/white-label agreement** on either vendor; and §2.6(xi)/Tripo's compete-ban directly threaten using purchased output to train/improve TRELLIS.2-class self-host (must NOT). **Pricing (both ~$0.30/textured image→3D mesh):** Tripo flat $0.01/credit — H2/H3 textured 30cr ($0.30), P1 50cr; no-texture 20/40cr; standalone texturing 10cr; **rig 25cr + animation 10cr/retarget = 35cr**. Meshy — textured 30cr (Meshy-6/low-poly) or 15cr (other models), no-texture 20/5cr; **rig 5cr + animation 3cr = 8cr (4× cheaper than Tripo on the animated step)**. Subscriptions: Meshy Pro $20/mo→1,000cr, 10 concurrent; Tripo Pro ~$14–20/mo→3,000cr, Max→25,000cr, Team→45,000cr (more credits/$ + scales higher). Gaps NOT published by either: API rate limits, max texture res, per-asset latency, named volume discounts (only Meshy "10 concurrent" + Meshy output formats glb/obj/fbx/stl/usdz/3mf surfaced) → require a sales conversation. **RECOMMENDATION (refines D43/D44): do NOT fully scrap local.** (1) **TRELLIS.2 stays the default generation backend** — $0/asset, no licensing strings, no anti-resale exposure, quality already validated (D44); buying is for quality/throughput OVERFLOW only. (2) **If buying one mesh vendor, pick Tripo** — transparent $0.01/credit maps onto per-asset cost accounting, larger volume bundles, and its ecosystem gives the MIT TripoSplat self-host path (one mental model: mesh-buy + splat-self-host). (3) **Meshy only as a cheap rig/animation add-on** IF its rig quality holds — but its rigging needs a *textured humanoid* GLB with clear limbs (likely too narrow for arbitrary signage/product assets; verify before relying). (4) **Splatting = self-host TripoSplat (MIT) regardless** — no one sells it. (5) **Before any contract, get white-label/reseller terms in writing from both** — our exact flow needs it, and that's where unpublished volume/throughput discounts live. Net stack: **TRELLIS.2 (self-host default) + TripoSplat (self-host splat) + Tripo API (on-demand overflow/premium mesh)**, Meshy as possible rig/anim lever. The "offload everything to a managed API" premise doesn't hold: splatting can't be bought + the reseller model needs an enterprise agreement either way → self-host's clean-license + no-anti-resale advantages get STRONGER, not weaker. Verify pricing/ToS live before contracting (time-sensitive, frequent promos). (§0.5, §0.9; builds on D43/D44, [[generation-texture-method]], [[licensing-landmines]])
